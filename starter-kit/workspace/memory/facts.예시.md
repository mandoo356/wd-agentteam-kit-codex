# 팀 규약 — 우리 회사가 아는 것

직원 전부가 일을 시작할 때 먼저 읽는 파일. 여기가 바뀌면 다음 대화부터 반영된다.

> 이 파일은 **견본**입니다. 모듈 3에서 같은 폴더에 `facts.md` 를 새로 만들고
> 아래 항목을 본인 것으로 채우세요. (`.예시` 가 붙은 파일은 채점에 안 들어갑니다)

---

## 대표

- 이름:
- 하는 일:
- 연락처:

## 우리가 파는 것

- 주력:
- 대상 고객:
- 경쟁자와 다른 점:

## 내 회사 겉모습 (제안서·교재·프로필이 이 네 줄을 읽어 칠한다 — P1 / P1-B)

- 로고 글자:
- 진한 색:   #12314B   ← 표지 배경·표 머리
- 기본 색:   #1F5B8E   ← 제목·번호
- 강조색:    #E9A23B   ← 눈에 띄는 숫자에만
- 옅은 색:   #EAF1F8   ← 밴드 배경·표 줄무늬

## 일하는 규칙

- 결과물은 전부 `workspace/결과물/` 에 저장한다.
- 다른 직원이 이어받아야 할 일은 `workspace/inbox/<상대이름>/` 에 쪽지로 남긴다.
- 쪽지 파일 이름은 `<날짜>_<한줄요약>.md` 로 한다.
- 확실하지 않으면 지어내지 말고 대표에게 묻는다.

## 지어내면 안 되는 것

- 금액, 날짜, 고객사 이름, 숫자로 된 실적
- 모르면 비워두고 "확인 필요"라고 적는다.

## 자주 쓰는 값

| 항목 | 값 |
|---|---|
| 기본 단가 | |
| 작업 기한 | |
| 파일 저장 위치 | `workspace/결과물/` |

---

## 갱신 이력

- (날짜) 최초 작성


## 작업 보존·위험 작업 (2026-09-05)
- 삭제·이동·기존 문서 전체 덮어쓰기·팀 규약 변경 전에는 대상과 변경 내용을 알리고 사용자의 명시적 지시를 확인한다.
- 훅이 차단한 작업은 다른 도구로 우회하지 않는다. 백업과 변경 대상을 설명하고 사용자가 노트북 터미널에서 직접 실행하도록 안내한다.
- 슬랙에서 위험 작업을 대신 실행하지 않는다. 일반 문서의 필요한 수정은 apply_patch로 하고 새 직원·스킬 파일 생성은 허용한다.
- 점검.py·.codex/hooks.json·.codex/hooks/·configure_hooks.ps1은 강사가 관리한다.
- MyData의 원본은 읽기만 한다. 결과는 workspace/결과물/에 새 파일로 저장한다.
- /hooks에서 신뢰한 경우 작업 기록과 Git 자동 저장을 사용한다. 기록 실패는 숨기지 말고 보고한다.
