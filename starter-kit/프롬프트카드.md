# 기본 카드 P1~P14 + 연결·확장 카드 P15~P23 — 수강생용 (2026-09-16 판: 쓰던 스킬 가져오기 P17-2 추가)

> **직원은 5명으로 먼저 완성합니다.** 다섯 명이 제대로 도는 것을 `P14-a` 로 확인한 다음, 필요해질 때 `직원견본/추가/` 의 세 명을 **한 명씩** 붙입니다(`P14` → `P14-c`).

**쓰는 법**: 회색 상자 안을 통째로 복사 → AI 창에 붙여넣기 → **대괄호 `[ ]` 안만** 내 것으로 바꾸기.
문장을 지어내지 마세요. 카드가 정답입니다.

**P0·관문 카드**(`P8-a` `P9` `P9-a` `P10` `P10-a`)는 2026-09-07 에 없앴습니다. 같은 일을
스타터킷의 `환경점검.bat` 과 `py 점검.py 4` 가 **코덱스 없이** 합니다 — 코덱스 사용량이 그만큼 줄어듭니다.

> 붙여넣기는 검은 창에서 **마우스 오른쪽 클릭**입니다.

---

## 모듈 0 · 출발선

**카드가 없습니다 — 전날 한 줄 설치가 대신합니다.**
프로그램 설치(Node·Git·Codex CLI) · **Python 3.14 무조건 설치** · 파이썬 꾸러미 · 슬랙 열쇠 3개 · 서버 첫 기동은
전날 스타터킷 한 줄 설치(교안 4쪽)가 **묻지 않고** 처리합니다.
빠진 것이 있으면 코덱스에게 시키지 말고 스타터킷 폴더의 **`환경점검.bat` 을 더블클릭**하세요.
당일 아침은 `py 점검.py 0` 만 치면 출발선입니다.

### P15 — 구글 캘린더·지메일·드라이브 연결 확인 (ChatGPT 앱)

**조건 넷** — 하나라도 빠지면 터미널에서 안 붙습니다 (2026-09-08 강의장 원인 확인).
① Codex CLI **최신판** — 환경점검이 묻지 않고 올려 둡니다 ② **ChatGPT 계정 로그인**
③ **Pro 이상 요금제** ④ Team 계정이면 **관리자가 커넥터 허용**.

**로그인해 둔 지금 미리 붙여 두세요.** `ChatGPT → 설정 → 앱(Apps) →
Google Calendar / Gmail / Google Drive → 연결` 로 붙인 다음, **Codex CLI 를 완전히 끄고 다시 켜서**
코덱스 안에서 `/mcp` 를 칩니다. 목록에 **Google Calendar** 가 보이면 붙은 것입니다. 그다음 아래 카드로 확인합니다.
`codex.cmd mcp add` 같은 명령어 방식은 쓰지 않습니다 — 구글 클라우드에서 인증 열쇠를 만들어야 해서 강의장에서는 오류만 납니다.

```
구글 캘린더에서 오늘부터 7일간 일정을 조회해서 건수와 제목만 알려줘.
조회만 하고 등록·수정은 하지 마.

읽지 못하면 "왜 못 읽었는지"를 한 줄로 적어줘. 추측하지 말고 실제로 시도한 결과만.
```

**통과 기준** — 실제 일정 제목이 나오면 통과. `0건`이어도 조회 자체가 됐으면 통과입니다.

> ❌ **`/mcp` 에 안 보이면** 순서대로: ① 검은 창에서 `codex.cmd --version`을 확인하고 필요하면 `npm.cmd install -g @openai/codex@latest`
> ② ChatGPT 앱 연결 화면에 "연결됨"인지 (아니면 다시 연결) ③ Codex CLI 를 **완전히 끄고 다시 켜기** — 앱은
> 프로그램이 켜질 때 한 번만 목록을 적어둡니다.
>
> 💬 커넥터가 끝내 안 되면 — 문의 메일은 **P19(메일 연결)** 로 읽고, 일정은 `workspace/결과물/일정표.md` 에 적습니다.
> 캘린더·드라이브는 커넥터 없이는 방법이 없습니다.

> 💬 이 연결은 **슬랙으로 부른 직원도 그대로 씁니다**(스타터킷 2026-09-07 판). 붙인 뒤 슬랙 서버를 한 번 다시 켜세요
> (`Ctrl`+`C` → `py -3 -X utf8 server.py`).

---

## 모듈 1 · 직원 뽑기

### P1 — 내 회사 정의하기

```
workspace/memory/ 폴더에 facts.md 파일을 만들어줘.

내 직업: [강의하는 사람]
내 이름: [홍길동]
회사 이름: [길동컨설팅]
내가 반복해서 하는 일 5가지:
  1. [제안서 쓰기·문의 응대]
  2. [내 교안·PPT로 교재 만들기]
  3. [블로그 쓰기]
  4. [일정 관리]
  5. [강사 프로필 관리]

내 회사 겉모습 (제안서·문서에 그대로 쓰입니다):
  로고 글자: [GD]           ← 회사 이니셜 1~2글자
  진한 색:   [#12314B]      ← 표지 배경·표 머리
  기본 색:   [#1F5B8E]      ← 제목·번호
  강조색:    [#E9A23B]      ← 눈에 띄는 숫자에만
  옅은 색:   [#EAF1F8]      ← 밴드 배경·표 줄무늬

규칙:
- 파일 경로는 반드시 workspace/memory/facts.md
- 파일은 UTF-8 로 저장할 것 (ANSI 로 저장하면 직원이 내 이름을 못 읽는다)
- "## 대표" 라는 제목 아래에 "- 이름: 내이름" 을 한 줄로 쓸 것.
  볼드(**이름**:)나 표로 쓰지 말 것 — 그러면 슬랙 직원이 그 줄을 못 읽는다
- 금액·날짜·고객사 이름은 내가 안 알려준 건 지어내지 말고 빈칸으로 둘 것
- 색을 안 정했으면 위 예시 값을 그대로 쓰고 "나중에 바꿀 것"이라고 적어둘 것
- 다른 파일은 만들지 말 것
```

> 🎨 **색을 모르겠으면 예시 값 그대로 두고 바로 다음 카드 P1-B 로 가세요.** P1-B 가 내 색을 뽑아 이 네 줄을 바꿔 줍니다.
> 명함·홈페이지 색을 이미 알면 여기서 바로 넣어도 됩니다. **이 네 줄이 오늘 만드는 제안서·교재·프로필의 색입니다.**

### P1-B — 핵심 컬러 뽑기 (내 브랜드 색 확정)

```
workspace/memory/facts.md 의 "내 회사 겉모습" 색 네 줄을 내 브랜드 색으로 바꿔줘.

내 색의 근거 (둘 중 하나만 채운다):
  A. 로고·명함·홈페이지 캡처가 있다 → C:/Agent/MyData/Logo/ 폴더에 넣어뒀다. 파일 이름: [logo.png]
  B. 없다 → 내 업종: [기업교육 강사] / 주고 싶은 느낌 3가지: [신뢰, 따뜻함, 전문성]

순서:
1. A면 그 이미지를 열어 가장 넓게 쓰인 색을 "기본 색"으로 뽑고, 같은 계열의 더 어두운 색을 "진한 색",
   대비되는 색 하나를 "강조색", 기본 색을 아주 옅게 만든 것을 "옅은 색"으로 정한다.
   B면 업종·느낌에 맞는 팔레트 3안을 표(이름 / 4색 hex / 한 줄 이유)로 보여주고 내가 고르게 한다.
   내가 고르기 전에는 파일을 바꾸지 않는다.
2. 정해진 4색으로 workspace/결과물/브랜드_컬러.html 을 만든다 — 색 견본 4칸 +
   그 색으로 칠한 제안서 표지 1장 + 표 머리 1줄 미리보기. 인터넷 주소는 쓰지 않는다.
3. 진한 색 위에 흰 글씨를 올렸을 때 대비가 4.5:1 미만이면 진한 색을 더 어둡게 고치고 그 사실을 말한다.
4. facts.md 의 네 줄(진한 색·기본 색·강조색·옅은 색)만 바꾸고 줄 끝에 "확정 (오늘 날짜)"라고 적는다.
   다른 줄은 건드리지 않는다.
5. 끝나면 네 색의 hex 와 브랜드_컬러.html 경로를 한 줄로 알려준다.

규칙:
- 색은 반드시 #RRGGBB 여섯 자리로 쓴다
- 이 4색이 오늘 만드는 제안서(P4)·교재(P8)·프로필(S7)에 그대로 쓰인다는 것을 알고 정한다
- 로고 파일이 없는데 있다고 하지 말 것. 없으면 B로 진행한다
```

> 🎨 **명함이나 홈페이지 색을 그대로 쓰는 게 제일 좋습니다.** 로고 파일은 `C:\Agent\MyData\Logo\` 에 넣으세요 (설치 때 만들어진 폴더입니다. 전날 넣어 뒀다면 그대로).
> 끝나면 `workspace/결과물/브랜드_컬러.html` 을 브라우저로 열어 보고, 마음에 안 들면 "더 차분하게", "강조색만 주황으로" 처럼 말로 바꾸세요.
> **여기서 정한 색이 P4 제안서 · P8 교재 · S7 프로필에 자동으로 들어갑니다.** 나중에 바꾸려면 facts.md 네 줄만 고치면 됩니다.

### P2 — 첫 직원 1명 뽑기

```
.codex/agents/ 폴더에 직원 파일 1개를 만들어줘.

이 직원의 담당: [제안서 쓰기]
이 직원의 성격: [꼼꼼하고 설득을 잘하는 타입]

규칙:
- 파일 경로는 반드시 .codex/agents/staff1.toml
- TOML 맨 위에 `name =`, `description =`, `developer_instructions = """` 세 항목을 만들 것
- description 에는 "언제 이 직원을 부르는지"를 적을 것
- 담당 업무는 3~5개를 목록으로 적을 것
- 결과물은 workspace/결과물/ 에 저장한다는 규칙을 본문에 넣을 것
- 다른 파일은 절대 건드리지 말 것
```

**합격 판정** → `py 점검.py 1` … 5개 중 **4개 통과가 정상**입니다.
"직원이 5명 이상 있다" 하나만 ❌ 인 게 맞아요. 아직 한 명이니까요.

### P3 — 5명으로 늘리기 (견본 데려오기 — 프리랜서 강사 핵심 세트)

> 📦 스타터킷 `직원견본/` 폴더에 **강사가 반복하는 일 5가지**로 만든 직원 5명이 들어 있습니다
> (팀장·교재대리·블로그대리·일정매니저·프로필대리 — 오후 P11 의 이름과 같습니다).
> 네 명을 새로 쓰는 대신 **데려와서 내 것으로 고치는** 게 빠릅니다. 강사가 아니면 아래 **P3-직접** 으로.
> 교안수석·정산과장·디자이너는 `직원견본/추가/` 에 있습니다 — **오늘은 5명으로 완벽하게 돌리고**, 팀이 안정된 뒤 모듈 6(P14)에서 한 명씩 데려옵니다.

```
직원견본/ 폴더에 프리랜서 강사용 직원 견본 5명(staff1.toml ~ staff5.toml)이 있어.

1. 직원견본/staff2.toml ~ staff5.toml 네 개를 .codex/agents/ 로 그대로 복사해줘. 내용은 고치지 말 것.
2. 내가 P2 에서 만든 .codex/agents/staff1.toml 는 지우지 말고, 직원견본/staff1.toml(팀장)의
   "내가 하는 일 · 시작할 때 묻는 것 · 일하는 방식 · 지어내지 않는 것"을 합쳐줘.
   내가 적은 담당과 성격 문장은 그대로 살릴 것. `name`·`description`·`developer_instructions`의 TOML 구조는 유지할 것.
3. workspace/memory/facts.md 의 "내가 반복해서 하는 일 5가지"와 견본 5명의 담당을 나란히 표로 보여주고,
   안 맞는 직원이 있으면 알려만 줄 것 (내가 바꾸라고 하기 전에는 바꾸지 말 것).
4. 직원견본/ 폴더(추가/ 포함)는 그대로 둘 것.

규칙:
- 파일 경로는 .codex/agents/staff1.toml ~ staff5.toml 다섯 개
- 다섯 명의 name 이 서로 겹치지 않게 할 것 (staff1 ~ staff5 그대로)
- 다른 파일은 만들지도 지우지도 말 것
```

> 🎨 담당을 바꾸고 싶으면 이어서 말로: `staff3 을 블로그 대신 유튜브 기획 담당으로 바꿔줘. 파일 형식은 그대로.`
> 견본에는 모듈 3 의 팀 규칙(저장 위치 · inbox 쪽지 · facts.md 먼저 읽기)이 **이미 들어 있습니다.**
> P6·P7 을 붙여넣었을 때 "이미 있음"이라고 나오면 정상입니다.
> 교재대리는 **강의 설계를 새로 짜지 않습니다.** 대표가 이미 가진 교안·PPT·PDF를 `C:\Agent\MyData\` 에서 받아 교재로 만듭니다 (HWP 는 PDF 로 저장해 넣기).

**합격 판정** → `py 점검.py 1` … ✅ 5개 전부

### P3-직접 — 강사가 아니면: 내 일 5가지로 직접 만들기

```
아까 종이에 적은 나머지 4가지 일로 직원을 4명 더 만들어줘.

  2. [교재 만들기]
  3. [블로그 쓰기]
  4. [일정 관리]
  5. [강사 프로필 관리]

규칙:
- 파일 경로는 .codex/agents/staff2.toml ~ staff5.toml
- staff1.toml 와 똑같은 형식을 유지할 것
- 다섯 명의 name 이 서로 겹치지 않게 할 것
- 각 직원에게 성격이 드러나는 혼잣말을 2개씩 넣을 것
- 담당이 서로 겹치지 않게 할 것
```

> 형식이 막막하면 `직원견본/staff1.toml` 를 열어 보세요. 한 명 분량이 어느 정도인지 보입니다.

**합격 판정** → `py 점검.py 1` … ✅ 5개 전부

---

## 모듈 2 · 일하는 방법 가르치기

> 📌 **순서가 바뀌었습니다 (2026-09-15) — 블로그를 먼저 합니다.**
> 제안서는 착수 전에 6가지(고객사·대상·시간·예산·일자·목적)를 묻습니다. 연습 단계에서는
> 그 답을 지어내야 해서, 첫 성공 경험이 **지어낸 답** 위에 세워집니다.
> 블로그는 물을 게 적고 결과가 **바로 눈에 보입니다.** 오늘 첫 성공은 여기서 만들고,
> 손에 감이 잡힌 다음 제안서로 갑니다.

### P4-0 — 블로그 스킬 만들기 (오늘의 첫 성공)

```
.agents/skills/ 폴더에 '블로그' 스킬을 만들어줘.
아래 내용을 SKILL.md 에 그대로 담아줘.

[이 스킬이 하는 일]
강의 후기·현장 스케치를 블로그 글로 쓴다.

[독자가 누구인지 — 이게 이 스킬의 핵심]
이 글의 독자는 일반인이 아니라 **교육 담당자**다.
"재밌었다"를 읽으러 오는 게 아니라 **우리 회사도 이 교육을 할지 판단하러** 온다.
그래서 감상문이 아니라 판단 자료로 쓴다.

[Step 0 — 착수 전 확인할 것]
한 번에 모아서 묻는다. 아는 것만 알려줘도 된다고 안내한다.
  1. 고객사 (공개 가능한지도 함께)
  2. 업종
  3. 과정명
  4. 대상 (직급·직무·인원)
  5. 교육 시간
  6. ⭐ 이 교육만의 특별한 점 ← 가장 중요
  7. ⭐ 다룬 내용
  8. 교육 후 변화·반응
  9. 어떤 조직에 추천하고 싶은지

  → 6·7번이 없으면 글이 아니라 광고가 된다. 이 둘은 반드시 받는다.

[글 구조]
1. 인사 + 어떤 교육이었는지 두세 줄
2. 판단 카드 (표로) — 고객사 / 업종 / 대상 / 인원 / 시간 / 형태 / 난이도 / 진행일
3. 이 교육의 차별점 (박스로 강조)
4. 실제로 다룬 내용 — 모듈별로
5. 교육 후 변화 (구체적인 반응·수치가 있으면 그것으로)
6. 이런 조직에 추천합니다
7. 문의 안내

[작성 원칙]
- 문장은 짧게. 한 문단 3~4줄.
- 숫자가 있으면 숫자로 쓴다. "많은 분이" ✕ / "32명이" ○
- 사진 자리는 [사진: 무엇] 으로만 표시하고 이미지는 만들지 않는다.
- 마지막에 SEO 제목 후보 5개를 뽑고, 각각 왜 그 제목인지 한 줄씩 붙인다.
- 해시태그 15개 이내.

[절대 금지]
- 고객사 실명은 공개 가능 확인을 받기 전에는 쓰지 않는다. 확인 전이면 '○○기업'.
- 참가자 개인의 말을 실명과 함께 쓰지 않는다.
- 안 들은 만족도 점수·재의뢰율 같은 수치를 지어내지 않는다.
- 발행하지 않는다. 초안까지만 만들고 사람이 검토한다.

[저장]
workspace/결과물/<날짜>_<고객사>_블로그초안.md

[기존 스킬 처리 — 이 줄을 빼면 스킬이 두 개가 된다]
- 같은 일을 하는 스킬이 .agents/skills/ 에 이미 있으면 (이름이 비슷한 다른 스킬 포함)
  새로 만들지 말고 그 폴더의 SKILL.md 를 이 내용으로 통째로 덮어쓴다.
- 폴더 이름이 다르면 이 이름으로 폴더를 바꾸고, 옛 폴더는 지운다.
- 덮어쓴 뒤 .codex/agents/ 의 직원 파일에서 이 스킬을 가리키는 줄을 찾아
  새 이름과 맞는지 확인하고, 안 맞으면 고친다.
- 무엇을 지우고 무엇을 고쳤는지 마지막에 목록으로 알려준다.

[트리거]
"블로그", "후기 써줘", "현장 스케치", "포스팅"
```

**이러면 정상** — 글 + SEO 제목 5개 + 해시태그가 한 파일에 나옵니다.

> 💡 **여기서 끝이 아닙니다.** 글이 나왔으면 **바로 다음 카드 P4-1** 로 가서 이 원고를
> **내 블로그 임시저장함에 실제로 넣습니다.** 글을 뽑는 것과, 그 글이 내 블로그에
> 들어가 있는 것은 체감이 다릅니다. **P4-0 + P4-1 이 한 세트**입니다.
>
> 그 다음이 **P4(제안서)** 입니다 — 같은 여섯 칸을 훨씬 깊게 채운 판입니다.

---

### P4-1 — 네이버 블로그 임시저장 (오늘의 와우 포인트)

방금 만든 원고가 **내 블로그 임시저장함에 실제로 들어가는** 단계입니다. 글을 쓰는 것까지는
"그럴듯하네" 지만, 내 블로그에 들어가 있는 걸 눈으로 보면 이야기가 달라집니다. 여기까지가 한 세트입니다.

> 🙋 **네이버 블로그가 없으면 건너뛰세요.** 다음 카드(P4 제안서)로 바로 가시면 됩니다.
> 나중에 만드시고 돌아와도 똑같이 됩니다.

**1단계 · 네이버 계정 최초 로그인** — 비밀번호는 **사람이 직접** 입력합니다. 창이 뜨면 로그인만 하세요.

```
cd "C:\Agent\01_KIT\starter-kit\naver-blog"
py -3 naver_draft.py login --blog-id 내블로그아이디
```

> `내블로그아이디` 는 `blog.naver.com/이아이디` 의 그 부분입니다.
> 🔒 세션은 `.naver-state.json` 에 저장됩니다. 로그인 토큰이니 절대 남에게 보내지 마세요.

**2단계 · 임시저장 실행** — 아래를 붙여넣으세요.

```
스타터킷/naver-blog/naver_draft.py 로 방금 만든 블로그 원고를 네이버 블로그
임시저장함에 넣어줘.

- blog-id: 내블로그아이디
- 제목: [○○기업 리더십 교육, 이렇게 진행했습니다]
- 원고 파일: 방금 만든 그 .md 파일
- 이미지: 넣지 않는다 (글만 저장한다)

실행 후 종료 코드와 화면 로그를 그대로 보여줘.
임시저장 목록에서 제목이 실제로 보이는지 확인하기 전에는 "저장했다"고 말하지 마.
"발행" 버튼은 어떤 경우에도 누르지 마.
```

> 🔒 **안전 규칙** — ① "발행" 버튼을 누르지 않는다(임시저장까지만) ② 비밀번호를
> 입력하지 않는다(로그인은 항상 사람이) ③ 복구 팝업은 항상 "취소"(기존 임시저장 보존).

**통과 기준** — 네이버 블로그 **임시저장 목록을 직접 열어** 방금 지은 제목이 보이면 통과입니다.
화면 로그만 보고 넘어가지 마세요. **눈으로 확인하는 것까지가 이 카드입니다.**

> 💡 **여기가 오늘의 분기점입니다.** 글을 뽑는 것과, 그 글이 내 블로그에 들어가 있는 것은
> 체감이 다릅니다. 이 감각을 가지고 다음 카드(제안서)로 가세요.

---

### P4 — 제안서 스킬 만들기

```
지금 열려 있는 이 스타터킷 폴더 안의 .agents/skills/제안서/ 에 SKILL.md 를 만들어줘.
(홈 폴더가 아니라 스타터킷 안의 `.codex/agents/`다.)
먼저 workspace/memory/facts.md 를 읽고, 거기 적힌 내 회사 이름·로고 글자·색 4개를
이 스킬 안에 그대로 박아 넣어줘.

[이 스킬이 하는 일]
교육 제안서를 만든다. 고객사에 그대로 보낼 수 있는 수준까지.
결과물을 두 개 만든다 — 내용 초안(.md)과 슬라이드 덱(.html).

[Step 0 — 착수 전 반드시 확인할 것]
아래 6가지를 한 번에 모아서 묻고, 답이 오면 되묻지 말고 끝까지 뽑는다.
  1. 고객사 이름과 업종
  2. 교육 대상 (직급·직무·인원)
  3. 교육 시간
  4. 예산 또는 시간당 단가
  5. 일자 확정 여부
  6. 이 교육을 왜 하려는지 (담당자가 풀려는 문제)

  → 1·2·3·6 중 하나라도 비면 제안서를 쓰지 말고 그것부터 묻는다.
     특히 6번이 없으면 어느 회사에나 붙는 표준 커리큘럼밖에 못 나온다.

[작성 원칙]
- 모듈은 4개 안팎. 각 모듈에 네 줄 —
  모듈명 / 학습 목표 / 주요 내용 / 학습자 활동(+몇 분)
  "제목 + 한 줄"로 끝내지 않는다.
- 시간 합계가 요청한 총 시간과 맞는지 검산하고, 맞는다고 문서에 적는다.
- 기대효과는 형용사가 아니라 담당자가 윗선에 보고할 수 있는 문장으로.
  ✕ "역량이 향상됩니다"
  ○ "교육 후 2주 내 각 팀이 적용 사례 1건씩을 제출합니다"
- 견적은 단가 × 시간 계산 과정을 표에 보이고, 부가세 별도 여부를 반드시 쓴다.

[절대 금지]
- 안 들은 금액·날짜·인원·실적을 지어내지 않는다. 모르면 '확인 필요'로 둔다.
- 실제로 하지 않은 교육을 실적으로 쓰지 않는다.

[문장 규칙 — 여기 적히는 모든 글에 적용]
- 제목은 [핵심 수단] + [정량 수치] 로 쓴다. 추상적 제목을 쓰지 않는다.
  ✕ "업무 효율성 향상 방안"   ○ "처리 시간을 40% 줄이는 보고 절차 재설계"
- 한 문장에 한 메시지만 담는다. 두 문장을 넘기지 않는다.
  "~하며, ~하고, ~함으로써" 로 길게 잇지 않고 끊어 쓴다.
- 서술형 문장은 마침표를 찍는다.
  개조식(- 로 시작하는 항목)은 끝에 마침표를 절대 찍지 않는다.
- 개조식은 명사만 나열하지 않는다. [주체] + [구체적 행동] + [결과] 를 한 줄에 담는다.
  ○ "- 담당자 사전 협의 1회로 업종 사례를 맞춰 당일 이해도 편차 축소"

[쓰지 않을 단어]
  대폭 · 획기적인 · 혁신적인 · 엄청난 · 극대화 · 다양한 · 효율적인 · 최적의
  → 근거 없는 형용사·부사를 빼고 사실과 숫자로 바꿔 쓴다.

[받은 자료를 줄일 때]
- 사용자가 준 자료의 수치·고유명사는 하나도 빼지 않는다.
  줄이는 것은 문장 길이지 데이터가 아니다.

[표를 만들 때]
- 세로 격자선을 넣지 않는다. 가로 구분선만 쓴다.
- 굵게와 강조색은 실제 수치·변화량에만 쓴다. 그게 표의 주인공이다.

[결과물 ① 내용 초안]  ← 경로는 스타터킷 루트 기준. 스킬 폴더 안에 workspace 를 만들지 말 것
workspace/결과물/<날짜>_<고객사>_제안서초안.md

[결과물 ② 슬라이드 덱]  ← 이게 고객사에 나가는 것
workspace/결과물/<날짜>_<고객사>_제안서덱.html

  판형: 슬라이드 1장 = 가로 1440px × 세로 810px 고정 (16:9)
        인쇄하면 그대로 PDF 가 되게 @page{size:1440px 810px;margin:0} 를 넣는다
        한 장에 내용이 넘치면 글씨를 줄이지 말고 장을 나눈다

  [디자인 뼈대 — 아래 CSS 를 그대로 쓴다. 네 색만 facts.md 값으로 바꾼다]
  이 뼈대는 겉모양(색·여백·글꼴)만 정한다. 스스로 새 CSS 구조를 짜지 말고 아래를 붙여넣는다.
  장 수·순서·항목 같은 내용 구성은 여기서 정하지 않는다. 나중에 카드 P18 로
  내 실제 자료(C:/Agent/MyData)를 읽히면 그때는 내 자료의 구성이 이 예시를 이긴다.

    :root{
      --main:#메인색; --deep:#진한색; --accent:#강조색; --pale:#옅은색;
      --ink:#1C2733; --mid:#4A5866; --line:#DDE6EE; --w:#fff;
      --f:"Noto Sans KR","Noto Sans Korean","Noto Sans CJK KR","NotoSansKR","Malgun Gothic","맑은 고딕",sans-serif;
    }
    *{box-sizing:border-box;margin:0;padding:0}
    body{font-family:var(--f);color:var(--ink);background:#59626E}
    .slide{width:1440px;height:810px;background:var(--w);position:relative;overflow:hidden;
           display:flex;flex-direction:column;padding:60px 88px 76px;margin:0 auto 26px}
    /* 헤더 — 모든 본문 장에 넣는다. 메인 컬러가 여기 들어간다 */
    .hd{display:flex;align-items:center;gap:14px;flex:0 0 auto;
        padding-bottom:16px;border-bottom:3px solid var(--main);margin-bottom:26px}
    .hd .no{background:var(--main);color:#fff;font-size:15px;font-weight:700;
            padding:7px 14px;border-radius:4px;letter-spacing:1px}
    .hd .en{color:var(--main);font-size:14px;font-weight:700;letter-spacing:2px}
    .hd .logo{margin-left:auto;color:var(--deep);font-size:15px;font-weight:700}
    h2{font-size:38px;line-height:1.25;color:var(--deep);margin-bottom:22px;flex:0 0 auto}
    .body{flex:1;display:flex;flex-direction:column;gap:20px}   /* ← 남는 세로를 채운다 */
    .grow{flex:1}                        /* 표나 카드 묶음 하나에 붙여 아래까지 채운다 */
    .cards{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}
    .card{background:var(--pale);border-top:4px solid var(--main);border-radius:10px;padding:22px 24px}
    .card b{display:block;color:var(--deep);font-size:19px;margin-bottom:8px}
    table{width:100%;border-collapse:collapse;font-size:17px}
    th{background:var(--deep);color:#fff;text-align:left;padding:13px 16px;font-weight:700}
    td{padding:13px 16px;border-bottom:1px solid var(--line)}
    .pg{position:absolute;right:88px;bottom:28px;color:var(--mid);font-size:13px}
    /* 표지·마지막 장 — 배경 전면 컬러 + 흰 글씨 */
    .cover,.end{background:linear-gradient(135deg,var(--deep) 0%,var(--main) 68%,var(--accent) 100%);
                color:#fff;justify-content:center;border:0}
    .cover h1{font-size:64px;line-height:1.2;margin:18px 0 22px}
    .cover .sub{font-size:26px;color:#fff;opacity:.92}
    .cover .meta{margin-top:auto;font-size:18px;opacity:.85;border-top:1px solid rgba(255,255,255,.35);padding-top:20px}
    .end{align-items:center;text-align:center}

  [면적 채우기 — 빈 장을 만들지 않는다]
  면적은 글자수가 아니라 아래 구조로 채운다. 이 세 줄이 지켜지면 장은 바닥까지 찬다.
  - 본문 장마다 블록 **3개 이상**(문단·표·카드묶음·목록·강조박스 중).
  - 그중 표나 카드묶음 **하나에 반드시 class="grow"** 를 붙인다. 이게 남는 세로를 먹는다.
  - 표는 4행 이상, 카드묶음은 3개, 목록은 4항목 이상.
  - ❌ margin-bottom:200px 같은 큰 여백으로 빈 곳을 때우지 않는다.
  - ❌ 제목과 한 줄만 있는 장을 만들지 않는다.

  분량은 **본문 장당 250자 이상**을 목표로 한다(제목 제외).
  - 250자를 못 채우겠으면 **글을 지어내지 말고 그 장을 없애서 앞 장에 합친다.**
    안 들은 사실을 지어내는 것보다 장을 줄이는 쪽이 항상 낫다.
  - 장 수가 적어도 된다. **적은 장을 꽉 채우는 쪽이 낫다.**
  - 정보가 모자라 '확인 필요'가 많은 장은 250자에 못 미쳐도 된다.
    대신 그 장에 **무엇을 받으면 채울 수 있는지**를 한 줄 적는다.

  [반드시 지킬 세 가지 — 다 만든 뒤 스스로 확인한다]
  1. 모든 본문 장에 .hd 헤더가 있고, 그 아래 선과 번호 배지가 메인 컬러다
  2. 첫 장(.cover)과 마지막 장(.end)의 배경이 컬러다. 흰 배경으로 두지 않는다
  3. 본문 장마다 블록 3개 이상 + class="grow" 가 하나씩 들어 있다

  장 구성 (이 순서):
    1  표지        진한 색 그라데이션 + 로고 글자 + 영문 라벨 + 큰 제목 +
                   강조색 부제 + 하단에 "고객사 | 대상 | 시간" 한 줄
    2  목차        01~06 번호와 한 줄 설명
    3  필요성      고객이 말한 문제를 그대로 인용해서 시작
    4  기대효과    시점별 표 (당일 / 2주 / 1개월) + 무엇으로 확인하는지
    5  설계 방향   4단계 카드 + 학습자 활동 비중
    6  커리큘럼    시간 / 구분 / 세부내용 / 방법 / 소요 표 + 합계 행 + 하단 카드 3개
    7~8 주요 내용  핵심 모듈 2개를 한 장씩
    9  견적서      요약 카드 4개 + 상세 표(산출 내역 칸 필수) + 공급가·부가세·합계 + 주석
    10 강사 소개   경력 / 학력 / 자격 3칸 + 강의 영역 표
    11 요약·다음단계  요약 카드 4개 + 진행 순서 표 + "확인 부탁드리는 것" 박스
    12 THANK YOU  표지와 같은 배경

  모든 본문 장에 넣을 것:
    - 좌상단 번호 배지 + 영문 섹션명 (예: 01 / WHY THIS TRAINING)
    - 제목은 명사가 아니라 문장으로.  ✕"기대효과"  ○"교육 후에 이런 것이 눈에 보입니다"
    - 우상단 로고 글자 / 우하단 쪽번호
    - 표 머리는 진한 색 배경에 흰 글씨

  색은 facts.md 의 네 값을 CSS 맨 위 :root 에 모아두고 그 변수만 쓴다.
  나중에 색을 바꿀 때 그 네 줄만 고치면 되게 한다.

  서체는 "Noto Sans KR" 계열로 하고, 인터넷에서 폰트·이미지를 불러오지 않는다.
  자바스크립트는 쓰지 않는다.

[규칙]
- 파일 맨 위 --- 안에 name, description 두 항목을 넣을 것
- description 에는 "제안서", "제안서 써줘", "교육 제안", "프로포절" 트리거를 적을 것
- 저장 후 두 파일 경로와 '확인 필요' 항목만 모아서 알려줄 것
- 마지막에 "덱은 브라우저로 열고 Ctrl+P → 대상을 PDF로 저장하면 됩니다" 를 알려줄 것

[마지막 보고 — 표로]
장별로 글자수와 grow 사용 여부를 표로 보여줄 것. 250자에 못 미치는 장이 있으면
말만 하지 말고 앞 장에 합치거나 보충한 뒤 다시 세어서 보고할 것.

  장 | 제목 | 글자수 | 헤더 색 | 판정
```

> ⏱ **이 카드는 좀 깁니다.** 그래도 여러분이 할 일은 똑같습니다 — **통째로 복사, 붙여넣기, 엔터.**
> 읽지 않으셔도 됩니다. 대신 **결과물이 처음부터 고객사에 보낼 수 있는 모양**으로 나옵니다.
>
> 만드는 데 **1~2분** 걸립니다. 화면이 멈춘 게 아닙니다.

### P5 — 직원과 스킬 연결

```
지금까지 만든 스킬들을 담당 직원과 연결해줘.

[담당은 네가 정해라 — 나한테 묻지 마]
.codex/agents/ 의 직원 파일(staff1.toml ~ staff5.toml, 더 있으면 그것도)을 전부 읽고,
각 파일에 적힌 담당 업무와 각 스킬이 하는 일을 대조해서 담당을 스스로 정한다.
"몇 번 직원으로 할까요" 라고 묻지 마라 — 역할은 이미 직원 파일에 적혀 있다.

- 고른 직원 파일 본문에 "이 일을 할 때는 [스킬이름] 스킬을 열고 시작한다" 를 넣을 것
- 그 파일의 `name`·`description`·`developer_instructions` 구조는 건드리지 말 것
- 담당이 아닌 직원 파일은 건드리지 말 것
- 끝나면 표로 알려줄 것 —  스킬 | 붙인 직원 | 그렇게 정한 근거(직원 파일의 그 문장)
- 어느 직원에게도 정말 안 맞는 스킬이 있을 때만, 그것만 골라서 물어볼 것
```

> 📌 **"몇 번 직원으로 할까요" 를 다시 물으면** 직원 파일에 담당이 안 적힌 것입니다.
> `P3` 으로 5명을 제대로 데려왔는지 보시고, 그래도 물으면 이렇게 답하세요 —
> `직원 파일 읽고 네가 정해. 묻지 마.`

**실제로 시켜보기** (이게 핵심):

```
staff1 불러서 [○○공단 관리자 리더십 4시간, 시간당 50만원] 제안서 만들어줘
```

직원이 먼저 **6가지를 물어봅니다.** 아는 것만 답하시면 됩니다.
모르는 건 "모르겠다"고 하시면 `확인 필요`로 남겨둡니다. **지어내지 않습니다.**

→ `workspace/결과물/` 폴더를 열어보세요. **파일이 두 개** 생겼습니다.

| 파일 | 무엇 |
|---|---|
| `..._제안서초안.md` | 내용 초안 — 내가 보고 고치는 것 |
| `..._제안서덱.html` | **슬라이드 12장** — 고객사에 나가는 것 |

`.html` 을 더블클릭하면 브라우저에서 열립니다.
**표지에 내 회사 이름과 색이 들어가 있습니다.** P1에서 적은 그 값입니다.

**PDF로 만들려면** → 브라우저에서 `Ctrl + P` → 대상을 **"PDF로 저장"**

> ⏱ **첫 실행은 3~5분 걸립니다.** 처음 한 번은 시동 거는 시간이 있어서 그래요.
> 화면이 멈춘 게 아니니 **Ctrl+C 누르거나 다시 치지 마세요.** 두 번째부터는 빨라집니다.

> 🎨 **색을 바꾸고 싶으면** — `.html` 파일 맨 위 `:root` 안의 **네 줄**만 고치면 전체가 바뀝니다.
> 또는 그냥 이렇게 말씀하세요: `제안서덱 색을 [진한 초록] 계열로 바꿔줘`

**합격 판정** → `py 점검.py 2` … **✅ 8개**
> 스킬은 1개면 됩니다. 대신 **덱(.html)이 나왔는지**까지 봅니다 —
> `.md` 만 만들고 넘어가면 여기서 ❌ 가 뜹니다.

> 📚 **다른 일도 이렇게 만들려면** → `프롬프트카드_전문스킬.md`
> **교안 · 메일 · 프로필 · 카드뉴스** 전문 스킬 카드가 들어 있습니다.
> (블로그는 **P4-0**, 제안서는 방금 만든 **P4**, 교재는 모듈 3의 **P8** 가 이미 완성품이라 그 문서에서 뺐습니다.)
> 시간이 남으면 강의장에서, 아니면 집에서 하세요.

---

## 모듈 3 · 팀으로 묶기

### P6 — 팀 규칙 심기 (저장 규칙 + 넘기는 법)

> 📌 예전에는 카드 두 장(P6·P7)이었습니다. **둘 다 "5명 파일에 규칙을 넣는" 같은 일**이라
> 한 장으로 합쳤습니다. 붙여넣기 한 번이면 됩니다.

```
직원 5명 전부가 지킬 팀 규칙 두 가지를 한 번에 넣어줘.

[규칙 1 — 어디에 저장하나]
- 모든 결과물은 workspace/결과물/ 에 저장
- 파일 이름은 날짜_고객사_무엇.확장자 형식

[규칙 2 — 서로에게 어떻게 넘기나]
상황: [제안서 담당이 계약을 따내면 → 교재 담당이 워크북을 만들어야 한다]

- 넘길 때는 workspace/inbox/[받는사람이름]/ 폴더에
  [날짜]_[한줄요약].md 파일을 만든다
- 쪽지에는 이 네 가지를 반드시 적는다:
  누가 보냈는지 / 무슨 일인지 / 언제까지인지 / 참고할 파일 경로
- 일을 시작하기 전에 자기 inbox 를 먼저 확인한다

[규칙 3 — 새 스킬이 생기면 담당을 스스로 정한다]
- 새 스킬이 만들어지면 "몇 번 직원이 쓸까요" 를 대표에게 묻지 않는다.
- 직원 파일에 적힌 담당 업무와 그 스킬이 하는 일을 대조해 담당을 스스로 정한다.
- 정한 뒤 그 직원 파일에 "이 일을 할 때는 [스킬이름] 스킬을 열고 시작한다" 를 넣고,
  누구에게 왜 붙였는지 한 줄로 알린다.
- 어느 직원에게도 정말 맞지 않을 때만 그때 묻는다.

[공통]
- 위 세 규칙을 5개 직원 파일 전부의 본문에 똑같이 넣을 것
- 각 파일의 `name`·`description`·`developer_instructions` 구조는 건드리지 말 것
- 다 넣은 뒤 어느 파일을 고쳤는지 목록으로 알려줄 것
```

> ⏱ 5명 파일을 전부 고치므로 **1분 안팎** 걸립니다. 화면이 멈춘 게 아닙니다.

> ⚠️ **직원 파일이 길어지면 답이 느려집니다.** 코덱스판은 직원이 `.codex/agents/` 파일을
> **매 요청마다 직접 읽습니다** — 잘리지는 않지만 길수록 읽는 시간이 붙습니다.
> 규칙을 계속 덧붙이지 말고 겹치는 줄은 지워 짧게 유지하세요.

### P7 — 팀 규약 채우기

```
workspace/memory/facts.md 를 채워줘.

우리 회사가 늘 지키는 것:
- 시간당 단가: [35만원, 부가세 별도]
- 최소 강의 시간: [2시간]
- 취소 정책: [3일 전까지 무료]
- 자주 가는 지역: [서울, 대전, 부산]

절대 지어내면 안 되는 것:
- 금액, 날짜, 고객사 이름, 숫자로 된 실적
- 모르면 "확인 필요"라고 쓴다

- 이 파일을 직원 5명이 일 시작할 때 먼저 읽도록 각 파일에 한 줄씩 넣을 것
```


> ⚠️ **팀 규약도 매번 직접 읽습니다.** `facts.md` 가 길수록 직원이 그 파일을 읽는 시간이
> 늘어납니다. 고객사·단가처럼 쌓이는 항목은 오래된 줄을 지우면서 관리하세요.

### P8 — 교재(워크북) 스킬 만들기

> 📌 **제안서 다음으로 많이 하는 일**이라 기본 카드에 넣었습니다.
> 제안서가 **팔기 위한 문서**라면, 교재는 **교육장에서 손에 쥐어주는 것**입니다.

```
.agents/skills/교재/ 폴더에 SKILL.md 를 만들어줘.
먼저 workspace/memory/facts.md 를 읽고 회사 이름·로고 글자·색 4개를 스킬 안에 박아 넣어줘.

[이 스킬이 하는 일]
학습자가 손에 쥐고 쓰는 워크북을 만든다. 진행 설계가 아니라 배포물이다.
결과물을 두 개 만든다 — 내용(.md)과 인쇄용 워크북(.html).

[Step 0 — 착수 전 확인할 것]
  1. 무엇을 근거로 만드나 — 확정된 교안 또는 제안서 (파일 경로)
  2. 어떤 형식 — 학습자용 워크북 / 강사용 교재 (기본: 학습자용 워크북)
  3. 인쇄 부수와 흑백/컬러
  4. 제출 마감일

  → 1번이 없으면 만들지 않는다. 근거 없이 만든 교재는 강의와 따로 논다.
     교안도 제안서도 없으면 "제안서부터 만드시겠어요?" 하고 되묻는다.
  → 4번은 받는 즉시 알려준다. 교재 마감은 강의일보다 먼저 온다.

[챕터마다 반드시 들어갈 여섯 블록]
  ◎ 학습 목표    — 이 챕터를 끝내면 뭘 할 수 있는가
  한 줄 정의     — 핵심 개념을 한 문장으로
  본문           — 설명. 예시는 학습자 업종에서 가져온다
  실습 / 워크시트 — 직접 써보는 칸
  ✓ CHECK       — 스스로 확인하는 질문 2~3개
  핵심 요약      — 3줄 이내

[작성 원칙]
- 워크북은 빈칸만 있으면 손이 안 간다. 맨 위 한 줄에 예시를 채워두고 나머지를 비운다.
- 실습지는 한 쪽에 하나만.
- 챕터는 교안(또는 제안서)의 모듈과 1:1로 맞춘다. 모듈이 4개면 챕터도 4개다.
- 흑백 인쇄를 전제로 색으로만 구분되는 표시를 쓰지 않는다.

[절대 금지]
- 근거 문서에 없는 내용을 교재에 넣지 않는다. 필요하면 근거부터 고친다.
- 출처 없는 통계·인용을 싣지 않는다.
- 강의 진행 방법(시간표·활동 운영)을 교재에 넣지 않는다. 그건 교안이다.

[문장 규칙 — 여기 적히는 모든 글에 적용]
- 제목은 [핵심 수단] + [정량 수치] 로 쓴다. 추상적 제목을 쓰지 않는다.
  ✕ "업무 효율성 향상 방안"   ○ "처리 시간을 40% 줄이는 보고 절차 재설계"
- 한 문장에 한 메시지만 담는다. 두 문장을 넘기지 않는다.
  "~하며, ~하고, ~함으로써" 로 길게 잇지 않고 끊어 쓴다.
- 서술형 문장은 마침표를 찍는다.
  개조식(- 로 시작하는 항목)은 끝에 마침표를 절대 찍지 않는다.
- 개조식은 명사만 나열하지 않는다. [주체] + [구체적 행동] + [결과] 를 한 줄에 담는다.
  ○ "- 담당자 사전 협의 1회로 업종 사례를 맞춰 당일 이해도 편차 축소"

[쓰지 않을 단어]
  대폭 · 획기적인 · 혁신적인 · 엄청난 · 극대화 · 다양한 · 효율적인 · 최적의
  → 근거 없는 형용사·부사를 빼고 사실과 숫자로 바꿔 쓴다.

[받은 자료를 줄일 때]
- 사용자가 준 자료의 수치·고유명사는 하나도 빼지 않는다.
  줄이는 것은 문장 길이지 데이터가 아니다.

[표를 만들 때]
- 세로 격자선을 넣지 않는다. 가로 구분선만 쓴다.
- 굵게와 강조색은 실제 수치·변화량에만 쓴다. 그게 표의 주인공이다.

[결과물 ① 내용]
workspace/결과물/<날짜>_<과정명>_교재.md

[결과물 ② 인쇄용 워크북]  ← 실제로 나눠주는 것
workspace/결과물/<날짜>_<과정명>_교재.html

  판형: 한 쪽 = A4 세로 794px x 1123px 고정
        인쇄하면 그대로 PDF 가 되게 @page{size:A4;margin:0} 를 넣는다
        한 쪽에 내용이 넘치면 글씨를 줄이지 말고 쪽을 나눈다
  쪽 구성: 표지(소속·이름·날짜 적는 칸) / 오늘 얻어갈 것 /
           챕터별 [개념 쪽 + 워크시트 쪽] / 한 장 요약
  서체는 "Noto Sans KR" 계열. 인터넷에서 폰트·이미지를 불러오지 않는다
  자바스크립트는 쓰지 않는다

  [디자인 뼈대 — 아래 CSS 를 그대로 쓴다. 네 색만 facts.md 값으로 바꾼다]
  이 뼈대는 겉모양(색·여백·글꼴)만 정한다. 스스로 새 CSS 구조를 짜지 말고 아래를 붙여넣는다.
  장 수·순서·항목 같은 내용 구성은 여기서 정하지 않는다. 나중에 카드 P18 로
  내 실제 자료(C:/Agent/MyData)를 읽히면 그때는 내 자료의 구성이 이 예시를 이긴다.

    :root{
      --main:#메인색; --deep:#진한색; --accent:#강조색; --pale:#옅은색;
      --ink:#1F2733; --mid:#556170; --line:#DDE3EA; --w:#fff;
      --f:"Noto Sans KR","Noto Sans Korean","Noto Sans CJK KR","NotoSansKR","Malgun Gothic","맑은 고딕",sans-serif;
    }
    *{box-sizing:border-box;margin:0;padding:0}
    body{font-family:var(--f);color:var(--ink);background:#6B7280}
    .page{width:794px;height:1123px;background:var(--w);position:relative;overflow:hidden;
          display:flex;flex-direction:column;padding:56px 56px 64px;margin:0 auto 20px}
    /* 머리 — 모든 쪽에 넣는다. 메인 컬러가 여기 들어간다 */
    .hd{display:flex;align-items:center;gap:10px;flex:0 0 auto;font-size:12px;font-weight:700;
        padding-bottom:12px;border-bottom:2.5px solid var(--main);margin-bottom:22px;color:var(--main)}
    .hd .ch{background:var(--main);color:#fff;padding:5px 11px;border-radius:4px;letter-spacing:1px}
    .hd .logo{margin-left:auto;color:var(--deep)}
    h2{font-size:27px;line-height:1.3;color:var(--deep);margin-bottom:18px;flex:0 0 auto}
    .body{flex:1;display:flex;flex-direction:column;gap:16px}   /* ← 남는 세로를 채운다 */
    .grow{flex:1}
    .goal{background:var(--pale);border-left:5px solid var(--main);border-radius:0 8px 8px 0;padding:16px 20px;font-size:15px}
    table{width:100%;border-collapse:collapse;font-size:14px}
    th{background:var(--deep);color:#fff;text-align:left;padding:10px 13px;font-weight:700}
    td{padding:10px 13px;border-bottom:1px solid var(--line)}
    /* 워크시트 — 적는 칸은 반드시 줄이 보이게 */
    .write{border:1.5px solid var(--line);border-top:3px solid var(--main);border-radius:8px;
           padding:16px 18px;min-height:110px}
    .lines{background:repeating-linear-gradient(var(--w) 0 30px,var(--line) 30px 31px);min-height:150px}
    .pg{position:absolute;right:56px;bottom:26px;color:var(--mid);font-size:11px}
    /* 표지·마지막 쪽 — 배경 전면 컬러 + 흰 글씨 */
    .cover,.end{background:linear-gradient(150deg,var(--deep) 0%,var(--main) 70%,var(--accent) 100%);
                color:#fff;justify-content:center}
    .cover h1{font-size:42px;line-height:1.3;margin:16px 0 20px}
    .cover .fill{margin-top:auto;border-top:1px solid rgba(255,255,255,.4);padding-top:18px;font-size:15px;line-height:2.4}

  [면적 채우기 — 빈 쪽을 만들지 않는다]
  면적은 글자수가 아니라 아래 구조로 채운다.
  - 쪽마다 블록 **3개 이상**(문단·표·적는 칸·강조박스 중).
  - 그중 표나 적는 칸 **하나에 반드시 class="grow"** 를 붙인다. 이게 남는 세로를 먹는다.
  - 워크시트 쪽은 적는 칸(.write / .lines)이 쪽 세로의 절반 이상을 차지하게 만든다.
    ❌ 질문 세 줄만 있고 나머지가 텅 빈 쪽을 만들지 않는다.
  - ❌ 큰 여백(margin 100px 이상)으로 빈 곳을 때우지 않는다.

  분량은 **개념 쪽 350자 이상**을 목표로 한다(제목 제외).
  - 못 채우겠으면 **글을 지어내지 말고 그 쪽을 없애서 앞 쪽에 합친다.**
  - 쪽 수가 적어도 된다. **적은 쪽을 꽉 채우는 쪽이 낫다.**

  [반드시 지킬 세 가지 — 다 만든 뒤 스스로 확인한다]
  1. 모든 쪽에 .hd 머리가 있고, 그 아래 선과 챕터 배지가 메인 컬러다
  2. 첫 쪽(.cover)과 마지막 쪽(.end)의 배경이 컬러다. 흰 배경으로 두지 않는다
  3. 쪽마다 블록 3개 이상 + class="grow" 가 하나씩 들어 있다

[규칙]
- 파일 맨 위 --- 안에 name, description 두 항목을 넣을 것
- description 에는 "교재", "워크북", "핸드아웃", "실습지" 트리거를 적을 것
- 같은 일을 하는 스킬이 이미 있으면 새로 만들지 말고 덮어쓸 것
- 저장 후 두 파일 경로를 알려주고, "Ctrl+P → PDF로 저장" 을 안내할 것

[마지막 보고 — 표로]
쪽별로 글자수와 grow 사용 여부를 표로 보여줄 것. 기준에 못 미치는 쪽이 있으면
말만 하지 말고 앞 쪽에 합치거나 보충한 뒤 다시 세어서 보고할 것.

  쪽 | 제목 | 종류(개념/워크시트) | 글자수 | 머리 색 | 판정
```

**합격 판정** → 챕터 수가 제안서 모듈 수와 같으면 정상입니다.

---

**릴레이 실습** — 이 두 줄을 차례로:

```
staff1 불러서 [고객사] 제안서 만들고, 계약 따냈다고 치고 교재 담당한테 넘겨줘
```

→ `workspace/inbox/` 에 쪽지가 생긴 걸 확인한 뒤

```
staff2 불러서 inbox 확인하고 이어서 교재(워크북) 만들어줘
```

**합격 판정** → `py 점검.py 3` … **✅ 7개**
> 쪽지뿐 아니라 **워크북(.html)이 나왔는지**까지 봅니다.

---

## 모듈 3.5 · 전문 스킬 · 내 자료 · 네이버 · 메일 연결

> 📌 **시간 추후 배정.** 오전에 만든 제안서(P4)·교재(P8)는 완성품이지만, 실제 업무는
> 그보다 많습니다. 이 구간은 ① 내 업종에 맞는 스킬을 더 만들고 ② 진짜 내 자료로
> 기준을 갈아 끼우고 ③ 블로그 원고를 네이버 임시저장까지 이어 붙입니다.

### P16 — 내 업종 스킬 설계 (인터뷰형)

완성된 스킬을 바로 주지 않습니다. **코덱스가 나를 여섯 칸으로 인터뷰**하게 만드는 카드입니다.

```
내가 자주 하는 업무 하나를 스킬로 만들려고 해. 업무 이름: [내가 매주 반복하는 문서 작업]

.agents/skills/ 에 이 스킬을 만들 건데, 먼저 완성하지 말고 아래 여섯 칸을
나한테 하나씩 순서대로 물어봐. 한 번에 다 묻지 말고, 내가 답할 때마다
다음 칸으로 넘어가.

  1. Step 0 정보 수집 — 이 일을 시작하기 전에 나는 뭘 알아야 하는가 (3~9개)
  2. 상황 분류 — 같은 일이라도 상황마다 톤·형식이 달라지는가? 갈래를 나눠줘
  3. 작성 원칙 — 이 업종에서 "이건 꼭 지켜야 한다"는 기준 3~5개
  4. 절대 금지 — 지어내면 안 되는 것, 하면 안 되는 것
  5. 출력 형식 — 결과물이 어떤 모양이어야 하는지, 파일은 몇 개 나와야 하는지
  6. 저장 위치 — workspace/결과물/ 아래 어떤 이름 규칙으로

다 물어본 뒤, 내 답을 바탕으로 .agents/skills/[블로그]/SKILL.md 를 만들어줘.

[규칙]
- 파일 맨 위 --- 안에 name, description 을 넣고, description 에 이 스킬을
  부르는 말(트리거) 2~4개를 적을 것
- 같은 일을 하는 스킬이 이미 있으면 새로 만들지 말고 덮어쓸 것
- 다 만든 뒤 여섯 칸이 전부 채워졌는지 스스로 표로 확인해서 보여줄 것
- 담당 직원은 네가 정해라. .codex/agents/ 의 직원 파일을 전부 읽고, 거기 적힌
  담당 업무와 이 스킬이 하는 일을 대조해 가장 맞는 직원을 고른다.
  "몇 번 직원으로 할까요" 라고 묻지 마라 — 역할은 이미 직원 파일에 적혀 있다.
  고른 직원 파일 본문에 "이 일을 할 때는 이 스킬을 열고 시작한다" 를 한 줄 넣고,
  누구에게 왜 붙였는지 한 줄로 알려줄 것 (근거는 그 직원 파일의 문장에서 인용).
  어느 직원에게도 정말 안 맞을 때만, 그때 물어볼 것
```

**통과 기준** — SKILL.md에 여섯 칸이 전부 채워져 있고, Step 0 항목이 **내 업종에만 해당하는
구체적인 질문**이면 통과. "친절하게 작성"처럼 어디에나 붙는 말뿐이면 다시 시키세요.

> 📚 완성 예시(블로그·교안·메일 세 장)는 `프롬프트카드_전문스킬.md` 에 있습니다.

### P17 — 내 자료 폴더(MyData) 채우기·열어주기

설치 때 `C:\Agent\MyData\` 폴더가 **이미 만들어져 있습니다** (안의 `여기에_넣으세요.txt` 참고).
각 폴더에 **내가 실제로 썼던 파일을 3개씩** 넣습니다 — 1개면 우연이지만 3개가 겹치면 그게 내 스타일입니다.
전날 넣어 뒀다면 바로 아래 카드로 갑니다.

| 폴더 | 개수 |
|---|---|
| `MyData\Proposal` (제안서) | 3개 |
| `MyData\Blog` (블로그) | 3개 |
| `MyData\Logo` (로고) | 1개 |
| `MyData\Profile` (프로필) | 3개 (선택) |

```
아래 폴더들을 앞으로 참고할 자료 창고로 삼아줘.

- 제안서: C:/Agent/MyData/Proposal
- 블로그: C:/Agent/MyData/Blog
- 로고: C:/Agent/MyData/Logo
- 프로필: C:/Agent/MyData/Profile

각 폴더에 파일이 몇 개 있는지, 어떤 형식(pptx/pdf/docx/hwp)인지 표로 보여줘.
그리고 이 경로를 workspace/memory/facts.md 에 "자료 창고" 항목으로 적어줘.
원본은 절대 고치거나 지우지 말고 읽기만 한다는 규칙도 같이 적어줘.
```

**통과 기준** — 파일 개수가 폴더별로 나오면 통과. 경로는 탐색기 주소창에서 **복사한 걸
통째로** 주세요 — 두루뭉술하게 부르면 후보만 찾고 멈춥니다.

### P17-1 — PPT·PDF 내용 추출 도구 (필요할 때만)

폴더는 열려도 pptx·pdf·hwp는 **파일명만** 보입니다. "못 읽는다"는 답이 나왔을 때만 쓰세요.

```
PPTX·PDF·DOCX 파일에서 글자만 뽑아내는 파이썬 도구를 만들어줘.

- 파일 하나든 폴더 하나든 받을 수 있게 해줘. 폴더면 안쪽까지 전부 훑어줘
- PPT는 슬라이드 번호와 표 안의 글자까지 뽑아줘
- 한글(.hwp)은 못 읽으면 "못 읽음"이라고만 표시하고 넘어가줘
- 필요한 라이브러리가 없으면 먼저 설치해줘

만든 다음, 방금 연 세 폴더로 실제로 한 번 돌려서 결과를 보여줘.
```

> ⚠️ 도구는 **실행되는 파일**로 만들어야 합니다. 스킬 문서에 코드를 글로만 적으면 아무도 안 씁니다.

### P17-2 — 이미 쓰던 내 스킬 가져오기 (P18보다 먼저)

Codex를 써 오셨다면 이미 손에 익은 스킬이 있을 수 있습니다. `C:/Agent/MyData/Skill/`에
그 `SKILL.md`를 넣어 두면, 그것이 그 일의 정본이 됩니다.

> Codex가 처음이시면 이 카드는 건너뛰세요. 넣을 파일이 없으면 할 일도 없습니다.

```
C:/Agent/MyData/Skill/ 폴더의 .md 파일을 전부 읽어줘. 내가 원래 쓰던 스킬들이야.

먼저 설치하지 말고, 파일마다 아래 네 줄로 정리해서 보여줘.
1. 무슨 일을 하는 스킬인지 한 줄
2. 어떤 말에 발동하는지 (트리거)
3. 지금 내 스킬 중에 같은 일을 하는 게 있는지 — 있으면 그 이름
4. 설치하면 뭐가 바뀌는지 한 줄

내가 좋다고 하면 그때 설치해줘. 설치할 때는 이렇게 해줘.
- .agents/skills/<이름>/SKILL.md로 넣어줘
- 같은 일을 하는 스킬이 이미 있으면 내가 넣은 것이 정본이야.
  기존 것은 지우지 말고 .agents/skills/_보관/으로 옮기고, 되돌리는 법을 한 줄로 알려줘
- 트리거 낱말이 다른 스킬과 겹치면 알려줘. 한 낱말은 한 스킬만 갖게 해줘
- 스킬 맨 위에 "출처: MyData/Skill/<파일이름>"을 적어줘
- 내용은 고치지 마. 회사 이름·색 같은 사실만 facts.md 값으로 맞춰줘
- 설치한 스킬과 담당 직원을 연결하고, facts.md에 어느 직원이 쓰는지 적어줘
```

**통과 기준** — 설치 전에 스킬의 역할 요약이 먼저 나와야 하며, 같은 일을 하는 스킬은 하나만 남아야 합니다.

> 🔒 `SKILL.md`는 자료가 아니라 명령문입니다. 남에게 받은 파일이라면 요약을 읽고 승인하세요.

**합격 판정** → `py 점검.py 35`의 `쓰던 내 스킬 ○개가 실제로 설치됐다`와
`같은 일을 하는 스킬이 둘 이상 살아 있지 않다`가 ✅여야 합니다.

### P18 — 스킬을 내 실물로 덮어쓰기

```
C:/Agent/MyData/ 아래 Proposal(제안서), Blog(블로그), Profile(프로필) 세 폴더를
전부 훑어줘. 그 다음 각 폴더에 맞는 스킬(블로그·제안서·프로필)을 고쳐줘.
프로필 스킬이 아직 없으면 P16 방식(여섯 칸)으로 새로 만들어줘.

각 스킬마다 아래를 정리해서 반영해줘.
1. 내가 실제로 쓰는 구성 (순서·항목이 몇 개로 나뉘는지)
2. 자주 등장하는 표현·제목 어투 — 지어내지 말고 실제 문장 3개를 근거로 들어줘
3. 분량·톤 (긴지 짧은지, 격식 있는지 편한지)

- 스킬 맨 위에 "참고 자료 1순위: <폴더 경로>" 라고 적고, 이 일을 할 때
  먼저 그 폴더를 훑고 시작하라고 규칙으로 넣어줘
- 스킬 안에 남아 있는 예시 문장·예시 구성·예시 실적은 전부 지워줘.
  내 자료에 근거가 없는 문장은 하나도 남기지 마. 빈칸으로 두는 게 낫다
- facts.md 를 다시 읽어서 회사 이름·로고 글자·색 4개도 지금 값으로 갱신해줘
  (P1-B 에서 색을 바꿨으면 스킬 안에 옛 색이 아직 남아 있다)
- 같은 일을 하는 스킬이 이미 있으면 새로 만들지 말고 덮어써줘.
  같은 스킬이 두 군데(스타터킷 .agents/skills 와 홈 폴더)에 있으면 알려줘 — 둘이 싸운다
- 단, P17-2로 직접 넣은 스킬(맨 위에 "출처: MyData/Skill/"이 적힌 것)은
  뼈대를 고치지 마. 그 스킬에는 "참고 자료 1순위" 경로만 덧붙이고 끝내줘
```

**통과 기준** — 두 가지를 같이 봅니다.
① 스킬 문서에 **내 실제 문장 3개**가 근거로 인용돼 있을 것.
② 스킬 문서에 **견본 문구가 하나도 안 남아 있을 것** (홍길동·길동컨설팅·4,000회 같은 것).
"전문적이고 신뢰감 있게" 같은 일반론만 있으면 자료를 안 읽은 것입니다 — 다시 시키세요.

**합격 판정** → `py 점검.py 35` … 내 자료와 기존 스킬 반영 여부 8가지를 봅니다.
견본 문구(홍길동·4,000회)가 남아 있으면 여기서 ❌ 로 잡힙니다.

### P18-1 — (선택) 다 됐는지 확인

시간이 남을 때만. 매번 돌리면 사용량만 듭니다.

```
새로 고친 블로그 스킬로, 방금 강의 하나를 예로 들어 짧게 후기 한 편만 써줘.
내 기존 글과 문체·구성이 비슷한지 스스로 비교해서 알려줘.
```

---

### 네이버 블로그 임시저장  →  **모듈 2의 카드 P4-1 로 옮겼습니다**

> 📌 **여기 없습니다.** 2026-09-15 부터 블로그는 **글쓰기(P4-0) → 임시저장(P4-1)** 이
> 모듈 2에서 한 세트로 끝납니다. 글만 뽑고 끝내면 체감이 약해서, **내 블로그에 실제로
> 들어가는 것까지**를 오늘의 첫 성공으로 묶었습니다.
>
> 다시 하시려면 `프롬프트카드.md` 의 **P4-1** 을 보세요. 네이버 로그인은 한 번 해두면
> `.naver-state.json` 에 남아 그대로 재사용됩니다.

### P19 — 메일 연결 확인 (문의 메일을 팀장이 읽게)

> 📧 네이버·다음·지메일 모두 **IMAP + 앱 비밀번호** 한 가지 방식입니다. 전날 환경점검에서 앱 비밀번호를 넣었으면
> `mail\.env` 가 이미 있습니다. 안 넣었으면 `환경점검.bat` 을 다시 실행하거나 `mail/README.md` 대로 만듭니다.
> 앱 비밀번호는 **사람이 브라우저에서 발급**합니다 (네이버: 메일 환경설정 → IMAP 켜기 → 2단계 인증 → 애플리케이션 비밀번호 /
> 다음: 메일 설정 → IMAP/SMTP 사용 → 카카오계정 2단계 인증 → 앱 비밀번호 / 지메일: 구글 계정 → 보안 → 앱 비밀번호).

```
mail/skill/SKILL.md 를 .agents/skills/mail/SKILL.md 로 복사하고, .codex/agents/staff1.toml 본문에
"문의 메일을 볼 때는 mail 스킬을 열고 시작한다" 한 줄을 `developer_instructions` 본문에 넣어줘. TOML의 세 필드 구조는 건드리지 말 것.

그다음 검은 창 명령 py -3.14 mail\mail_check.py 를 실행해서 결과 한 줄을 그대로 보여줘.
OK 가 나오면 py -3.14 mail\mail_read.py --count 5 로 최근 메일 5통의 날짜·보낸 사람·제목만 표로 보여줘.
본문은 보여주지 말고, 📌문의 표시가 붙은 게 있으면 그 건만 접수 노트 초안을 workspace/결과물/ 에 만들어줘.
메일은 보내지 말 것. 비밀번호를 화면에 찍지 말 것.
```

**통과 기준** — `OK: naver 아이디@naver.com IMAP ✅ SMTP ✅` 한 줄 + 메일 5통 표.
`FAIL` 이면 그 줄에 원인(IMAP 미사용 / 앱 비밀번호 틀림 / 2단계 인증 안 켬)이 적혀 있습니다. `mail/README.md` 의 처방대로.

> 🔒 발송은 **초안까지만**입니다. "보내" 라고 명시했을 때만 `--approved` 로 보내고, 외부 주소는 `mail\.env` 의
> `MAIL_ALLOW_EXTERNAL=1` 을 대표가 직접 켜기 전에는 막혀 있습니다.
> 💬 이 연결은 슬랙으로 부른 팀장도 그대로 씁니다. 붙인 뒤 슬랙 서버를 한 번 다시 켜세요.

### 문의 접수 자동화 — 웹앱 + 시트 + 자동 메일

이 구간은 **고객이 문의를 넣으면 저절로 시트에 쌓이고 메일이 나가는 접수 웹앱**을 만듭니다.
지금까지 만든 것과 기술이 다릅니다 — Codex CLI 스킬이 아니라 **Google Apps Script**로
만드는 별도 프로그램입니다. 그래서 카드도 붙여넣는 대상이 다릅니다 —
**Google Sheets → 확장 프로그램 → Apps Script** 창에 붙여넣습니다.

> 🔒 **카카오톡 알림은 여기 없습니다.** 인가코드 유효시간이 10분뿐이라 강의장에서
> 다 같이 하면 실패율이 제일 높습니다. 별도 부록(`부록_카카오톡연동.md`)으로 뺐습니다 —
> 집에서 혼자, 시간 여유 있을 때 하세요.

> ✅ **문의가 오면 팀장이 슬랙에 먼저 나와서 물어봅니다.** "문의 등록됐어요 — ○○기업,
> 리더십 4시간, 20명. 제안서 써드릴까요?" 이게 오늘 목표입니다. 그런데 이건 **감시
> 프로그램이 아닙니다** — 24시간 도는 별도 프로세스를 새로 켜는 게 아니라, **고객이
> 폼을 제출하는 그 실행 안에서 슬랙 메시지도 같이 보내는 것**입니다. 폼 제출 = 메일 발송
> = 슬랙 알림이 **한 번의 실행**이라, "감시 프로그램이 조용히 죽는다"는 실패 자체가
> 구조적으로 일어날 수 없습니다. (실제로 문의를 놓친 사고는 이것과 다른 방식 —
> 구글폼에 따로 매달아 둔 트리거가 아무도 모르게 끊어진 경우였습니다. 오늘 만드는 건
> 그 트리거가 아예 없는 구조입니다.)
>
> 사람이 슬랙에서 "네, 써주세요"라고 답하면 그때부터 팀장이 진짜로 제안서를 씁니다 —
> **오늘 모듈 4에서 만든 슬랙 연결을 그대로 재사용**하는 것뿐, 새 장치가 아닙니다.

> 🔒 **카카오톡 알림은 여기 없습니다.** 인가코드 유효시간이 10분뿐이라 강의장에서
> 다 같이 하면 실패율이 제일 높습니다. 별도 부록(`부록_카카오톡연동.md`)으로 뺐습니다 —
> 집에서 혼자, 시간 여유 있을 때 하세요.

### P20-b — 슬랙 알림 창구 만들기 (전날 만든 슬랙 앱 재사용)

**모듈 4(P9)에서 만든 슬랙 앱**에 기능 하나만 더 켭니다. 새 앱을 만드는 게 아닙니다.

1. [api.slack.com/apps](https://api.slack.com/apps) → 모듈 4에서 만든 내 앱 클릭
2. 왼쪽 메뉴 **Incoming Webhooks** → 상단 토글 **On**
3. 맨 아래 **Add New Webhook to Workspace** → 알림 받을 채널 선택(팀장과 대화하던 채널이면 됩니다) → **허용**
4. 생성된 **Webhook URL**(`https://hooks.slack.com/services/...` 형태)을 복사해 메모장에 붙여두세요.
   이 주소는 Apps Script가 슬랙으로 메시지를 보낼 때 씁니다. **비밀번호처럼 취급하세요** — 이 URL을 아는 사람은 누구나 그 채널에 메시지를 보낼 수 있습니다.

### P20-c — 저녁 8시 자동 블로그 (시키지 않아도 임시저장까지)

> 📌 **언제 하나**: P20-b 바로 다음. 10분이면 됩니다.
> **먼저 돼 있어야 하는 것 넷** — P4-0(블로그 스킬) · P4-1(네이버 임시저장) ·
> P15(구글 캘린더 앱 연결) · P20-b(슬랙 알림 창구).
> 하나라도 없으면 이 카드는 건너뛰고, 준비된 뒤 돌아오세요.

이 카드를 끝내면 강의를 다녀온 날 저녁 8시에 일정매니저가 오늘 캘린더를 확인합니다.
끝난 강의가 있으면 블로그대리가 초안을 쓰고 네이버 임시저장함에 넣은 뒤 슬랙으로 알립니다.

```text
저녁 8시  →  일정매니저: 오늘 끝난 강의가 있나?
              ├─ 없다  →  조용히 끝
              └─ 있다  →  블로그대리: 초안 작성 → 네이버 임시저장 → 슬랙 알림
```

> 🚫 **공개 발행은 하지 않습니다.** 임시저장까지만 하고, 발행 버튼은 사람이 누릅니다.

#### 1단계 · 알림 주소 넣기

`slack-server` 폴더의 **`.env`**를 메모장으로 열고 맨 아래에 한 줄 추가하세요.
주소는 P20-b에서 복사해 둔 값입니다.

```dotenv
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
```

#### 2단계 · 손으로 한 번 돌려보기

예약하기 전에 `저녁블로그/evening_blog.bat`를 더블클릭하세요.

- 오늘 강의가 있었으면 몇 분 뒤 슬랙 알림과 네이버 임시저장을 확인합니다.
- 강의가 없으면 조용히 끝납니다. `저녁블로그/evening_blog.log`에 `오늘 강의 없음`이 남으면 정상입니다.
- `캘린더 조회 실패`가 보이면 P15 앱 연결을 확인하고 Codex CLI를 완전히 껐다가 다시 켜세요.

> ⚠️ 예약만 걸고 끝내지 말고, 이 단계에서 한 번은 실제 실행 결과를 확인하세요.

#### 3단계 · 매일 저녁 8시로 예약하기

`저녁블로그/예약등록.bat`을 더블클릭하세요. 관리자 권한은 필요 없습니다.
`[OK] Done.`이 보이면 아래 명령으로 등록 여부를 확인합니다.

```bat
schtasks /Query /TN "EveningBlog"
```

지금 바로 시험하려면:

```bat
schtasks /Run /TN "EveningBlog"
```

끄고 싶을 때:

```bat
schtasks /Delete /TN "EveningBlog" /F
```

| 증상 | 확인 | 해결 |
|---|---|---|
| 저녁 8시에 실행되지 않음 | 그때 PC가 절전 상태였음 | `README.md`의 절전 해제 안내를 적용 |
| `캘린더 조회 실패` | P15 앱 연결이 끊김 | ChatGPT 앱 연결 확인 → Codex CLI 재시작 |
| 강의를 찾지 못함 | 일정 제목에 과정명이 없음 | 캘린더 제목에 강의명·교육명을 적기 |
| 임시저장은 됐는데 슬랙이 조용함 | 웹훅 주소가 비어 있음 | `.env`의 `SLACK_WEBHOOK_URL` 확인 |

시간을 바꾸려면 `예약등록.bat`을 메모장으로 열어 `/ST 20:00` 숫자를 고친 뒤 다시 실행하세요.

---

### P21 — 문의 접수 웹앱 만들기

Google 계정으로 **새 스프레드시트**를 하나 만들고 이름을 `교육문의`로 바꾸세요.
그다음 메뉴에서 **확장 프로그램 → Apps Script**를 열고, 기본으로 열려 있는
`Code.gs`의 내용을 전부 지운 뒤 아래 카드를 붙여넣으세요.

```
너는 비개발자를 도와주는 Google Apps Script 자동화 선생님이자, HP 스타일의 세련된
웹앱 UI를 설계하는 실무형 개발 튜터야.

나는 Google Apps Script로 "(본인 회사명으로 수정해주세요) 교육 문의 웹앱"을 만들고 싶어.
목표는 사용자가 웹페이지에서 교육 문의 내용을 입력하면 다음 기능이 모두 자동으로 실행되는
시스템을 만드는 것이야.
1. 구글 스프레드시트에 문의 내용 자동 저장
2. 고객에게 HTML 디자인 자동응답 메일 발송
3. 관리자 Gmail로 새 문의 알림 메일 발송
4. 접수 완료 후 화면에 "담당자가 1~2 영업일 이내 연락드리겠습니다" 문구 표시
5. 구글 시트에는 상담완료 체크박스와 성사여부 yes/no 드롭다운까지 자동 생성
6. 접수 즉시 슬랙 채널에 "새 문의가 등록됐다"는 메시지를 보내고, 내용을 요약해서
   제안서를 써도 될지 먼저 물어보게 한다 (사람이 답하기 전까지는 아무것도 쓰지 않는다)

──────────────────────
[웹앱 이름]
──────────────────────
(본인 회사명으로 수정해주세요) 교육 문의

──────────────────────
[폼 설명 문구]
──────────────────────
(본인 회사 소개 및 안내 문구로 수정해주세요)
예시: 조직의 상황과 교육 목적에 맞는 맞춤형 교육을 제안드립니다.
아래 내용을 입력해주시면 담당자가 확인 후 연락드리겠습니다.

──────────────────────
[입력 항목]
──────────────────────
1. 신청자 성함
2. 직장명 / 부서 / 직책
3. 연락처
4. 이메일
5. 교육 주제 및 목적
6. 교육대상 및 인원수
7. 교육희망 일자 및 예산
8. 교육희망 장소

──────────────────────
[구글 시트 헤더]
──────────────────────
A열: 접수일시   B열: 문의번호   C열: 신청자 성함   D열: 직장명/부서/직책
E열: 연락처     F열: 이메일     G열: 교육 주제 및 목적   H열: 교육대상 및 인원수
I열: 교육희망 일자 및 예산   J열: 교육희망 장소   K열: 상태
L열: 상담완료   M열: 성사여부

──────────────────────
[시트 저장 규칙]
──────────────────────
- 시트 이름은 "교육문의"
- 시트가 없으면 자동 생성
- 첫 번째 행에는 헤더 자동 입력. 헤더는 연한 블루 배경, 굵은 글씨, 가운데 정렬
- 폼 제출 시 새 행에 데이터 저장
- 문의번호는 "(회사 약자)-yyyyMMdd-HHmmss" 형식으로 자동 생성
- 상태는 기본값 "접수"
- L열 상담완료는 체크박스로, M열 성사여부는 yes/no 드롭다운으로 생성
- 기존 기능이 깨지지 않도록 안정적으로 작성

──────────────────────
[자동 이메일 기능]
──────────────────────
폼 제출 후 submitForm(data) 함수 안에서 자동으로 메일과 슬랙 알림이 발송되게 해줘.
실행 흐름: 웹폼 제출 → submitForm(data) → saveToSheet_(data) → 구글 시트 저장
→ sendCustomerEmail_(data) → 고객 자동응답 메일 발송
→ sendAdminEmail_(data) → 관리자 Gmail 알림 메일 발송
→ sendSlackAlert_(data) → 슬랙 채널에 새 문의 알림 + 제안서 착수 여부 질문
→ 화면에 접수 완료 메시지 표시

중요:
- 시간 기반 트리거 사용하지 마. onEdit 트리거도 사용하지 마
- submitForm(data)가 웹폼 제출 순간 바로 실행되게 해줘 (예약 작업이 아니라 그 실행 안에서 끝내야 해)
- 메일 발송·슬랙 발송 둘 다 오류가 나도 시트 저장은 완료되도록 각각 try-catch 구조 사용
- 슬랙 발송이 실패해도 메일은 정상 발송되게, 서로 독립적으로 작동하게 만들어줘

──────────────────────
[관리자 이메일]
──────────────────────
(본인 Gmail 주소로 수정해주세요)  예시: example@gmail.com

──────────────────────
[슬랙 알림 발송]
──────────────────────
슬랙 Incoming Webhook URL: (P20-b에서 복사한 주소로 수정해주세요)
예시: https://hooks.slack.com/services/여기에_본인_주소

- UrlFetchApp.fetch 로 이 URL에 POST 요청. Content-Type은 application/json
- payload 예시: {"text": "메시지 내용", "username": "팀장", "icon_emoji": ":page_facing_up:"}
- 메시지 내용에는 신청자 성함·직장명·교육 주제 및 목적·교육대상 및 인원수·교육희망 일자
  를 요약해서 넣고, 맨 끝에 "제안서 써드릴까요?"로 물어보는 문장을 붙여줘
- username을 "팀장"으로 보내도 슬랙 쪽 설정에 따라 앱 기본 이름으로 보일 수 있어.
  그건 괜찮아 — 메시지 도착 자체가 중요하지 이름 표시는 부가적인 거야
- 이 요청도 try-catch로 감싸서, 슬랙 URL이 비어있거나 요청이 실패해도
  나머지 흐름(시트 저장·메일 발송)은 멈추지 않게 해줘

──────────────────────
[고객 자동응답 메일 제목]
──────────────────────
[(본인 회사명으로 수정해주세요)] 교육 문의가 접수되었습니다

──────────────────────
[고객 자동응답 메일 내용]
──────────────────────
안녕하세요, {{신청자 성함}}님.
[(본인 회사명으로 수정해주세요)] 교육 문의가 정상적으로 접수되었습니다.
접수해주신 내용은 아래와 같습니다.
- 문의번호: {{문의번호}}   - 교육 주제 및 목적: {{교육 주제 및 목적}}
- 교육대상 및 인원수: {{교육대상 및 인원수}}   - 교육희망 일자 및 예산: {{교육희망 일자 및 예산}}
- 교육희망 장소: {{교육희망 장소}}
담당자가 확인 후 1~2 영업일 이내 연락드리겠습니다. 감사합니다.
[(본인 회사명으로 수정해주세요)] 드림

──────────────────────
[관리자 알림 메일 제목]
──────────────────────
[(본인 회사명으로 수정해주세요)] 새 교육 문의 접수

──────────────────────
[관리자 알림 메일 내용]
──────────────────────
새로운 교육 문의가 접수되었습니다.
- 접수일시: {{접수일시}}   - 문의번호: {{문의번호}}   - 신청자 성함: {{신청자 성함}}
- 직장명/부서/직책: {{직장명/부서/직책}}   - 연락처: {{연락처}}   - 이메일: {{이메일}}
- 교육 주제 및 목적: {{교육 주제 및 목적}}   - 교육대상 및 인원수: {{교육대상 및 인원수}}
- 교육희망 일자 및 예산: {{교육희망 일자 및 예산}}   - 교육희망 장소: {{교육희망 장소}}   - 상태: {{상태}}
구글 스프레드시트에서 상세 내용을 확인해주세요.

──────────────────────
[웹 화면 접수 완료 문구]
──────────────────────
교육 문의가 정상적으로 접수되었습니다. 담당자가 확인 후 1~2 영업일 이내 연락드리겠습니다.

──────────────────────
[HTML 디자인 요구사항]
──────────────────────
HP 스타일의 세련된 실무형 웹앱 디자인으로 만들어줘.
- 화이트 + 딥 네이비 + 블루 포인트, "Noto Sans KR" 계열 폰트, 넓은 여백, 카드형 UI
- 왼쪽 브랜드 소개 패널 / 오른쪽 문의 입력 폼, 모바일 반응형
- 입력창 focus 시 블루 glow 효과, 제출 버튼은 블루 그라데이션
- 성공/실패 메시지는 카드형으로 표시

──────────────────────
[브랜드 소개 영역 — 반드시 사용자가 수정할 수 있게]
──────────────────────
- 회사명 → (본인 회사명으로 수정해주세요)
- 회사 소개 → (본인 회사 소개로 수정해주세요)
- 주요 서비스 → (본인 서비스 내용으로 수정해주세요)
- 회사 슬로건 → (본인 슬로건으로 수정해주세요)

──────────────────────
[Index.html 필수 기능]
──────────────────────
- <base target="_top"> 포함, 모바일 viewport 설정, 연락처 자동 하이픈
- 모든 입력 항목 required 적용
- google.script.run으로 submitForm(formData) 호출
- 제출 중 버튼 문구 변경, 제출 완료 후 폼 reset
- 성공 시 접수 완료 문구, 실패 시 오류 메시지 표시

──────────────────────
[Code.gs 필수 함수]
──────────────────────
1. doGet() — Index.html을 웹앱 화면으로 출력
2. submitForm(data) — 전체 접수 흐름 제어
3. saveToSheet_(data) — 시트/헤더 생성, 데이터 저장, L열 체크박스, M열 드롭다운 생성
4. sendCustomerEmail_(data) — 고객 자동응답 메일 (plainBody·htmlBody 포함)
5. sendAdminEmail_(data) — 관리자 알림 메일 (plainBody·htmlBody 포함)
6. sendSlackAlert_(data) — 슬랙 Incoming Webhook으로 새 문의 알림 + 착수 질문 발송
7. customerRow_(label, value) / adminRow_(label, value) — 메일 HTML 표 행 생성
8. isValidEmail_(email) — 이메일 형식 검증
9. escapeHtml_(text) — HTML 메일 보안 처리
10. testMail() — MailApp 권한 확인용 테스트 메일 발송
11. testSlack() — 슬랙 Webhook 연결 확인용 테스트 메시지 발송

──────────────────────
[출력 요청]
──────────────────────
1. 전체 시스템 구조 설명   2. Code.gs 전체 코드   3. Index.html 전체 코드
4. 최초 실행 순서   5. 웹앱 배포 방법   6. 테스트 방법   7. 자주 발생하는 오류와 해결법

──────────────────────
[중요 조건]
──────────────────────
- Code.gs와 Index.html을 완성 코드로 각각 제공. 복사해서 바로 붙여넣을 수 있게 작성
- 비개발자도 이해할 수 있도록 주석을 충분히 작성
- 외부 유료 API 사용 금지. 시간 기반 트리거·onEdit 트리거 사용 금지
- 기존 기능이 깨지지 않도록 작성. HTML 파일명은 Index 기준으로 작성
- 메일 권한 테스트를 위해 testMail() 함수, 슬랙 연결 확인을 위해 testSlack() 함수 포함
- 코드가 너무 복잡하지 않게 수업용으로 설명 가능한 구조 유지
```

> ⏱ **Apps Script는 Codex CLI보다 답이 깁니다.** Code.gs와 Index.html 두 파일이
> 한 번에 나옵니다. 나오는 대로 Apps Script 창의 `Code.gs`와 새로 만든 `Index.html`에
> 각각 붙여넣으세요. (Apps Script 왼쪽 `+` → HTML → 파일명을 정확히 `Index`로.)

### P22 — 배포하고 링크 받기

```
1. Apps Script 화면 오른쪽 위 파란 버튼 배포 → 새 배포
2. 유형 선택(⚙️) → 웹 앱
3. 실행 계정: 나 (내 이메일)  /  액세스 권한: 전체
4. 배포 → 웹 앱 URL 이 나오면 복사
5. 처음 열면 "확인되지 않은 앱" 경고가 뜬다 → 고급 → (프로젝트 이름)으로 이동 → 허용
   (내가 만든 내 앱이라 뜨는 정상적인 경고입니다. 다른 사람 앱이면 누르면 안 됩니다.)
```

**통과 기준** — 웹 앱 URL을 새 탭에 붙여 넣었을 때 방금 만든 문의 폼 화면이 뜨면 통과.

### P23 — 실제로 접수해보고 팀장에게 연결하기

```
방금 만든 웹앱 URL로 가서 테스트 문의를 하나 접수해줘.
신청자 성함: [테스트]로 넣어서.

접수 후:
1. 구글 시트 "교육문의" 탭에 새 행이 생겼는지 확인해줘
2. 관리자 이메일로 알림 메일이 왔는지 확인해줘
3. 슬랙 채널에 "팀장" 이름(또는 앱 기본 이름)으로 새 문의 알림 + "제안서
   써드릴까요?" 질문이 왔는지 확인해줘
4. staff1.toml(팀장·제안서 담당) 본문에 이 웹앱 시트 링크를 적어주고,
   "슬랙에서 새 문의 알림을 보면, 그 문의 내용으로 제안서를 써도 될지 되묻지 말고
    시트에서 해당 행을 찾아 확인한 뒤 착수 여부만 물어본다. 사람이 '네/써줘'라고
    답하기 전까지는 제안서를 쓰지 않는다"는 규칙을 본문에 넣어줘
```

**합격 판정** — 시트에 테스트 행 1개 + 메일 1통 + **슬랙 알림 1건**(팀장이 먼저 말을 겁니다).
그 알림에 `팀장 불러서 방금 그 문의로 제안서 써줘`라고 답하면 착수합니다 —
새 장치가 아니라 오늘 만든 웹앱·시트·슬랙을 연결한 것뿐입니다.

> 🔎 **슬랙 알림이 안 오면** — 먼저 `testSlack()`을 Apps Script에서 직접 실행해
> Webhook URL 자체가 살아있는지 확인하세요. 그것도 안 되면 P20-b의 Webhook URL을
> 다시 복사해 붙여넣은 값인지(공백·따옴표 없이) 확인하세요 — 열쇠 모양 문제는
> 모듈 4에서 겪은 것과 같은 종류의 함정입니다.

---

## 모듈 4 · 슬랙에서 부르기

> **카드가 짧아졌습니다.** 열쇠 3개는 전날 한 줄 설치 때 이미 `.env` 에 들어갔고, 서버도 그때 한 번 켜 봤습니다.
> 2026-09-08 판부터 직원을 **이름으로 부르고**, 답은 **말한 직원의 말풍선**으로 올라오며, 서버가 **대표 이름**을 알고 시작합니다.
> 당일에는 **서버 켜기 두 줄 + 첫 대화**뿐입니다. 예전 관문 4개(P8-a·P9·P9-a·P10·P10-a)는
> `환경점검.bat` 과 `py 점검.py 4` 가 대신합니다.

### 서버 켜기 — 검은 창(코덱스 밖)에서 두 줄

코덱스 안이면 `/exit` 로 먼저 나오세요. 이건 코덱스에게 시키는 카드가 아니라 **검은 창에 치는 명령**입니다.

```
cd "C:\Agent\01_KIT\starter-kit\slack-server"
py -3 -X utf8 server.py
```

성공하면 이렇게 찍힙니다. **이 창은 닫으면 안 됩니다.**

```
직원 파일: 5개                     ← 여기가 0개면 아무도 대답 안 합니다 → 모듈 1
직원 등록: 팀장(staff1), 교재대리(staff2), ... (staff5)
대표 이름: 홍길동 (출처: facts.md)   ← (이름 모름) 이면 P1 의 내 이름이 비어 있는 것
✅ 준비 완료 — 슬랙 '내 워크스페이스' 에서 말을 걸어보세요.
```

> 🔎 **`직원 파일: __개` 를 눈으로 읽고 넘어가세요.** `0개` 인데 `준비 완료` 만 보고 넘어가면
> 그다음부터 원인을 찾기 어렵습니다. `0개` 면 모듈 1로 돌아가 직원을 만들고 서버를 다시 켭니다.

### 첫 대화

**슬랙에는 채널이 하나도 없는 게 정상입니다.** 프로그램은 채널을 만들지 않습니다.

1. 왼쪽 **다이렉트 메시지** 옆 `+` (또는 왼쪽 위 ✏️) → `AI` 검색 → **AI 직원** 클릭
2. `안녕` 이라고 쳐보세요. 팀장이 대답합니다. `팀 소개` 라고 치면 다섯 명이 차례로 인사합니다
3. 대화창에 **사진이나 PDF 를 끌어다 놓고** "이거 읽어줘" 라고 해보세요. 직원이 파일을 열어 읽습니다.
   받은 파일은 `workspace/받은파일/<날짜>/` 에 남습니다 (슬랙 앱에 `files:read` 권한 필요)
3. 검은 창에 `전달: 채널=DM 직원=staff1 …` 이 뜨면 슬랙 → 서버는 정상입니다
4. `내 이름이 뭐야` 라고 물어보세요. P1 에 적은 **내 이름**이 나와야 합니다. 다른 이름이 나오면 `새 대화` 를 치고 아래 🚑

**합격 판정** — 다른 검은 창에서 스타터킷 폴더로 가서 `py 점검.py 4`. 전부 ✅ 여야 합니다.
값은 화면에 찍히지 않으니 화면 공유 중에도 안전합니다.

> 🚑 **"채팅이 막혀 있습니다"** 라고 뜨면 슬랙 앱 설정 문제입니다.
> `Features → App Home → Show Tabs` 에서 **Messages Tab 켜기 + 바로 아래 체크박스도 체크**
> → 슬랙 `Ctrl`+`R`. **체크박스를 빼먹으면 그대로 잠겨 있습니다.**
>
> 🚑 **채널에서 무반응**이면 초대를 안 한 겁니다. `/invite @AI 직원`.
> 1:1 대화는 초대가 필요 없지만 **채널은 초대가 필수**입니다.
>
> 🚑 슬랙에서 **"다시 로그인하라"** 고 하면 구글·네이버를 붙인 뒤 **서버를 다시 안 켠 것**입니다.
> 서버 창에서 `Ctrl`+`C` → `py -3 -X utf8 server.py`.
>
> 🚑 `❌ .env 에 이게 비어 있습니다` / 열쇠가 틀렸다 → `환경점검.bat` 더블클릭. 열쇠 3개를 다시 묻습니다.

### 직원 부르는 법 — 이름을 문장 맨 앞에 (2026-09-08 판)

| 이렇게 치면 | 누가 받나 |
|---|---|
| `안녕` / `제안서 써줘` (이름 없이) | **팀장(staff1)** 이 받습니다. 남의 일이면 그 직원 이름으로 답하고, 큰 일은 그 직원에게 넘깁니다 |
| `교재대리 불러서 워크북 만들어줘` / `@교재대리 …` / `교재대리아 …` | **그 직원**이 직접 받습니다 (P11 에서 정한 표시 이름, `staff2` 같은 키도 됩니다) |
| `#교재대리` 채널에서 (초대 필수 `/invite @AI 직원`) | 그 직원 |
| `팀 소개` | 5명이 한 명씩 인사합니다 |
| `새 대화` | 어제 대화·잘못 기억한 이름을 지우고 새로 시작합니다. **직원이 나를 남의 이름으로 부르면 이것부터** |

> 답이 여러 줄이면 줄마다 `이름: 말` 로 나오고, 그 이름의 직원 말풍선으로 올라갑니다.
> 그래서 **팀장 혼자 다 대답하지 않습니다** — 교재 얘기는 교재대리가, 일정 얘기는 일정매니저가 한 줄씩 말합니다.
> 서버가 켜질 때 `대표 이름: ○○ (출처: facts.md)` 가 찍힙니다. `(이름 모름)` 이면 P1 의 `내 이름` 이 비어 있는 겁니다.

### P11 — 이름·아이콘·인사말 붙이기 (캐릭터 완성)

```
슬랙에 뜨는 직원 이름·아이콘·인사말을 내 팀에 맞게 바꾸고 싶어.
slack-server/personas.py 를 고쳐줘. 먼저 .codex/agents/staff1.toml ~ staff5.toml 를 읽고
각 직원의 담당과 성격을 파악한 다음, 아래 표대로 바꿔줘.

  staff1 → 이름 [팀장]        아이콘 [📄]
  staff2 → 이름 [교재대리]    아이콘 [📚]
  staff3 → 이름 [블로그대리]  아이콘 [✍️]
  staff4 → 이름 [일정매니저]  아이콘 [📅]
  staff5 → 이름 [프로필대리]  아이콘 [🪪]

- display_name 과 icon_emoji 를 위 표대로 바꿀 것. 이모지는 📄 처럼 그대로 넣어도 된다 (서버가 슬랙 형식으로 바꾼다)
- intro 와 intro_class 에 있는 [무엇]·[무슨 일]·[재밌는 한마디]·[내 회사 이름] 대괄호를 전부 지우고,
  그 직원 파일의 담당과 성격이 드러나는 말로 채울 것. 한 줄 40자 안팎. 대괄호가 하나도 남지 않게
- CLASS_OPENING 의 [내 회사 이름] 은 workspace/memory/facts.md 의 회사 이름으로 바꿀 것
- 내부 키(staff1 같은 영어 이름)와 파일 구조는 바꾸지 말 것 — 바꾸면 연결이 끊긴다
- 다 고친 뒤 5명의 이름·아이콘·인사말을 표로 보여주고, 서버를 다시 켜야 한다고 알려줄 것
```

> 🎨 이름은 **내 팀에 맞게** 지으세요. 위 예시는 P1 의 5가지 일 순서대로 붙인 것뿐입니다.
> 이름을 정했으면 **그 이름으로 부르면** 그 직원이 나옵니다 (위 표).
> ⚠️ 인사말의 `[무엇]` 을 안 채우면 "팀 소개" 때 `제 담당은 [무엇]이에요` 가 그대로 나옵니다 — 그게 "캐릭터가 안 생긴" 증상입니다.

**합격 판정** → `py 점검.py 4` … 16개 전부 ✅ (대표 이름 · 표시 이름 · 아이콘 · 인사말 빈칸까지 봅니다). 서버를 다시 켜야 반영됩니다.

> 🚑 **직원이 나를 다른 이름(강사 이름 등)으로 부르면** — ① `workspace/memory/facts.md` 의 `- 이름:` 줄이 내 이름인지 확인 ② 슬랙에 `새 대화`.
> 그래도면 `C:\Users\<나>\.codex\AGENTS.md` 와 `.codex\projects\…\memory\` 에 남의 이름이 있는지 봅니다. 서버 켤 때 `대표 이름:` 줄이 답입니다.
>
> 🚑 **팀장만 대답하고 나머지 직원이 안 나오면** — 2026-09-08 이전 판 서버입니다. 아래 「이미 설치한 킷 새 판으로 올리기」 한 줄.
> 새 판인데도 그러면 이름을 찍어 부르세요 (`교재대리 불러서 …`).
>
> 🚑 **아이콘이 앱 기본 아이콘으로만 뜨면** — ① 옛 판은 📄 같은 이모지를 못 알아듣습니다 → 새 판으로 ② 슬랙 앱 `OAuth & Permissions → Bot Token Scopes` 에 `chat:write.customize` 가 있는지 → 추가했으면 **Reinstall to Workspace** → 서버 재시작.
>
> 🚑 **"팀 소개" 에 `[무엇]` 이 그대로 나오면** — P11 새 판(위 카드)을 다시 붙여넣으세요. 인사말까지 채웁니다.

---

## 모듈 5 · 사무실 차리기

### P12 — 오피스 실행

```
office 폴더에서 가상 오피스를 실행하는 방법을 알려줘.

- 여기는 파이썬이 아니라 npm 이야
- node_modules 가 없으니 먼저 npm ci 로 설치한다 (약 690MB, 인터넷 필요, 1분 안팎)
- npm run dev 로 켜고 브라우저에서 localhost:3000
- 슬랙 서버(py)와 오피스(npm)는 둘 다 켜져 있어야 한다고 알려줄 것
```

### P13 — 내 회사로 바꾸기

```
company.config.ts 파일만 고쳐서 내 회사로 바꿔줘.
다른 파일은 절대 건드리지 마.

회사 이름: [길동컨설팅]
대표 이름: [홍길동]
직원 5명의 이름은 .codex/agents/ 에 있는 것과 똑같이 맞춰줘.

지켜야 할 것:
- 부서 id(영어 단어)는 절대 바꾸지 말 것
- 부서는 12개를 유지할 것 — 안 쓰는 부서는 지우지 말고 이름만 바꿀 것
- app/ 폴더는 건드리지 말 것
- 색은 지금 쓰는 파스텔 톤에서 벗어나지 않게 할 것
```

**합격 판정** → `py 점검.py 5`

---

## 모듈 6 · 6번째 직원 데려오기 · 시연하고 가져가기

> 오늘은 5명으로 **완벽하게 도는 것**을 확인했습니다. 직원은 필요할 때 **한 명씩** 늘립니다.
> `직원견본/추가/` 에 교안수석(새 과정 강의 설계) · 정산과장(강사료·세금) · 디자이너(인스타 카드뉴스)가 준비돼 있습니다.

### P14-a — 핵심 5명이 다 되는지 확인 (직원을 늘리기 전에)

**직원을 늘리기 전에 여기서 한 번 멈춥니다.** 5명이 제대로 안 도는 상태에서 3명을 더 붙이면
무엇 때문에 안 되는지 찾을 수가 없습니다.

**① 검은 창에서 순서대로** — 전부 ✅ 여야 합니다.

```
py 점검.py 1     직원 5명이 있고 담당이 안 겹친다
py 점검.py 2     스킬이 있고 결과물이 나온다
py 점검.py 3     팀 규약·쪽지·교재 워크북
py 점검.py 35    내 자료(MyData)가 스킬에 들어갔다 · 견본이 안 남았다
py 점검.py 4     슬랙 연결 · 내 이름 · 표시 이름 · 아이콘 · 인사말
py 점검.py 5     가상 사무실
```

**② 슬랙에서 다섯 가지** — 직접 쳐 보고 눈으로 확인합니다.

| 쳐 볼 말 | 이렇게 나와야 합니다 |
|---|---|
| `안녕` | **내 이름**으로 부릅니다. 엉뚱한 이름이면 `py 점검.py 4` 의 이름 항목을 보세요 |
| `팀 소개` | 다섯 명이 **각자 다른 이름·아이콘**으로 차례로 인사합니다 |
| `블로그 하나 써줘` | 팀장이 아니라 **블로그대리**가 답합니다 (담당이 나뉜다는 증거) |
| 사진 한 장 끌어다 놓고 `이거 뭐야` | 파일을 **열어 읽고** 답합니다. `workspace/받은파일/` 에 파일이 생깁니다 |
| 아무 일이나 시킨 뒤 `workspace/기록/작업일지.md` 열기 | 방금 시킨 일이 **한 줄로 적혀** 있습니다 |

**③ 하나라도 어긋나면 직원을 늘리지 않습니다.** 그 항목부터 고칩니다.
다섯 명이 조용히 잘 도는 것이 여덟 명이 삐걱대는 것보다 낫습니다.

---

### 추가 직원 3명 — 언제 누구를 데려오나

`직원견본/추가/` 에 세 명이 준비돼 있습니다. **필요해진 다음에** 한 명씩 데려옵니다.

| 견본 | 담당 | 이럴 때 데려옵니다 | 아이콘 |
|---|---|---|---|
| `교안수석.toml` | 강의 설계 — 모듈 구조·시간표·학습자 활동·스크립트 아웃라인 | 가진 교안이 없는 **새 과정**을 자주 만들 때 | 🎓 |
| `정산과장.toml` | 강사료 장부·입금 확인·미수금·3.3%·부가세·종소세 일정 | 강의 건수가 늘어 **입금 확인이 일**이 될 때 | 💰 |
| `디자이너.toml` | 인스타 카드뉴스·SNS 이미지·캡션·해시태그 | **인스타 홍보**를 시작할 때 | 🎨 |

세 파일 모두 `name = "staff6"` 으로 돼 있습니다. **한 명**만 데려올 때는 그대로 두면 되고,
**두 명 이상**이면 두 번째부터 파일 이름과 `name` 을 `staff7`·`staff8` 로 맞춥니다(카드 P14-c).

---

### P14 — 6번째 직원 데려오기

```
직원견본/추가/ 폴더에 추가 직원 견본 3명이 있어 (교안수석·정산과장·디자이너).
[정산과장] 을 여섯 번째 직원으로 데려와줘.

1. 직원견본/추가/[정산과장].toml 를 .codex/agents/staff6.toml 로 복사할 것. name 은 staff6 그대로.
2. slack-server/personas.py 에 "staff6" 칸을 추가하고 INTRO_ORDER 끝에 "staff6" 을 넣을 것.
   표시 이름 [정산과장], 아이콘 [💰]. intro 와 intro_class 는 직원 파일의 담당·성격으로 채우고 대괄호를 남기지 말 것.
3. 다른 직원 파일과 다른 personas 칸은 건드리지 말 것.
4. 끝나면 "서버를 다시 켜야 한다"고 알려주고, 슬랙에서 "[정산과장] 불러서 …" 로 부르면 된다고 알려줄 것.
```

> ➕ 두 번째로 데려올 때는 `staff7.toml` 로 복사하고 파일 안의 `name:` 도 `staff7` 로 맞추라고 덧붙이면 됩니다.
> 견본에 없는 직원(유튜브 담당 등)은 P3-직접 형식으로 새로 씁니다.

**합격 판정** → `py 점검.py 4` … "짝이 없는 키" 없이 전부 ✅. 서버를 다시 켜면 `직원 파일: 6개`.


### P14-c — 두 번째·세 번째 추가 직원 (7·8번째)

**한 번에 한 명씩.** 한 명 붙이고 → 서버 다시 켜고 → 슬랙에서 불러 보고 → 그 다음 한 명입니다.
세 명을 한꺼번에 붙이면 안 될 때 누구 때문인지 알 수 없습니다.

```
직원견본/추가/[디자이너].toml 를 일곱 번째 직원으로 데려와줘.

1. .codex/agents/staff7.toml 로 복사할 것.
   ⚠️ 견본 파일 안의 name 이 staff6 으로 돼 있으니 staff7 로 고칠 것 (파일 이름과 name 이 같아야 한다).
2. slack-server/personas.py 에 "staff7" 칸을 추가하고 INTRO_ORDER 끝에 "staff7" 을 넣을 것.
   표시 이름 [디자이너], 아이콘 [🎨]. intro 와 intro_class 의 대괄호를 남기지 말 것.
3. 이미 있는 직원 파일과 personas 칸은 건드리지 말 것.
4. 끝나면 서버를 다시 켜라고 알려줄 것.
```

여덟 번째도 같은 카드에서 **staff7 을 staff8 로, 이름과 아이콘만** 바꿔 쓰면 됩니다.

**붙인 뒤 확인** — `py 점검.py 4` 에서 "짝이 없는 키"·"빈칸 남음" 이 없어야 하고,
서버를 다시 켜면 `직원 파일: 7개` 로 나옵니다. 슬랙에서 이름을 불러 답이 오면 끝입니다.

> 🧹 **데려온 직원을 도로 내보내려면** `.codex/agents/staff7.toml` 를 지우고
> `personas.py` 의 `"staff7"` 칸과 `INTRO_ORDER` 의 `"staff7"` 도 같이 지웁니다.
> 파일만 지우고 personas 를 두면 점검 4 에서 "짝이 없는 키" 로 잡힙니다.

### P14-b — 시연 시나리오

```
지금까지 만든 걸 1분 안에 보여주는 시연 순서를 짜줘.

조건:
- 1분 안에 끝날 것
- 슬랙에서 직원 부르기 → 결과물 생기기 → 오피스 화면 확인,
  이 세 가지가 다 들어갈 것
- 내가 슬랙에 그대로 칠 문장을 만들어줄 것
- workspace/결과물/시연대본.md 로 저장할 것
```

**합격 판정** → `py 점검.py 6`

**내 회사 백업** → 스타터킷 폴더의 `02_내작업_백업.bat`을 더블클릭하세요.
`workspace`, 직원, 스킬, 오피스 회사 설정만 ZIP으로 묶고 `.env`와 로그인 상태는 자동으로 뺍니다.

---

## 이미 설치한 킷 새 판으로 올리기 — 재수강생·강의 후 배포

**재수강이시면 설치 한 줄(install.ps1)이 아니라 이것을 쓰세요.**
이미 깔려 있는 상태에서 설치 한 줄을 돌리면 기존 폴더를 건드리지 않고 그냥 끝나서 **새 판으로 바뀌지 않습니다.**
(코덱스판은 바뀌기는 하지만 **직원 파일이 견본으로 되돌아갑니다.**)

PowerShell 창에 아래 한 줄. 엔진 파일만 바꾸고 **내가 만든 직원·스킬·personas.py·.env 는 그대로** 둡니다.

```powershell
irm https://raw.githubusercontent.com/mandoo356/wd-agentteam-kit-codex/main/update.ps1 | iex
```

CMD 창이면:

```bat
powershell -NoProfile -ExecutionPolicy Bypass -Command "irm https://raw.githubusercontent.com/mandoo356/wd-agentteam-kit-codex/main/update.ps1 | iex"
```

끝나면 서버가 새 창에서 켜집니다. 슬랙에 `새 대화` 를 치고, 이름·인사말이 아직이면 P11(새 판)을 한 번 더.

## 집에 가서 다시 켜는 법

```
1. slack-server 폴더에서 →  py -3 -X utf8 server.py
2. office 폴더에서       →  npm run dev
3. 브라우저             →  localhost:3000
```

검은 창 두 개는 켜둔 채로.

---

## 망가졌을 때

```
git checkout .
```

마지막 세이브 지점으로 되돌아갑니다. 무서워하지 말고 이것저것 해보세요.
