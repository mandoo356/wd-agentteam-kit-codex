# 에이전트팀 만들기 과정 — 문서 사용 안내

- 기준일: 2026-09-07 · Codex 전환판
- 강의 기준본: `교안1/학습자교안_통합_WD - 수정판.html` (2026-09-07 개편본)
- 호환 경로: `교안1/학습자교안_통합_WD.html` (같은 수업 내용 유지)
- 수강생 배포 원본: `스타터킷/`
- 안전한 배포 ZIP 생성(예비): `01_수강생_배포본_만들기.bat`
- 한 줄 설치 갱신(선택): `03_깃허브_배포_갱신.bat` — Codex 전용 저장소를 만든 뒤에만 사용
- 설치·전송·무응답 위험 분석: `스타터킷_설치·전송·무응답_위험분석_2026-09-03.md`

## 기본 설치 — PowerShell 또는 CMD 한 줄

PowerShell:
```powershell
irm https://raw.githubusercontent.com/mandoo356/wd-agentteam-kit-codex/main/install.ps1 | iex
```
CMD(명령 프롬프트):
```bat
powershell -NoProfile -ExecutionPolicy Bypass -Command "irm https://raw.githubusercontent.com/mandoo356/wd-agentteam-kit-codex/main/install.ps1 | iex"
```
Node.js 설치 후 Codex만 설치할 때는 두 창 모두 `npm.cmd install -g @openai/codex`를 사용합니다. 로그인은 `codex.cmd login`입니다.
PowerShell 자체가 기관 정책으로 차단되면 CMD 한 줄도 작동하지 않습니다. 별책의 수동 설치 절차를 참고하고, 훅을 포함한 전 과정 실습은 PowerShell을 사용할 수 있는 Windows 노트북에서 진행합니다.

별책: `05_검은창_매뉴얼_CMD와PowerShell_WD.html` (배포본에서는 `docs/`)

## 강사·학습자 진행 순서

1. 강사는 `03_깃허브_배포_갱신.bat`으로 검증된 스타터킷과 교안을 GitHub에 배포합니다.
2. 수강생은 교안 2~3쪽에서 개인 슬랙 워크스페이스·앱을 만들고 봇 토큰·앱 토큰·내 멤버 ID를 먼저 준비합니다. 다음으로 4쪽의 한 줄 설치를 실행합니다. 예비 ZIP은 강사가 `01_수강생_배포본_만들기.bat`으로 만듭니다.
3. 설치는 환경점검으로 바로 이어집니다. Node.js 22 이상·Git·Codex CLI 설치, Python 3.14 설치·실행 확인, 패키지 설치·실제 import, ChatGPT 로그인, 슬랙 열쇠 입력·검증·서버 첫 연결 시험을 진행합니다. 프로그램 설치 여부는 다시 묻지 않습니다. 로그인과 Windows 권한 창은 사람이 처리합니다.
4. MyData에 제안서 3·블로그 3·로고 1 → Codex 프로젝트 신뢰·`/hooks` 검토·신뢰 → 환경점검 재확인 → 구글 도구 실제 조회 → 모듈 1~6 순서입니다. 모듈 4에서는 서버 켜기·첫 대화를 합니다.
5. 수업 종료 전 내 작업 백업을 실행합니다.

## 자동 기록·저장·위험 작업 확인 — Codex판

1. 설치 후 `01_Codex_YOLO_시작.bat`에서 프로젝트를 신뢰하고 `/hooks`를 엽니다. 새로 추가·변경된 5개 이벤트를 검토하고 신뢰합니다. 업데이트 뒤에도 다시 확인합니다.
2. `workspace/기록/작업기록_날짜.md`에 **Codex 훅 실행 확인**이 남는지 확인합니다. 파일 존재 점검만으로 작동을 보증하지 않습니다.
3. 요청 접수·도구 종료·변경 파일·차단 이유가 한글로 기록됩니다. 요청 원문과 명령 인수는 비밀키 노출을 줄이기 위해 기록하지 않습니다.
4. `git init` 후 답변 종료 시 결과물·직원·스킬·팀 규약 변경을 자동 커밋합니다. 변경이 없거나 사용자가 이미 스테이징한 파일이 있으면 생략합니다. 기록 파일만으로 커밋을 만들지 않습니다. 전체 회사 보관은 `02_내작업_백업.bat`을 사용합니다.
5. 삭제·이동·되돌리기·셸 덮어쓰기·보호 파일 편집은 훅이 **차단**합니다. Codex의 PreToolUse 훅은 `ask` 확인창을 지원하지 않습니다. 대상을 확인하고 백업한 뒤 사람이 별도 터미널에서 직접 명령을 실행합니다. 슬랙에서는 터미널로 안내합니다.
6. 일반 문서는 `apply_patch`로 수정하고, 최초 직원·스킬·규약 파일 생성은 허용합니다. 보호 대상은 `.codex/`, `.agents/`, `AGENTS.md`, `workspace/memory/`, 회사 설정·열쇠입니다.
7. 훅은 보조 안전장치입니다. 임의 프로그램·모든 외부 도구를 완벽히 통제하지 않으며, YOLO는 운영체제 샌드박스를 해제합니다. 훅을 신뢰하지 않았거나 꺼 두면 보호·기록이 작동하지 않습니다.

설치 위치를 옮겼으면 `powershell -NoProfile -ExecutionPolicy Bypass -File configure_hooks.ps1`을 실행한 뒤 `/hooks`에서 다시 신뢰하세요. 자동화는 이미 설치하는 **Node.js 22 이상**으로 실행하여 Python 실행기 버전 충돌을 줄였습니다.

확인 명령: `git log --oneline -10`. 복원은 `git diff`로 변경을 살펴보고 백업한 뒤 `git restore --source=커밋번호 -- 파일경로`를 사람이 직접 실행합니다.

공식 근거: https://learn.chatgpt.com/docs/hooks

## 변경 이력 (2026-09-07) — 핸드오프 HO_20260907_010256

- 슬랙 열쇠 준비를 설치 앞으로 옮기고, 수정판을 강의 기준본으로 지정했습니다. 두 교안의 29쪽 구성·탭·방향키·복사·전체 인쇄 기능을 유지합니다. 화면 캡처는 단계마다 한 장씩 보여줍니다.
- 설치 확인 질문을 제거하고 Python 3.14를 수업 표준으로 고정했습니다. 실행 파일의 절대경로로 패키지와 점검을 실행합니다. 기존 다른 Python 버전을 삭제하지 않습니다.
- `codex.cmd`·`npm.cmd`를 사용하므로 PowerShell 실행 정책을 영구 변경하지 않습니다. Codex는 Node.js 22 이상 설치 후 `npm.cmd install -g @openai/codex`로 설치합니다.
- 슬랙 열쇠 3개를 가려서 입력받고 UTF-8 BOM 없이 저장합니다. 기존 다른 설정은 보존합니다. `slack_check.py`는 봇·앱 토큰을 API로 검증하고 멤버 ID는 형식만 확인합니다. 서버 시험은 실제 소켓 연결 로그까지 확인합니다.
- P0·P8-a·P9·P9-a·P10·P10-a 카드를 제거했습니다. 환경점검과 `점검.py 4`가 대신하고, 로그 분석 프롬프트는 점검으로 원인을 찾지 못할 때만 사용합니다.
- 슬랙 브리지는 동일 Windows 계정의 환경과 스타터킷 프로젝트를 사용합니다. ChatGPT 앱 연결이 Codex CLI에 자동 제공된다고 보장하지 않습니다. 도구·조회 성공을 확인하고, 연결 변경 후 CLI·서버를 재시작합니다.
- `github/README.md`·`github/starter-kit/README.md`는 수정 금지 대상이므로 코디네이터가 날짜·이력을 갱신합니다.

## 변경 이력 (2026-09-07) — 핸드오프 HO_20260907_005353

- 설치·환경점검에서 누락된 `.codex/agents`, `.agents/skills`, `workspace/inbox`, `workspace/memory`, `workspace/결과물` 폴더를 복구한다. 설치 시 `slack-server/logs`도 복구한다. 기존 파일 내용은 보존한다.
- 폴더를 만들 수 없거나 필수 파일이 없으면 환경점검에서 실패 경로와 확인 사항을 표시한다.
- 슬랙 캡처 20은 Verification Token 값을 가린 화면으로 반영했다. 대응 수정판이 없어 Codex 기준본에서 `교안1/학습자교안_통합_WD - 수정판.html`을 만들었다. 기준본·수정판 모두 29쪽이며 강의 기준본 경로는 유지한다.
- 교안·별책·스타터킷 README의 날짜와 변경 이력을 갱신했다. README 3종은 `스타터킷/README.md`, `github/README.md`, `github/starter-kit/README.md`로 해석한다. `github/`의 두 사본은 수정 금지이므로 코디네이터 갱신 대상으로 남긴다.

## 변경 이력 (2026-09-05) — 핸드오프 HO_20260905_224521

- 슬랙 앱 manifest의 `display_name`을 한글이 아닌 `AI`로 통일했다.
- `OWNER_USER_ID`를 필수 입력값으로 명확히 하고, 슬랙 앱에서 **프로필 → 더보기 → 멤버 ID 복사**로 찾는 절차를 교안·상세 안내·프롬프트 카드에 반영했다.
- 모듈별 점검 순서, 최초 Git 출발선 저장, 복원 전 백업 안내를 Codex 훅의 차단 방식에 맞춰 보강했다.
- `github/`와 `distribution/`은 핸드오프 수정 금지 경로이므로 코디네이터의 배포 단계에서 기준본으로 다시 생성한다.

## 변경 이력 (2026-09-05) — 핸드오프 HO_20260905_230011

- 슬랙 서버가 실행 모드와 관계없이 `OWNER_USER_ID`가 비어 있으면 시작되지 않도록 강화했다.
- `py 점검.py 4`에서 토큰 모양과 멤버 ID 모양을 별도 항목으로 검사하고, 안내문·교안·프롬프트 카드의 통과 기준을 세 항목으로 맞췄다.
- `github/`는 핸드오프 수정 금지 경로이므로 `github/README.md` 갱신은 코디네이터 배포 단계에 남겼다.

## 변경 이력 (2026-09-05) — 핸드오프 HO_20260905_134754

- 전달된 제목 한 줄을 Codex CLI 기준으로 재작성해 `test_manual.txt`에 반영했다. 원본에는 실행 절차 본문이 없다.
- 교안·검은 창 별책·스타터킷 README의 개정 이력을 갱신했다. `github/`의 README는 핸드오프의 수정 금지 규칙에 따라 코디네이터 갱신 대상으로 남겼다.

## 변경 이력 (2026-09-04 오후) — 설치 폴더 이름·MyData

- Codex판 설치 루트 `C:\WD-AgentTeam` → **`C:\Agent`**. ZIP 설치 스크립트·README·프롬프트카드·교안 기준본의 경로를 함께 교체했다.
- 설치 시 **`MyData\Proposal·Blog·Logo·Profile`**(제안서·블로그·로고·프로필) 4개 폴더와 `여기에_넣으세요.txt`를 함께 만든다. 환경점검에 「내 자료 (제안서 3·블로그 3·로고 1)」 선택 항목을 추가했다.
- 교안 표지 사전 과제·4쪽(8번 내 자료 미리 넣기)·P1-B·18쪽·28쪽 체크리스트에 반영했다. 카드 P1-B·P17·P18·C2 경로는 `C:/Agent/MyData/...`를 쓴다.
- 새로 만드는 경로는 전부 영문(`C:\Agent\MyData\…`)으로 통일하고, 킷 안의 기존 한글 파일명은 유지한다.

## 정리 이력 (2026-09-04)

구형 교안 8개(`00_환경설정_WD`, `01_기획안` 2, `02_교안` 2, `06_학습자교안` 2, `07_간결판`)와
루트 중복 `flow-image-JJ/`, `withdream-instructor-profile.skill`, 무관한 태평양 제안서 PDF는 **휴지통으로 이동**했다.
복구가 필요하면 휴지통 또는 git 커밋 `512d736`(삭제 직전 스냅샷)에서 꺼낸다.
`office/node_modules` 등 오피스 프로그램은 대표 지시로 삭제하지 않는다.

남은 문서는 전부 현행이다: `교안1/`(기준본·검증보고), `04_슬랙앱_안내문` 2종 + `04-1` 상세, 위험분석·검수보고, `강의문의 자동하/`(P20~P23), `학습자가만드결과/`(예시).

## 배포 금지

- 원본 `스타터킷/` 폴더를 탐색기에서 직접 ZIP으로 만들지 않는다.
- 실제 `.env`, 로그인 상태, `환경점검_결과.html`, `node_modules`, 캐시·로그는 배포하지 않는다.
- `distribution/`에서 생성된 ZIP만 전달한다.
