# 에이전트팀 만들기 과정 — 문서 사용 안내

- 기준일: 2026-09-18
- 강의 기준본: `교안1/학습자교안_통합_WD - 수정판.html` (캡처 반영본. 2026-09-07 전면 개편)
- 수강생 배포 원본: `스타터킷/`
- 한 줄 설치 갱신(기본): `03_깃허브_배포_갱신.bat`
- 안전한 배포 ZIP 생성(예비): `01_수강생_배포본_만들기.bat`
- 설치·전송·무응답 위험 분석: `스타터킷_설치·전송·무응답_위험분석_2026-09-03.md`
- 별책 검은 창 매뉴얼(CMD vs PowerShell · 파워셸 없을 때): `05_검은창_매뉴얼_CMD와PowerShell_WD.html` (2026-09-05)

## 변경 이력 (2026-09-18) — 저녁 자동 블로그 · AI 삽화 제거 · 로그 배포 제외

- 카드 P20-c와 `스타터킷/저녁블로그/`를 추가했다. 매일 20:00에 일정매니저가 끝난 강의를 확인하고, 블로그대리가 원고 작성과 네이버 임시저장까지만 처리한다.
- 네이버 블로그 AI 삽화 생성 코드를 제거했다. 사진은 사용자가 `--images` 또는 `--images-dir`로 지정한 것만 넣는다.
- 설치·배포 복사에서 `*.log`를 제외하고, 저녁 자동화 결과는 `evening_blog.log`로 확인하도록 정리했다.

## 변경 이력 (2026-09-16) — 절전 점검 · 쓰던 스킬 가져오기 · 슬랙 채널 입장

- PC 절전이 슬랙 서버를 멈추는 문제를 README와 `점검.py 4`에 반영했다. 전원 연결 상태의 절전 시간을 `powercfg`로 확인한다.
- `MyData\Skill`과 카드 P17-2를 추가했다. 기존 `SKILL.md`는 설치 전 요약·승인을 거쳐 `.agents/skills/`에 넣고, 같은 업무 스킬은 `_보관`으로 옮긴다.
- 슬랙 앱 권한 14개 확인, 공개 채널 직접 입장, 그룹 DM 수신 안내를 반영했다. 새 권한을 넣은 뒤에는 Slack 앱을 다시 설치해야 한다.
- 블로그 P4-0 → 네이버 임시저장 P4-1 → 제안서 P4 순서와 담당 직원 자동 배정을 교안과 프롬프트 카드에 반영했다.

## 변경 이력 (2026-09-15) — 타사 모델 혼동 차단 · Codex 모델 안내

- 강의 기준본과 수강생 배포 교안의 모델 안내를 Codex 전용으로 정리했다. 수강생은 **ChatGPT Plus + GPT-5.6 Terra**, 단순 점검은 Luna, 어려운 제작은 Sol, 강사 고난도 문제 해결은 Astra를 사용한다.
- 타사 모델명은 Codex 교안에서 사용하지 않는다. 옛 문구가 든 백업 교안은 `_구판_백업_열지마세요/`로 분리한다.

## 변경 이력 (2026-09-14) — 파일 읽기 · 이름 복구 · 핵심 5명 확인 관문

- 슬랙 앱에 `files:read` 권한을 추가하고 사진·PDF·PPT를 `workspace/받은파일/<날짜>/`에 안전하게 내려받아 직원에게 전달하도록 서버를 보강했다.
- `roster.py`가 Codex 직원 TOML과 UTF-8·CP949의 대표 이름을 읽도록 고쳤고, `점검.py 4`가 이름·직원 파일·아이콘·인사말을 함께 검사한다.
- 모듈 6에 P14-a(핵심 5명 완성 확인)와 P14-c(7·8번째 직원 한 명씩 추가)를 넣었다. 직원 견본은 `.codex/agents/*.toml`, 스킬은 `.agents/skills/*/SKILL.md` 기준이다.

## 변경 이력 (2026-09-08 저녁) — 핵심 직원 5명 체계 · JJ 삭제 · 메일 연동 · 엔진 속도 · 구글 커넥터 조건 (대표 지시)

대표 지시 ① "핵심 역할 팀장·교재·블로그대리·일정매니저·프로필대리까지만. 완벽하게 연동되고 역할을 찾고 속도가 나오면 그다음에 에이전트를 추가하는 방식" ② "교안은 대부분 강사가 갖고 있어 그걸 전달해 교재를 만드는 게 낫다(토큰 절약·안정)" ③ "구글 메일·드라이브·캘린더 연결이 터미널에서 안 됐다 — 오류 없는 방향" ④ "네이버 블로그 AI 이미지 3장·JJ 스킬 삭제(너무 오래 걸림), 네이버·다음 메일 연동으로" ⑤ "지금도 느린데 더 느려지면 안 된다" ⑥ "MCP 는 ChatGPT 앱 커넥터에서".

- **직원 5명 재편** `직원견본/staff1~5` = 팀장(제안서·견적·**문의 메일 읽기·회신 초안**) · 교재대리(**대표의 교안·PPT·PDF → 교재**, 설계 안 함, HWP 는 PDF 로) · 블로그대리(글만 임시저장) · 일정매니저 · 프로필대리(기업·공공기관·주제 맞춤). `직원견본/추가/` 교안수석·정산과장·디자이너(`name: staff6`). 번호 1~5 재부여 — 카드·교안·연결·전문스킬 카드의 staff 번호 전수 교체(P8b staff3→staff2, C1 staff8→staff4, 전문스킬 표).
- **채점기** `점검.py` 모듈 1·4·5 "8명 이상"→"5명 이상". `personas.py` 5칸(+staff6 추가 안내 주석). `agent_channels.py` 는 이미 staff1~5.
- **엔진 정리** (`codex_bridge.py`·`server.py`) — facts.md와 inbox를 서버가 읽어 프롬프트에 넣고, `codex exec --json`의 `thread.started` 세션 ID를 3시간 유지한다. 90초가 지나면 `INTERIM_SEC` 기준 중간 보고를 보낸다.
- **JJ 삭제** `flow-image-JJ/` 폴더 제거. `env_check.ps1` Google Flow 항목·자동 로그인 제거. `naver_draft.py` 기본 `--auto-images 0`, 이미지 0장이면 **글만 저장**(예전엔 Gemini 3장 자동 생성 후 없으면 거부). 카드 3.5·P19·P20, 교안 19쪽, README, naver-blog README 반영.
- **메일 연동 신설** `mail/` — `mail_common.py`(IMAP 993/SMTP 465 + 앱 비밀번호, naver/daum/gmail, mail/.env → slack-server/.env 순) · `mail_check.py`(OK/FAIL 한 줄) · `mail_read.py`(BODY.PEEK, 📌문의 표시, --json) · `mail_send.py`(**--approved 없으면 초안만·종료 2**, 외부 주소는 `MAIL_ALLOW_EXTERNAL=1` 전엔 종료 3) · `skill/SKILL.md` · `README.md`. `env_check.ps1` 6/6 에 앱 비밀번호 별표 입력 → `mail/.env`(BOM 없음) → `mail_check.py` 실제 로그인. 카드 **P19 = 메일 연결 확인**(스킬 복사·팀장 연결·최근 5통 표), 교안 19쪽 끝에 같은 블록. `update.ps1` 받을 파일에 `mail/*` 7개 추가.
- **구글 커넥터**는 ChatGPT 앱에서 연결하고 Codex CLI를 최신판으로 유지한다. `env_check.ps1`은 `codex.cmd --version`과 ChatGPT 로그인을 확인하며, 필요하면 `npm.cmd install -g @openai/codex@latest`로 갱신한다.
- **모듈 6** "6번째 직원 데려오기" 카드 **P14** 신설(추가 견본 → staff6.toml, personas 칸 추가, 서버 재기동), 시연은 P14-b. 표지 "AI 직원 여덟 명"→"핵심 AI 직원 다섯 명", 흐름표·카드 색인·체크리스트 갱신.
- **검증** 임시 킷(견본 5명 + personas 5칸 + 새 엔진): `점검.py 1` 5/5 ✅, 브리지 프롬프트에 facts·inbox 주입 확인, `env_check.ps1` 파서 통과, `naver_draft.py`·엔진 py_compile 통과, 교안 HTML 태그 균형 0. 메일 스크립트: .env 없이 실행 시 한국어 한 줄 오류+종료 1, `--approved` 없이 초안+종료 2.
- 옛 8명 판은 커밋 `26cbc1e`(교안 `.bak_20260908` 은 그 전 판) 에서 꺼낼 수 있다.

## 변경 이력 (2026-09-08 오후) — 직원 견본 8명, 프리랜서 강사 기본 세트 (대표 지시)

대표 지시 "8가지 에이전트 역할을 어느 정도 만들어 두고, 프리랜서 강사 업무에 맞게 역할 설정". 수강생이 모듈 1 에서 일곱 명을 백지에서 쓰는 대신 **견본을 데려와 고치는** 길을 기본으로 했다.

- **신설** `스타터킷/직원견본/staff1.toml ~ staff8.toml` + `README.md`. 역할 — 팀장(제안서·견적·**문의 접수·회신 초안**·영업 메일, 이름 없이 부르면 받는 사람) · 교안수석(강의 설계) · 교재대리(교재·워크북·핸드아웃·평가지) · 블로그대리(후기·홍보 글) · 정산과장(강사료 장부·입금·미수금·세금 일정) · 프로필대리(**기업용 · 공공기관용 · 강의 주제 맞춤** 세 갈래, 원본 하나에 주제 태그) · 일정매니저(캘린더·이동·마감 리마인더) · **디자이너(인스타 카드뉴스·SNS 이미지·배너 시안·캡션)**. 이름은 P11 예시 표와 일치.
- **2차(같은 날) 대표 지시** "프로필대리에 기업·공공기관·강의 주제 맞춤 / 디자이너가 없다(인스타·카드뉴스)" → 처음 넣었던 **문의담당(staff8)을 디자이너로 교체**하고 문의 접수·회신은 팀장에 합쳤다. 9명으로 늘리지 않은 이유: "8명"이 표지·교안·카드·`점검.py`(8명 이상)·오피스 좌석·personas 8칸에 60곳 넘게 박혀 있고, 과정의 문의 웹앱(P23)이 이미 팀장(staff1)에 연결돼 있어 팀장이 문의를 맡는 것이 흐름과 맞다. P1·P3-직접의 8번째 일 "[문의 응대]" → "[인스타 카드뉴스]", P11 staff8 → 디자이너 🎨, `personas.py` staff8 아이콘 `:art:`.
- 파일마다 언제 부르나 / 내가 하는 일 / 시작할 때 묻는 것 / 일하는 방식 / 지어내지 않는 것 / 혼잣말. 모듈 3 팀 규칙(결과물 위치·inbox 쪽지·facts.md 먼저 읽기)과 "초안까지만, 발송·발행은 대표가"를 전부 내장. 본문 996~1,176자 — 슬랙 브리지 `AGENT_BODY_LIMIT`(1,400) 안이라 잘리지 않는다. `name` 은 staff1~8 그대로(파일·personas 키와 짝).
- **카드** `프롬프트카드.md` P3 → 견본 데려오기(staff2~8 복사 + P2 에서 만든 staff1 에 견본 팀장 내용 병합 + facts 8가지 일과 대조 표). 원래 카드는 **P3-직접**(강사가 아닌 수강생용)으로 유지. 합격 판정 문구 "✅ 4개" → "✅ 5개 전부"(2026-09-04 브랜드 색 항목 추가 뒤 낡은 숫자였음).
- **교안** 기준본 모듈 1 흐름 · P3 카드 · 담당 바꾸기 노트 · P3-직접 카드 · 합격 판정 문구.
- **README** 폴더 표에 `직원견본/` 행.
- **검증** 견본 8개를 `.codex/agents/` 에 넣은 임시 킷에서 `점검.py 1` 5개 전부 ✅, `roster.load_agents` 8명 로드 확인.
- 배포 반영은 `03_깃허브_배포_갱신.bat` 실행 시(ZIP·github/starter-kit 에 `직원견본/` 이 함께 실린다 — 제외 목록에 없음).

## 변경 이력 (2026-09-08) — 강의장 슬랙 문제 3건 (대표 지시)

수강생 캡처 4장에서 확인: ① 직원이 대표를 "김민주 강사님"으로 부름 ② 팀장만 대답 ③ 이모지·캐릭터(인사말) 안 생김.

- **원인** ① 브리지 프롬프트에 대표 이름이 없고 24시간 세션을 이어받아 옛 맥락을 끌고 옴 ② 프롬프트에 팀 명단이 없어 전부 "staff1 정체성"으로 답했고, `split_reply` 가 `personas.py` 표시 이름과 글자 그대로 일치할 때만 말풍선을 나눔(직원 파일 name·staff2·`**이름**:` 은 전부 팀장으로) ③ P11 카드가 📄 같은 유니코드 이모지를 넣게 했는데 슬랙 `icon_emoji` 는 `:page_facing_up:` 형식만 받아 조용히 무시 + 인사말 `[무엇]` 빈칸은 P11 이 안 채움.
- **엔진** `slack-server/roster.py` 신설(직원 파일·대표 이름·이모지 변환·이름 별칭). `codex_bridge.py` 프롬프트에 대표 이름·팀 명단·담당 직원 파일 본문 주입, 세션 수명 24h→3h. `server.py` 이름으로 부르기(`교안수석 불러서…`/`@`/채널), 말풍선 분리 관대화, 유니코드 이모지→슬랙 형식, 인사말 빈칸이면 description 으로 대체, `새 대화` 명령, 시작 로그에 `대표 이름:`.
- **채점** `점검.py 4` 에 6항목 추가(대표 이름·personas 문법·키 짝·표시 이름·아이콘 형식·인사말 빈칸) → 16항목.
- **카드·교안** P11 을 이름·아이콘·**인사말**까지 채우는 카드로 교체(예시 이름을 강사 팀 이름에서 P1 의 8가지 일 기준 일반 이름으로). 「직원 부르는 법」 표·🚑 3건을 카드 모듈 4 와 교안 22쪽에, 처방표(24쪽) 3행 + 업데이트 한 줄 추가.
- **기존 수강생** `github/update.ps1` 신설 — `irm https://raw.githubusercontent.com/mandoo356/wd-agentteam-kit-codex/main/update.ps1 | iex`. 엔진만 교체, 백업, `.codex_session.json` 삭제, 강사 이름 섞임 진단, `점검.py 4`, 서버 재기동.
- 미확인: "김민주" 라는 문자열은 킷·카드·교안 어디에도 없다. 수강생 PC의 옛 세션 또는 사용자 설정에서 온 것으로 보이며, `새 대화`로 세션을 초기화한다.

## 변경 이력 (2026-09-07) — 전면 개편 (대표 지시 4건)

1. **순서**: 슬랙 가입·워크스페이스·앱·열쇠 3개(교안 2~3쪽) → 스타터킷 한 줄 설치(4쪽) → 로그인(6쪽) → 구글 연결(7쪽). 옛 PART 3(슬랙 4쪽)은 「서버 켜기·첫 대화」 2쪽으로 압축.
2. **묻지 않고 설치**: `env_check.ps1` 이 Node·Git·Codex CLI 를 없으면 설치하고, **Python 은 있어도 무조건 3.14** 를 설치해 `py` 를 3.14 로 고정(`PY_PYTHON`/`PY_PYTHON3`). 꾸러미 6개를 3.14 에 넣고 import 로 실제 확인. 실행 정책(`RemoteSigned`, CurrentUser)도 자동 — 파워셸에서 `codex.cmd` 한 단어. 슬랙 열쇠 3개를 별표 입력으로 받아 `.env`(BOM 없음) 저장 → `slack_check.py` 로 실제 슬랙 확인 → 서버 첫 기동(「준비 완료」)까지. `[Y/n]` 질문 전부 제거.
3. **토큰 절감**: 카드 P0·P8-a·P9·P9-a·P10·P10-a 삭제(환경점검·`점검.py 4` 가 대신). 로그 읽히기 프롬프트는 "표로 안 잡힐 때만".
4. **캡처 1장씩**: 2열 격자·접힘 해제, 슬랙 화면은 단계마다 한 장(`.step-shot`).
5. **슬랙 세션 정리**: `codex_bridge.py`는 `.codex_session.json`의 세션 ID를 최대 3시간 재사용하고, 실패하면 새 세션으로 한 번 재시도한다. 연결 설정을 바꾼 뒤에는 서버를 재기동한다.
6. **`codex.cmd` 실행** 설명을 6쪽에 추가하고 PowerShell 실행 정책과 CMD 대안을 정리했다.

백업: `..\_backup_20260907\` (개편 전 교안·스크립트·카드).

## 수업 전 사용 순서 — 기본: 명령어 한 줄 설치 (2026-09-04, 2026-09-07 갱신)

수강생은 PowerShell 창에 아래 한 줄을 붙여넣는다. 프로그램 다운로드·압축 풀기·더블클릭이 모두 사라진다.

```powershell
irm https://raw.githubusercontent.com/mandoo356/wd-agentteam-kit-codex/main/install.ps1 | iex
```

**CMD 창밖에 안 열리면**(줄 맨 앞에 `PS` 가 없으면) 아래 한 줄을 대신 붙여넣습니다. 파워셸을 잠깐 빌려 똑같이 설치합니다.

```bat
powershell -NoProfile -ExecutionPolicy Bypass -Command "irm https://raw.githubusercontent.com/mandoo356/wd-agentteam-kit-codex/main/install.ps1 | iex"
```

1. 강사는 스타터킷을 고친 뒤 `03_깃허브_배포_갱신.bat`을 실행한다 (안전 ZIP 생성 → `github/starter-kit` 갱신 → push).
2. 수강생은 위 한 줄을 붙여넣는다. 깃허브에서 약 3MB를 받아 `C:\Agent\01_KIT\starter-kit`에 설치하고 환경점검까지 이어서 돈다. `C:\Agent\MyData\`(제안서·블로그·로고·프로필)도 같이 만들어진다 — 수강생은 여기에 제안서 3·블로그 3·로고 1을 수업 전에 넣는다.
3. 환경점검이 Node·Git·Codex CLI 를 없으면 설치하고 Python 3.14 는 무조건 설치한다(묻지 않음). 이어서 꾸러미·로그인 창·슬랙 열쇠 3개 입력·서버 첫 기동까지 진행한다. Codex CLI 버전을 읽지 못하면 `npm.cmd install -g @openai/codex@latest` 로 갱신한다. 선택으로 메일 앱 비밀번호를 받아 `mail/.env` 를 만들고 실제 로그인을 확인한다. **수강생은 열쇠 3개를 먼저 받아 두고 실행한다**(교안 2~3쪽).
4. 필수 항목을 통과한 뒤 강의 기준본 순서로 진행한다.
5. 수업 종료 전 `02_내작업_백업.bat`으로 개인 작업 ZIP을 만든다.

- 저장소: https://github.com/mandoo356/wd-agentteam-kit-codex (로컬 클론 `github/`, 과정 git에서는 제외)
- 설치 스크립트 원본: `github/install.ps1`
- 인터넷이 막힌 강의장용 예비: 아래 ZIP 방식

### 예비: ZIP 방식 (인터넷 없을 때)

1. 강사는 `01_수강생_배포본_만들기.bat`을 실행한다.
2. `distribution/`의 ZIP과 SHA-256 파일을 수강생에게 전달한다.
3. 수강생은 ZIP을 모두 푼다.
4. 풀린 폴더의 `00_폴더생성_및_설치.bat`을 더블클릭한다.
5. 설치된 `C:\Agent\01_KIT\starter-kit\환경점검.bat`을 실행한다.

## 변경 이력 (2026-09-04 오후) — 설치 폴더 이름·MyData

- 설치 루트 `C:\WD-AgentTeam` → **`C:\Agent`** (대표 지시. 처음 `C:\Agent`로 했다가 같은 날 "안전하게, 오류 적은 방향으로" 지시로 영문 확정). `install_starterkit.ps1`·`github/install.ps1`·README 2종·프롬프트카드 2종·교안 기준본 전부 교체.
- 설치 시 **`MyData\Proposal·Blog·Logo·Profile`**(제안서·블로그·로고·프로필) 4개 폴더와 `여기에_넣으세요.txt` 를 함께 만든다. 환경점검에 「내 자료 (제안서 3·블로그 3·로고 1)」 선택 항목 추가.
- 교안 표지 사전 과제·4쪽(8번 내 자료 미리 넣기)·P1-B·18쪽·28쪽 체크리스트에 반영. 카드 P1-B·P17·P18·C2 경로가 `C:/Agent/MyData/...` 로 바뀜.
- 새로 만드는 경로는 전부 영문(`C:\Agent\MyData\…`). 킷 안의 한글 파일명(점검.py·프롬프트카드.md 등)은 그대로 — 원본 스타터킷이 한글 경로에서 개발·검증돼 문제 없음.

## 정리 이력 (2026-09-04)

구형 교안 8개(`00_환경설정_WD`, `01_기획안` 2, `02_교안` 2, `06_학습자교안` 2, `07_간결판`)와
루트 중복 `flow-image-JJ/`, `withdream-instructor-profile.skill`, 무관한 태평양 제안서 PDF는 **휴지통으로 이동**했다.
복구가 필요하면 휴지통 또는 git 커밋 `512d736`(삭제 직전 스냅샷)에서 꺼낸다.
`office/node_modules` 등 오피스 프로그램은 대표 지시로 삭제하지 않는다.

남은 문서는 전부 현행이다: `교안1/`(기준본·검증보고), `04_슬랙앱_안내문` 2종 + `04-1` 상세, 위험분석·검수보고, `강의문의 자동하/`(P20~P23), `학습자가만드결과/`(예시).

## 배포 금지

- 원본 `스타터킷/` 폴더를 탐색기에서 직접 ZIP으로 만들지 않는다.
- 실제 `.env`, 로그인 상태, `환경점검_결과.html`, `node_modules`, 캐시·로그는 배포하지 않는다.
- `distribution/`에서 생성된 ZIP만 전달한다.
