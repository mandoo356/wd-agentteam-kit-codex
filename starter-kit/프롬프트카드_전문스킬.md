# 전문 스킬 카드 5장 — 강사 업무용

> 제안서(**P4**)와 교재(**P8**)는 기본 카드로 옮겼습니다. 여기는 **블로그 · 교안 · 메일 · 프로필 · 카드뉴스** 다섯 장입니다.

모듈 2에서 카드 P4로 만든 **제안서 스킬**이 이 회사의 기준선입니다.
Step 0 · 작성 원칙 · 절대 금지 · 출력 형식 · 저장 위치가 다 들어 있고, 슬라이드 덱까지 나옵니다.

**여기 다섯 장은 나머지 업무를 그 수준으로 올리는 카드입니다** — 블로그 · 교안 · 메일 · 프로필 · 카드뉴스.

> 📌 **언제 쓰나**: 모듈 2를 통과한 뒤. 시간이 남으면 강의장에서, 아니면 집에서.
> 한 장에 5~10분씩 걸립니다. 다섯 장을 다 할 필요는 없어요. **내가 제일 자주 하는 일부터** 하세요.
>
> ⚠️ 각 카드에 **"같은 일을 하는 스킬이 이미 있으면 덮어쓸 것"** 이 들어 있습니다.
> 이걸 빼고 돌리면 같은 일을 하는 스킬이 두 개가 되어, 어느 쪽이 뜰지 알 수 없게 됩니다.

---

## 먼저 — 전문 스킬은 뭐가 다른가

아마추어 스킬과 전문 스킬의 차이는 분량이 아니라 **칸이 몇 개냐**입니다.

| 칸 | 하는 일 | 이게 없으면 |
|---|---|---|
| ① **Step 0 정보 수집** | 일 시작 전에 필요한 걸 **한 번에 모아서** 묻는다 | 반쯤 만들다가 "그건 안 알려주셨는데요"를 반복 |
| ② **상황 분류** | 같은 일도 상황마다 다르다. 갈래를 나눈다 | 영업 메일과 독촉 메일이 같은 톤으로 나감 |
| ③ **작성 원칙** | 이 바닥에서 통하는 기준 | 그럴듯하지만 현장에서 안 먹히는 결과물 |
| ④ **절대 금지** | 하면 안 되는 것을 명시 | 금액·날짜를 지어냄 |
| ⑤ **출력 형식** | 결과물이 어떤 모양이어야 하는지 | 매번 모양이 달라 손보는 데 더 걸림 |
| ⑥ **저장 위치** | 어디에 어떤 이름으로 | 결과물을 못 찾음 |

**①번이 제일 중요합니다.** 프로는 일을 시작하기 전에 묻습니다.

아래 다섯 장은 전부 이 여섯 칸을 채우도록 짜여 있습니다. (기본 카드 P4·P8 도 같은 여섯 칸입니다.)

---

## S1 — 제안서 스킬  →  **기본 카드 P4로 옮겼습니다**

> 📌 **여기 없습니다.** 제안서는 `프롬프트카드.md` 의 **카드 P4** 가 이미 완성품입니다.
> Step 0 여섯 가지 확인 · 모듈 4줄 · 확인 가능한 기대효과 · 부가세 명시 ·
> **슬라이드 덱(16:9, 인쇄하면 PDF)** 까지 한 번에 나옵니다.
>
> 예전에는 P4가 "순서 몇 줄"짜리 연습이고 제대로 된 건 여기 있었는데,
> **처음부터 제대로 나오는 게 맞다**고 판단해 P4 자체를 올렸습니다.
> 제안서 스킬을 고치고 싶으면 `프롬프트카드.md` 의 P4 를 보세요.

---

## S2 — 블로그 스킬

```
.agents/skills/ 폴더에 '블로그' 스킬을 만들어줘.
아래 내용을 SKILL.md 에 그대로 담아줘.

[이 스킬이 하는 일]
강의 후기·현장 스케치를 블로그 글로 쓴다.

[독자가 누구인지 — 이게 이 스킬의 핵심]
이 글의 독자는 일반인이 아니라 **교육 담당자**다.
"재밌었다"를 읽으러 오는 게 아니라 **우리 회사도 이 교육을 할지 판단하러** 온다.
그래서 감상문이 아니라 판단 자료로 쓴다.

[Step 0 — 착수 전 확인할 것]
한 번에 모아서 묻는다. 아는 것만 알려줘도 된다고 안내한다.
  1. 고객사 (공개 가능한지도 함께)
  2. 업종
  3. 과정명
  4. 대상 (직급·직무·인원)
  5. 교육 시간
  6. ⭐ 이 교육만의 특별한 점 ← 가장 중요
  7. ⭐ 다룬 내용
  8. 교육 후 변화·반응
  9. 어떤 조직에 추천하고 싶은지

  → 6·7번이 없으면 글이 아니라 광고가 된다. 이 둘은 반드시 받는다.

[글 구조]
1. 인사 + 어떤 교육이었는지 두세 줄
2. 판단 카드 (표로) — 고객사 / 업종 / 대상 / 인원 / 시간 / 형태 / 난이도 / 진행일
3. 이 교육의 차별점 (박스로 강조)
4. 실제로 다룬 내용 — 모듈별로
5. 교육 후 변화 (구체적인 반응·수치가 있으면 그것으로)
6. 이런 조직에 추천합니다
7. 문의 안내

[작성 원칙]
- 문장은 짧게. 한 문단 3~4줄.
- 숫자가 있으면 숫자로 쓴다. "많은 분이" ✕ / "32명이" ○
- 사진 자리는 [사진: 무엇] 으로만 표시하고 이미지는 만들지 않는다.
- 마지막에 SEO 제목 후보 5개를 뽑고, 각각 왜 그 제목인지 한 줄씩 붙인다.
- 해시태그 15개 이내.

[절대 금지]
- 고객사 실명은 공개 가능 확인을 받기 전에는 쓰지 않는다. 확인 전이면 '○○기업'.
- 참가자 개인의 말을 실명과 함께 쓰지 않는다.
- 안 들은 만족도 점수·재의뢰율 같은 수치를 지어내지 않는다.
- 발행하지 않는다. 초안까지만 만들고 사람이 검토한다.

[저장]
workspace/결과물/<날짜>_<고객사>_블로그초안.md

[기존 스킬 처리 — 이 줄을 빼면 스킬이 두 개가 된다]
- 같은 일을 하는 스킬이 .agents/skills/ 에 이미 있으면 (이름이 비슷한 다른 스킬 포함)
  새로 만들지 말고 그 폴더의 SKILL.md 를 이 내용으로 통째로 덮어쓴다.
- 폴더 이름이 다르면 이 이름으로 폴더를 바꾸고, 옛 폴더는 지운다.
- 덮어쓴 뒤 .codex/agents/ 의 직원 파일에서 이 스킬을 가리키는 줄을 찾아
  새 이름과 맞는지 확인하고, 안 맞으면 고친다.
- 무엇을 지우고 무엇을 고쳤는지 마지막에 목록으로 알려준다.

[트리거]
"블로그", "후기 써줘", "현장 스케치", "포스팅"
```

**이러면 정상** — 글 + SEO 제목 5개 + 해시태그가 한 파일에 나옵니다.

---

## S3 — 교안 스킬

> ⚠️ **교안과 교재는 다른 일입니다.** 이걸 헷갈리면 스킬 두 개가 같은 걸 만듭니다.
>
> | | **교안** (S3) | **교재** (기본 카드 P8) |
> |---|---|---|
> | 답하는 질문 | 이 강의를 **어떻게 가르칠까** | 학습자 손에 **무엇을 쥐어줄까** |
> | 결과물 | 모듈 구조 · 분 단위 시간표 · 학습자 활동 · 강의 스크립트 | 인쇄용 교재 · 워크북 · 슬라이드 |
> | 순서 | **먼저** | 교안이 확정된 **다음** |

```
.agents/skills/ 폴더에 '교안' 스킬을 만들어줘.
아래 내용을 SKILL.md 에 그대로 담아줘.

[이 스킬이 하는 일]
강의를 어떻게 진행할지 설계한다. 인쇄물이 아니라 진행 설계다.

[Step 0 — 착수 전 확인할 것]
  1. 과정명
  2. 대상 (직급·직무·인원)
  3. 총 시간과 쉬는 시간 방식 (예: 4시간, 중간 10분 2회)
  4. 학습 목표 — 교육이 끝나면 학습자가 무엇을 할 수 있어야 하는가
  5. 사전 수준 (입문 / 기초 / 중급)
  6. 장소와 환경 (노트북 있는지, 조 편성 가능한지, 인터넷 되는지)

  → 4번이 없으면 설계를 시작하지 않는다. 목표 없이 짠 시간표는 못 쓴다.
  → 6번은 활동 설계를 좌우한다. 노트북이 없으면 실습을 못 넣는다.

[설계 원칙 — 성인 학습]
- 어른은 강의만 들으면 안 남는다. **경험 → 성찰 → 개념 → 적용** 순서로 배치한다.
  자기 경험을 꺼내게 한 다음 개념을 얹고, 마지막에 자기 일에 적용시킨다.
- 도입 5분이 하루를 좌우한다. 왜 이 교육을 듣는지부터 납득시킨다.
- 4시간이면 학습자 활동을 최소 2개 넣는다. 강의만 4시간은 설계 실패다.
- 한 덩어리는 50분을 넘기지 않는다.
- 마지막 15분은 반드시 '내 일에 어떻게 적용할지' 정리에 쓴다.

[반드시 만들 것]
1. 모듈 구조 — 3~5개, 각각 학습 목표 한 줄씩
2. 분 단위 시간표 — 시작~종료, 쉬는 시간 포함. 합계가 총 시간과 맞는지 검산한다
3. 학습자 활동 — 각 활동의 진행 방법·소요 시간·필요 준비물
4. 강의 스크립트 아웃라인 — 각 모듈 도입부에 무슨 말로 시작할지
5. 준비물 목록

[절대 금지]
- 시간표 합계가 총 시간과 안 맞는 채로 내놓지 않는다. 반드시 더해서 확인한다.
- 환경을 모르는 채로 실습을 넣지 않는다.
- 교재·워크북·슬라이드를 만들지 않는다. 그건 교재 스킬(기본 카드 P8)의 일이다.
  교안이 확정되면 교재 담당에게 넘긴다.

[문장 5대 수칙 — 슬라이드에 들어가는 모든 글]
① 슬라이드 제목
   [핵심 수단] + [정량 수치] 로 쓴다. 추상적 제목을 쓰지 않는다.
   ✕ "업무 효율성 극대화 방안"
   ○ "업무 처리 시간을 40% 단축하는 통합 플랫폼 구축"

② 거버닝 메시지 (제목 아래 1~2줄, 결론 리드 문구)
   [현황·문제 데이터] → [핵심 조치] → [정량적 변화] 3단으로 잇는다.
   "~하며, ~하고, ~함으로써" 처럼 접속어를 이어 붙이지 않는다. 단문으로 끊는다.
   ✕ "부서 간 장벽을 없애고 통합 체계를 구축하여 서비스 질을 도모하고자 합니다"
   ○ "분산된 11개 시스템을 통합 허브로 연계합니다. 의사결정 시간을 평균 50% 단축합니다."

③ 소제목
   구체적 조치와 바뀐 상태를 적고 반드시 명사형으로 끝낸다.
   ○ "승인 절차 3단계에서 2단계로의 전환 현황"

④ 본문 서술형
   1문장 1메시지. 두 문장을 넘지 않는다. 문장 끝에 마침표를 찍는다.
   [현황 → 원인 → 작동 방식 → 결과] 구조로 쓴다. 상황만 나열하지 않는다.

⑤ 본문 개조식
   [주체] + [구체적 행동] + [정량적 결과] 를 한 줄에 다 넣는다. 명사만 나열하지 않는다.
   개조식 항목 끝에는 마침표를 절대 찍지 않는다.
   ○ "- 발주처·현업 3자 협의체를 구성해 월 1회 정기 이슈 조율"

[쓰지 않을 단어]
  대폭 · 획기적인 · 혁신적인 · 엄청난 · 극대화 · 다양한 · 효율적인 · 최적의
  → 형용사·부사를 빼고 사실과 수치로 바꿔 쓴다.

[원문 무결성]
- 사용자가 준 자료의 수치·정량적 사실·고유명사는 요약하면서 하나도 빼지 않는다.
- 줄이는 것은 문장 길이지 데이터가 아니다.

[청중에 따라 톤을 바꾼다]
- 비전공자·학생 → 기술 용어 대신 비유. 쉬운 결론 문체.
- 임원 보고    → 이미지 최소화. 데이터·인과관계·대안 중심. 강조색은 수치에만.
- 착수 전에 청중이 누구인지 확인하고 이에 맞춘다.


[결과물 ② 인쇄물]  ← 실제로 손에 쥐어지는 것
workspace/결과물/<날짜>_<과정명>_교안.html

  판형: 한 쪽 = 가로 16:9  1440px x 810px 고정   ← 교재(A4 세로)와 반드시 다르게 한다
        강사가 화면으로 보는 문서라 가로가 맞다. 세로로 만들면 교재와 구분이 안 된다
        인쇄하면 그대로 PDF 가 되게 @page{size:1440px 810px;margin:0} 를 넣는다
        한 쪽에 내용이 넘치면 글씨를 줄이지 말고 쪽을 나눈다
        가로 폭을 살려 2단으로 배치한다 — 왼쪽은 진행 표, 오른쪽은 강사 멘트·주의
  색은 workspace/memory/facts.md 의 네 값을 CSS 맨 위 :root 에 모아두고 그 변수만 쓴다
  모든 쪽에 머리(로고 글자 + 회사명)와 꼬리(쪽번호)를 넣는다
  서체는 "Noto Sans KR" 계열. 인터넷에서 폰트·이미지를 불러오지 않는다
  자바스크립트는 쓰지 않는다
  쪽 구성: 표지 / 한눈에(목표·대상·환경) / 분 단위 시간표 / 도입 /
           모듈별 상세(모듈 수만큼) / 준비물·환경 점검 / 강사 메모(예상 질문·실패 지점)
  모듈 상세 쪽에는 반드시 [진행 순서 표] [강사 멘트] [여기서 막히면] 세 덩어리를 넣는다

  ── 화면 규칙 ──────────────────────────────────
  · 색은 facts.md 의 네 값만 쓴다. 거기 없는 색을 새로 만들지 않는다.
  · 이모지를 쓰지 않는다. 아이콘이 꼭 필요한 게 아니면 넣지 않는다.
  · 본문 영역은 1×2 · 1×3 · 2×2 카드 격자로 묶는다. 글이 제멋대로 흩어지지 않게 한다.
  · 모든 쪽 상단에 지금 어느 섹션인지 보여주는 얇은 내비게이터 줄을 넣는다.

  ── 표 규칙 ────────────────────────────────────
  · 세로 격자선을 전부 지운다. 가로 구분선만 남긴다.
  · 머리 행은 진하게 칠하지 말고 연한 회색으로 영역 구분만 한다.
  · 표 안에서 굵게+강조색을 쓰는 곳은 실제 수치·변화량뿐이다. 그게 주인공이다.
  · 셀 안 한글이 어색하게 잘리지 않도록 문장을 짧게 끊고 명사형 2~3항목으로 나눈다.

  ── 목차 규칙 ──────────────────────────────────
  · 항목을 사각형 테두리 박스로 가두지 않는다.
  · 수평 가이드라인 + 굵기 대비 + 번호 크기 차이로 정렬한다.

  ── 내보내기 전 스스로 확인 ────────────────────
  1. 준 자료의 수치·고유명사가 빠짐없이 살아 있는가
  2. 개조식 항목 끝에 마침표가 하나도 없는가
  3. 제목이 [핵심 수단 + 정량 수치] 결합으로 되어 있는가
  4. 금지 표현(대폭·획기적인·극대화 등)이 남아 있지 않은가
  5. 표에 세로 테두리선이 없는가
  6. 이모지가 없는가
  7. 시간표 합계가 총 시간과 맞는가
  → 하나라도 아니면 고친 뒤에 저장한다. 무엇을 고쳤는지 마지막에 알려준다.

[결과물 ① 내용]
workspace/결과물/<날짜>_<과정명>_교안.md

[기존 스킬 처리 — 이 줄을 빼면 스킬이 두 개가 된다]
- 같은 일을 하는 스킬이 .agents/skills/ 에 이미 있으면 (이름이 비슷한 다른 스킬 포함)
  새로 만들지 말고 그 폴더의 SKILL.md 를 이 내용으로 통째로 덮어쓴다.
- 폴더 이름이 다르면 이 이름으로 폴더를 바꾸고, 옛 폴더는 지운다.
- 덮어쓴 뒤 .codex/agents/ 의 직원 파일에서 이 스킬을 가리키는 줄을 찾아
  새 이름과 맞는지 확인하고, 안 맞으면 고친다.
- 무엇을 지우고 무엇을 고쳤는지 마지막에 목록으로 알려준다.

[트리거]
"교안", "강의 설계", "커리큘럼 설계", "시간표 짜줘", "모듈 설계"
```

**이러면 정상** — 시간표 합계가 총 시간과 맞습니다. 안 맞으면 스킬이 덜 만들어진 겁니다.

> 📐 **2026-08-15 — 문장·표 규칙이 들어갔습니다.**
> 컨설팅 펌 방식의 문장 작성 지침을 이 카드에 넣었습니다. 붙여넣는 건 똑같지만
> 결과물이 달라집니다 — **제목에 수치가 들어가고, 개조식 마침표가 사라지고,
> 표에서 세로선이 없어집니다.**
>
> ※ 원 지침에 있던 **그레이스케일 배색 · 단색 배경 강제**는 넣지 않았습니다.
> 우리는 P1 에서 회사 색 4개를 받아 쓰는 구조라 서로 부딪힙니다.
>
> **다시 만들면 예전 것과 모양이 다릅니다.** 이미 만든 교안이 있으면
> "새 규칙으로 다시 만들어줘" 하시면 됩니다.

> ⚠️ **PPTX 파일 자체는 만들지 않습니다.** 이 과정은 인터넷 없이 도는 게 원칙이라
> PPTX 를 만드는 프로그램을 따로 깔지 않습니다. 대신 **16:9 HTML** 로 만들고
> **Ctrl+P → PDF 저장**으로 씁니다. PPT 로 옮기고 싶으면 그때 화면을 그대로 붙여넣거나
> 위 "단색 배경 · 그림자 없음" 규칙 덕분에 도형으로 다시 그리기 쉽습니다.

---

## S4 — 교재 스킬  →  **기본 카드 P8로 옮겼습니다**

> 📌 **여기 없습니다.** 교재는 `프롬프트카드.md` **모듈 3의 카드 P8** 가 완성품입니다.
> 여섯 블록(학습목표·한줄정의·본문·워크시트·CHECK·핵심요약) ·
> **A4 세로 인쇄용 워크북(.html)** 까지 한 번에 나옵니다.
>
> 제안서(P4) 다음으로 많이 하는 일이라 **기본 흐름에 올렸습니다.**
> 모듈 3의 릴레이 실습이 바로 이것입니다 — 제안서 담당이 계약을 따내면 교재 담당이 워크북을 만듭니다.
>
> ※ 교재는 **교안이 없어도** 제안서만 있으면 만들 수 있게 해 두었습니다.
> 제안서에 커리큘럼과 모듈이 이미 들어 있기 때문입니다.

---

## S5 — 메일 스킬

```
.agents/skills/ 폴더에 '메일' 스킬을 만들어줘.
아래 내용을 SKILL.md 에 그대로 담아줘.

[이 스킬이 하는 일]
교육 담당자에게 보낼 업무 메일을 쓴다.

[Step 0 — 착수 전 확인할 것]
  1. 어떤 상황인지 (아래 여섯 갈래 중)
  2. 받는 사람 — 소속·직함·이름 (모르면 '담당자')
  3. 받는 곳 유형 — 대기업 / 중소기업 / 공공기관 / 교육 대행사 / 대학
  4. 이 메일에서 가장 중요하게 전할 것 1~2가지
  5. 톤 — 정중 / 표준 / 단호 (기본: 표준)

[여섯 갈래 — 상황마다 톤이 다르다]
  ① 강의 제안·첫 접촉     → 짧게. 내 얘기보다 상대 문제를 먼저
  ② 강의 확정·계약 조율   → 조건을 숫자로 못 박는다
  ③ 강의 전 사전 협의     → 확인할 항목을 목록으로
  ④ 강의 후 감사·재계약   → 감사 먼저, 제안은 맨 끝에 한 줄
  ⑤ 강사료 정산 요청      → 담담하게. 감정 빼고 사실과 날짜만
  ⑥ 일정 변경·취소        → 사과 먼저, 대안 반드시 함께

[제목 규칙]
[소속/이름] 핵심 용건 — 부가 정보
  예) [○○컨설팅/홍길동] 리더십 과정 강의 제안의 건
  예) [강사 홍길동] 10월 강의분 강사료 정산 요청
제목 후보를 2~3개 제시한다.

[호칭]
  대기업·대형기관  → 직함+님 (김○○ 팀장님)
  공공기관·지자체  → 직급을 정확히 (이○○ 주무관님, 최○○ 사무관님)
  중소기업        → 담당자님 또는 직함+님
  교육 대행사     → 매니저님 또는 실명+직함
  대학·평생교육원 → 교수님 또는 원장님

[작성 원칙]
- 본문은 화면 한 번에 들어오게. 스크롤 넘어가면 안 읽는다.
- 용건을 첫 세 줄 안에 밝힌다.
- 요청이 있으면 마지막에 '무엇을 언제까지' 한 줄로 다시 적는다.
- 첨부가 있으면 본문에 파일명을 적는다.

[절대 금지]
- 확정 안 된 날짜·금액을 단정해서 쓰지 않는다.
- 독촉 메일에도 감정 표현을 넣지 않는다. 사실과 날짜만 적는다.
- 보내지 않는다. 초안까지만 만들고 사람이 읽고 보낸다.

[저장]
workspace/결과물/<날짜>_<받는곳>_메일초안.md
제목 후보 2~3개를 파일 맨 위에 적는다.

[기존 스킬 처리 — 이 줄을 빼면 스킬이 두 개가 된다]
- 같은 일을 하는 스킬이 .agents/skills/ 에 이미 있으면 (이름이 비슷한 다른 스킬 포함)
  새로 만들지 말고 그 폴더의 SKILL.md 를 이 내용으로 통째로 덮어쓴다.
- 폴더 이름이 다르면 이 이름으로 폴더를 바꾸고, 옛 폴더는 지운다.
- 덮어쓴 뒤 .codex/agents/ 의 직원 파일에서 이 스킬을 가리키는 줄을 찾아
  새 이름과 맞는지 확인하고, 안 맞으면 고친다.
- 무엇을 지우고 무엇을 고쳤는지 마지막에 목록으로 알려준다.

[트리거]
"메일", "메일 써줘", "제안 메일", "정산 메일", "사전 협의 메일"
```

**이러면 정상** — 제목 후보 2~3개 + 본문이 나오고, **보내지는 않습니다.**

---

## S7 — 프로필 스킬  →  **완성 스킬을 그대로 설치합니다**

> 📌 **여기는 한 줄짜리 카드가 아닙니다.** 강사 개인 브랜딩 프로필을
> **HTML 슬라이드(1920×1080, 8종 구조)** 로 만드는 스킬 자체가
> `프로필-스킬/` 폴더에 완성돼 있습니다. S2·S3·S5처럼 프롬프트 하나로
> 만들지 않고, **폴더째 설치**합니다 — CSS와 QA 검사 스크립트가 따로 있어서입니다.

```
xcopy /E /I "프로필-스킬" "$env:USERPROFILE\.agents\skills\프로필"
```

Codex CLI를 재시작하면 "강사 프로필 만들어줘"에 자동으로 켜집니다. 색은
**여러분이 P1에서 만든 `facts.md` 4색**을 그대로 씁니다 — 새 색을 지어내지 않습니다.

```
[홍길동] 강사 프로필 덱 만들어줘.

강의 경력: 현) 길동컨설팅 대표 강사
자격: CS강사 1급, TA 교류분석 …
기업 교육 레퍼런스: …
캐치프레이즈: …
이 강사를 선택해야 하는 이유 3가지: …
연락처: 이메일, 휴대폰
```

빠진 항목이 있으면 직원이 **한 번에 모아서** 물어봅니다. 사진·언론보도·후기가
없으면 그 슬라이드를 빼고 진행합니다 — 없는 걸 지어내지 않습니다.

**QA 게이트** — 완성 선언 전에 자동으로 돕니다. 걸린 게 있으면 직접 확인:

```
py -3 .agents/skills/프로필/scripts/validate.py workspace/결과물/파일명.html
py -3 .agents/skills/프로필/scripts/qa_screenshot.py workspace/결과물/파일명.html
```

`validate.py`는 슬라이드 순서·규격·**내 facts.md 색인지**·잔존 자리표시자를 봅니다.
`qa_screenshot.py`는 슬라이드를 전부 캡처해 `workspace/결과물/qa_profile/` 에 저장합니다 —
글자가 슬라이드 밖으로 넘쳤는지는 마지막에 사람 눈으로 확인하세요.

자세한 설치·문제해결은 `프로필-스킬/README.md` 참고.

> 📌 **담당은 staff6(프로필대리)입니다.** 스킬 연결은 아래 "다섯 장을 다 만들었다면"에서 함께 합니다.

---

## S8 — 카드뉴스 문안 스킬

> 📌 **문안(글)까지만 만듭니다. 이미지는 오늘 만들지 않습니다.**
> 실제 이미지 카드뉴스는 별도 이미지 생성 스킬이 필요해 오늘 범위 밖입니다.
> 블로그 스킬(S2)이 원고까지만 만들고 발행은 안 하는 것과 같은 이유입니다 —
> 오늘은 사람이 승인할 수 있는 "문안 초안"까지가 목표입니다.

```
.agents/skills/ 폴더에 '카드뉴스' 스킬을 만들어줘.
아래 내용을 SKILL.md 에 그대로 담아줘.

[이 스킬이 하는 일]
인스타그램 카드뉴스(여러 장 슬라이드) 문안을 쓴다. 이미지는 만들지 않고 글만 쓴다.

[Step 0 — 착수 전 확인할 것]
  1. 주제 (교육 후기 / 업계 인사이트 / 우리 회사 소식 중 무엇인지)
  2. 슬라이드 장수 (기본 8~11장)
  3. 톤 (정보 전달형 / 공감형 / 후기형)
  4. 이 카드뉴스로 하고 싶은 말 한 줄 (핵심 메시지)
  5. 참고할 원고나 자료가 있는지 (블로그 원고·제안서 등)

  → 4번이 없으면 슬라이드마다 다른 얘기를 하게 된다. 반드시 받는다.

[슬라이드 구성 — 기본 8장]
1. 표지 — 궁금증을 부르는 한 줄 제목
2. 문제 제기 — 독자가 겪는 상황
3~6. 핵심 내용 — 슬라이드 하나에 메시지 하나만
7. 요약 — 3줄 이내
8. 마무리 — 문의·팔로우 유도 (CTA)

[작성 원칙]
- 슬라이드 한 장에 문장 2~3개 이내. 길면 다음 장으로 나눈다.
- 숫자·사례가 있으면 반드시 앞세운다. 추상적 조언으로 채우지 않는다.
- 슬라이드마다 [이미지 설명: 무엇] 을 한 줄씩 붙인다. 이미지는 만들지 않고 설명만 남긴다.
- 마지막에 캡션(본문 설명글)과 해시태그 15개 이내를 함께 만든다.

[절대 금지]
- 안 들은 만족도·수치를 지어내지 않는다.
- 고객사 실명은 공개 가능 확인 전에는 '○○기업'으로 둔다.
- 발행하지 않는다. 문안까지만 만들고 사람이 검토한다.

[저장]
workspace/결과물/<날짜>_<주제>_카드뉴스문안.md

[기존 스킬 처리 — 이 줄을 빼면 스킬이 두 개가 된다]
- 같은 일을 하는 스킬이 .agents/skills/ 에 이미 있으면 새로 만들지 말고 덮어쓴다.
- 덮어쓴 뒤 .codex/agents/ 의 직원 파일에서 이 스킬을 가리키는 줄을 찾아 맞는지 확인한다.

[트리거]
"카드뉴스", "인스타 카드", "카드뉴스 문안"
```

**이러면 정상** — 슬라이드별 문안 + 이미지 설명 + 캡션 + 해시태그가 한 파일에 나옵니다.
담당은 **staff7(디자이너)**입니다.

---

## 다섯 장을 다 만들었다면

직원들과 연결해 주세요.

```
방금 만든 스킬들을 담당 직원과 연결해줘.

  제안서 스킬     → [staff1]
  블로그 스킬     → [staff4]
  교안 스킬       → [staff2]
  교재 스킬       → [staff3]
  메일 스킬       → [staff5]
  프로필 스킬     → [staff6]
  카드뉴스 스킬   → [staff7]

- 각 직원 파일의 developer_instructions 안에 "이 일을 할 때는 [스킬이름] 스킬을 열고 시작한다"를 넣을 것
- name과 description은 건드리지 말고 TOML 문법을 깨뜨리지 말 것
- 담당이 아닌 직원 파일은 건드리지 말 것
```

> 📌 **staff8(귀요미매니저)은 스킬이 아니라 연결카드 C1(구글 캘린더 연결)로 일합니다.**
> `.agents/skills/`가 아니라 chatgpt.com 커넥터로 붙는 일이라 여기 표에는 없습니다.
> `프롬프트카드_연결.md`의 C1을 하면 staff8이 캘린더를 씁니다.

---

## 내 업종으로 바꾸려면

강사가 아니시면 위 다섯 장의 **내용만** 바꾸고 **여섯 칸 구조는 그대로** 두세요.

| 강사 | 유튜버 | 디자이너 | 세무사 |
|---|---|---|---|
| 제안서 | 기획안 | 견적·제안 | 상담 회신 |
| 블로그 | 섬네일·제목 | 포트폴리오 글 | 블로그 |
| 교안 | 대본 구성 | 시안 설계 | 신고 절차 정리 |
| 교재 | 자막·설명란 | 최종 납품물 | 고객 안내문 |
| 메일 | 협업 메일 | 클라이언트 메일 | 고객 안내 메일 |

바꿀 때 **Step 0 항목이 제일 중요합니다.** "이 일을 시작하기 전에 내가 뭘 알아야 하는가" —
그 목록이 곧 그 업종의 전문성입니다.

---

## S6 — 제안서 슬라이드 덱  →  **기본 카드 P4에 포함되었습니다**

> 📌 **따로 만들지 않습니다.** 카드 P4 가 내용 초안(`.md`)과 슬라이드 덱(`.html`)을
> **한 번에 두 개** 만듭니다. 덱 판형·장 구성·인쇄 PDF 설정이 전부 P4 안에 들어 있습니다.
>
> 덱만 다시 만들고 싶으면 이렇게 말하면 됩니다 —
> `아까 만든 [고객사] 제안서 초안을 덱으로 다시 만들어줘`
