# 나의 AI 회사 만들기 — 스타터킷

개정일: 2026-09-07

변경 이력 — HO_20260907_010256: 슬랙 열쇠를 먼저 준비하고 설치·환경점검을 질문 없이 이어갑니다. Python 3.14 절대경로 실행, 패키지 실제 import, 열쇠 3개 가림 입력·실제 API 점검·서버 연결 시험을 반영했습니다. P0·슬랙 관문 카드는 자동 점검으로 대체했습니다. Codex 계정의 도구는 실제 조회로 확인합니다.

변경 이력 — 2026-09-07: 설치·환경점검이 누락된 `.codex/agents`·`.agents/skills`·작업 폴더를 복구합니다. 설치 시 `slack-server/logs`도 복구하며 기존 파일 내용은 보존합니다. 필수 파일 누락·폴더 생성 실패는 환경점검에서 안내합니다. 교안 캡처 20에는 Verification Token 값을 가린 화면을 반영했습니다.

변경 이력 — 2026-09-05: 핸드오프 HO_20260905_224521의 슬랙 OWNER_USER_ID 필수화·멤버 ID 찾기와 manifest 표시 이름 AI 규칙을 반영했다. HO_20260905_230011에서 모든 실행 모드의 OWNER_USER_ID 필수 검사와 별도 자가진단 항목을 반영했다. 설치·로그인·프로젝트 신뢰·훅 신뢰 절차는 아래 안내를 따른다.

이 폴더가 여러분의 회사가 될 자리입니다. 지금은 뼈대만 있고, 오늘 하루 동안 내용을 채웁니다.

---

## 기본 설치 — PowerShell 또는 CMD 한 줄

먼저 교안 2~3쪽대로 개인 슬랙 앱을 만들고 **봇 토큰·앱 토큰·내 멤버 ID**를 준비하세요. 그다음 4쪽의 설치 명령을 실행합니다.

PowerShell:
```powershell
irm https://raw.githubusercontent.com/mandoo356/wd-agentteam-kit-codex/main/install.ps1 | iex
```
CMD(명령 프롬프트):
```bat
powershell -NoProfile -ExecutionPolicy Bypass -Command "irm https://raw.githubusercontent.com/mandoo356/wd-agentteam-kit-codex/main/install.ps1 | iex"
```
Node.js 설치 후 Codex만 설치할 때는 두 창 모두 `npm.cmd install -g @openai/codex`를 사용합니다. 로그인은 `codex.cmd login`입니다.
PowerShell 자체가 기관 정책으로 차단되면 CMD 한 줄도 작동하지 않습니다. 별책의 수동 설치 절차를 참고하고, 훅을 포함한 전 과정 실습은 PowerShell을 사용할 수 있는 Windows 노트북에서 진행합니다.

별책: `05_검은창_매뉴얼_CMD와PowerShell_WD.html` (배포본에서는 `docs/`)

## 예비 — 받은 ZIP 설치

1. 강사가 준 이름에 **Codex**가 들어간 ZIP을 선택해 **모두 압축 풀기**를 누릅니다.
2. 풀린 폴더의 **`00_폴더생성_및_설치.bat`**을 더블클릭합니다.
3. `C:\Agent\01_KIT\starter-kit`에 설치되고 환경점검이 이어집니다.
   같이 만들어지는 **`C:\Agent\MyData\`**에 제안서 3개·블로그 글 3개·회사 로고를 수업 전에 넣어 두세요 (아래).
4. 프로그램 설치·환경점검은 확인 질문 없이 이어집니다. 로그인과 Windows 권한 창은 사람이 처리합니다. 기존 사용자 작업·비밀키는 보존하고 실행 파일은 최신 Codex판으로 갱신합니다.

---

## ZIP이 만드는 폴더

1. ZIP 파일에서 **모두 압축 풀기**를 먼저 합니다.
2. 풀린 스타터킷의 **`00_폴더생성_및_설치.bat`**을 더블클릭합니다.
3. 프로그램이 아래 표준 위치를 만들고 가벼운 스타터킷을 복사합니다.

```text
C:\Agent\01_KIT\starter-kit
```

이동을 오래 걸리게 하는 `office/node_modules`와 캐시·로그·비밀키는 복사하지 않습니다.
기존 설치가 있으면 사용자 작업·비밀키는 보존하고 실행 파일을 최신 Codex판으로 갱신하며, 설치 후 탐색기와 환경점검을 이어서 열어줍니다.

---

## 설치된 폴더에서 시작 전에 딱 하나만

이 폴더의 **`환경점검.bat` 을 더블클릭**하세요. 검은 창이 열리고 알아서 돕니다.

- Node.js 22 이상·Git이 없으면 설치하고, Node.js를 확인한 뒤 Codex CLI를 `npm.cmd`로 설치합니다. 설치 확인 질문은 없습니다
- Python 3.14 설치를 진행하고 해당 실행 파일의 **절대경로**로 버전과 패키지 import를 확인합니다
- `codex.cmd login`으로 ChatGPT 로그인 창을 엽니다. 로그인 후 프로젝트와 `/hooks`의 5개 이벤트를 검토·신뢰하고 환경점검을 다시 확인합니다
- 슬랙 열쇠 3개를 가림 입력으로 받아 `.env`에 BOM 없이 저장합니다. 봇·앱 토큰은 실제 API로, 멤버 ID는 형식으로 확인합니다
- 서버를 잠깐 실행해 실제 소켓 연결을 확인한 뒤, **검은 창 없는 자동 시작**을 등록합니다. Windows 로그인 때 자동으로 켜지고 비정상 종료되면 다시 시작합니다
- `codex.cmd`·`npm.cmd`를 쓰므로 사용자 실행 정책을 영구 변경하지 않습니다
- 끝나면 체크리스트가 검은 창과 브라우저(`환경점검_결과.html`)에 뜹니다

✅ 가 필수 항목에 전부 켜지면 준비 끝입니다. ❌ 가 있으면 그 줄에 뭘 해야 하는지 적혀 있어요.
열쇠를 아직 준비하지 못했으면 입력 중 `s`로 건너뛰고, 준비한 뒤 `환경점검.bat`을 다시 실행하세요. 슬랙 검사는 그때 통과해야 합니다.

로그인을 마친 뒤에는 **`01_Codex_YOLO_시작.bat`**을 더블클릭하세요. 현재 스타터킷 폴더에서 Codex가 YOLO 모드로 시작됩니다. Slack 서버는 별도 창 없이 백그라운드에서 유지됩니다.

기존 설치를 자동 시작 방식으로 바꿀 때는 PowerShell에 아래 한 줄만 붙여넣습니다.

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "C:\Agent\01_KIT\starter-kit\slack-server\install_autostart.ps1"
```

`WD_AUTOSTART_OK`가 나오면 그 설치 창은 닫아도 됩니다. 이후에는 Windows 로그인 후 인터넷이 연결되면 Slack과 Codex 직원이 자동으로 연결됩니다.

> 파이썬이 없어도 돌아가게 PowerShell 로 만들어져 있습니다.
> "Windows 의 PC 보호" 창이 뜨면 「추가 정보 → 실행」. 인터넷이 켜져 있어야 합니다.

당일 출발선 확인은 설치된 Python 3.14로 실행합니다. 폴더 안 빈 공간에서
**Shift + 오른쪽 클릭 → "여기에 PowerShell 창 열기"** 후:

```
& "$env:LOCALAPPDATA\Programs\Python\Python314\python.exe" 점검.py 0
```

> 붙여넣기는 **마우스 오른쪽 클릭**입니다. Ctrl+V 는 안 될 때가 있어요.

---

## 내 자료 미리 넣기 — `C:\Agent\MyData\`

설치가 이 폴더를 만들어 둡니다. 모듈 3.5(내 자료)에서 직원이 **내 문체·구성**을 여기서 배웁니다.
**수업 전날까지** 넣어 두면 당일이 편합니다. 없는 칸은 비워 두어도 수업은 진행됩니다.

| 폴더 | 넣을 것 | 개수 |
|---|---|---|
| `MyData\Proposal\` (제안서) | 내가 실제로 보낸 제안서 (pptx·pdf·docx·hwp) | **3개** |
| `MyData\Blog\` (블로그) | 내가 쓴 블로그 글 (txt·docx·pdf, 또는 글 주소를 적은 txt) | **3개** |
| `MyData\Logo\` (로고) | 회사 로고 (png·jpg·svg). 명함·홈페이지 캡처도 됩니다 | **1개** |
| `MyData\Profile\` (프로필) | (선택) 강사 프로필·이력서 | 3개 |

폴더 이름이 영문인 것은 프로그램이 한글 경로에서 막히는 일을 없애기 위해서입니다. 폴더 안의 `여기에_넣으세요.txt`에 같은 내용이 적혀 있습니다. 환경점검 「선택」 항목에 몇 개 들어갔는지 표시됩니다.

---

## 폴더가 뭐가 뭔지

| 폴더 | 여기에 뭐가 들어가나 | 내가 할 일 |
|---|---|---|
| `.codex/agents/` | 직원들. 한 명당 파일 하나 | **모듈 1에서 만듭니다** |
| `.agents/skills/` | 일하는 방법 설명서 | **모듈 2에서 만듭니다** |
| `workspace/inbox/` | 직원끼리 넘기는 쪽지함 | 모듈 3에서 채워집니다 |
| `workspace/memory/` | 팀 전체가 아는 규약 | **모듈 3에서 씁니다** |
| `workspace/결과물/` | 실제로 만들어진 것들 | 모듈 2부터 저절로 쌓입니다 |
| `slack-server/` | 슬랙에서 부르면 깨워주는 프로그램 | 프로그램은 이미 들어 있습니다. **전날 열쇠 준비·환경점검 시 숨김 자동 시작까지 등록** |
| `office/` | 가상 오피스 화면 | 프로그램은 이미 들어 있습니다. **모듈 5에서 `company.config.ts` 만** |
| `flow-image-JJ/` | 구글 플로우로 이미지 대량 생성하는 스킬 | 모듈 3.5에서 `.agents/skills/` 로 복사 + 최초 1회 로그인 |
| `naver-blog/` | 네이버 블로그 임시저장 자동화 | 프로그램은 이미 들어 있습니다. **모듈 3.5에서 `--blog-id` 만** |
| `프로필-스킬/` | 강사 프로필 덱(HTML 슬라이드) 완성 스킬 | `.agents/skills/프로필` 로 복사 (전문 스킬 S7) |

> `slack-server` 와 `office` 는 **완성된 프로그램이 이미 들어 있습니다.**
> 여러분이 만드는 건 직원(`.codex/agents/`)과 스킬(`.agents/skills/`)이고,
> 저 둘은 그걸 **불러 쓰고 보여주는** 도구입니다. 설정 한 줄씩만 채우면 됩니다.

---

## 카드가 세 벌 있습니다

| 카드 | 언제 | 몇 장 |
|---|---|---|
| `프롬프트카드.md` | **오늘 강의장에서** — 모듈 1~6 순서대로 | 기본 P1~P14 + 연결·확장 P15~P23 (설치·슬랙 관문은 자동 점검) |
| `프롬프트카드_연결.md` | 모듈 3.5 이후. 시간 남으면 강의장, 아니면 집 | 4장 (C1~C4) |
| `프롬프트카드_전문스킬.md` | 집에 가서, 내가 자주 하는 일부터 | 5장 (블로그·교안·메일·프로필·카드뉴스) |

**`프롬프트카드_연결.md` 를 빠뜨리지 마세요.** 모듈 6까지 하면 직원이 슬랙에서 대답은 하는데
**내 일정도 내 자료도 모릅니다.** 그 상태로 집에 가면 "만들긴 했는데 안 쓰게 되는" 자리에서 멈춥니다.
캘린더를 붙이고(C1) 내 제안서 폴더를 열어주면(C2~C4) 그때부터 실제로 씁니다.

---

## 합격 판정 받는 법

모듈이 끝날 때마다 검은 창에서:

```
& "$env:LOCALAPPDATA\Programs\Python\Python314\python.exe" 점검.py 1     ← 모듈 1이 끝났으면
& "$env:LOCALAPPDATA\Programs\Python\Python314\python.exe" 점검.py 2     ← 모듈 2가 끝났으면
```

✅ 가 전부 켜지면 다음으로 넘어갑니다. ❌ 가 있으면 **그 줄만** 고치면 돼요.
위 명령은 PowerShell용입니다. 실행 파일이 없으면 `환경점검.bat`을 다시 실행해 Python 3.14 설치 결과를 확인하세요.

**`점검.py` 는 고치지 마세요.** 여러분이 만든 게 규격에 맞는지 보는 채점표입니다.

---

## 끝나고 내 회사 가져가기

스타터킷 폴더의 **`02_내작업_백업.bat`**을 더블클릭하세요.

- 내가 만든 `workspace`, 직원, 스킬, 오피스 회사 설정만 ZIP으로 묶습니다.
- `.env`, 슬랙 열쇠, 로그인 상태, `node_modules`는 넣지 않습니다.
- 자동 설치한 경우 `C:\Agent\04_LEARNER_BACKUP`에 저장됩니다.
- 만들어진 ZIP을 개인 USB나 클라우드에 보관하세요.

새 PC에서 복원할 때는 깨끗한 스타터킷을 먼저 설치하고 슬랙 서버와 오피스를 끈 뒤,
백업 ZIP의 `workspace`, `.codex`, `.agents`, `AGENTS.md`, `office`를 설치된 스타터킷에 덮어씁니다.
슬랙 `.env`와 구글·네이버 로그인은 보안상 백업되지 않으므로 새 PC에서 다시 설정합니다.

---

## 이름 끝에 `.txt` 가 붙은 파일

`_예시_직원.toml.txt`처럼 `.txt`로 끝나는 건 **보라고 넣어둔 견본**입니다.
채점에는 안 들어가니 열어보고 참고만 하세요. 여러분이 만드는 진짜 파일은 `.txt` 없이 만듭니다.

---

## 자동 기록·저장·위험 작업 확인 — Codex판

1. 설치 후 `01_Codex_YOLO_시작.bat`에서 프로젝트를 신뢰하고 `/hooks`를 엽니다. 새로 추가·변경된 5개 이벤트를 검토하고 신뢰합니다. 업데이트 뒤에도 다시 확인합니다.
2. `workspace/기록/작업기록_날짜.md`에 **Codex 훅 실행 확인**이 남는지 확인합니다. 파일 존재 점검만으로 작동을 보증하지 않습니다.
3. 요청 접수·도구 종료·변경 파일·차단 이유가 한글로 기록됩니다. 요청 원문과 명령 인수는 비밀키 노출을 줄이기 위해 기록하지 않습니다.
4. `git init` 후 답변 종료 시 결과물·직원·스킬·팀 규약 변경을 자동 커밋합니다. 변경이 없거나 사용자가 이미 스테이징한 파일이 있으면 생략합니다. 기록 파일만으로 커밋을 만들지 않습니다. 전체 회사 보관은 `02_내작업_백업.bat`을 사용합니다.
5. 삭제·이동·되돌리기·셸 덮어쓰기·보호 파일 편집은 훅이 **차단**합니다. Codex의 PreToolUse 훅은 `ask` 확인창을 지원하지 않습니다. 대상을 확인하고 백업한 뒤 사람이 별도 터미널에서 직접 명령을 실행합니다. 슬랙에서는 터미널로 안내합니다.
6. 일반 문서는 `apply_patch`로 수정하고, 최초 직원·스킬·규약 파일 생성은 허용합니다. 보호 대상은 `.codex/`, `.agents/`, `AGENTS.md`, `workspace/memory/`, 회사 설정·열쇠입니다.
7. 훅은 보조 안전장치입니다. 임의 프로그램·모든 외부 도구를 완벽히 통제하지 않으며, YOLO는 운영체제 샌드박스를 해제합니다. 훅을 신뢰하지 않았거나 꺼 두면 보호·기록이 작동하지 않습니다.

설치 위치를 옮겼으면 `powershell -NoProfile -ExecutionPolicy Bypass -File configure_hooks.ps1`을 실행한 뒤 `/hooks`에서 다시 신뢰하세요. 자동화는 이미 설치하는 **Node.js 22 이상**으로 실행하여 Python 실행기 버전 충돌을 줄였습니다.

확인 명령: `git log --oneline -10`. 복원은 `git diff`로 변경을 살펴보고 백업한 뒤 `git restore --source=커밋번호 -- 파일경로`를 사람이 직접 실행합니다.

공식 근거: https://learn.chatgpt.com/docs/hooks

## 막혔을 때

| 증상 | 이렇게 |
|---|---|
| `py` 를 모르는 명령어래요 | Python 이 안 깔린 겁니다. 검은 창을 새로 열어보고, 그래도 안 되면 손 드세요 |
| 한글이 깨져 보여요 | 검은 창에 `chcp 65001` 치고 다시 실행 |
| AI가 엉뚱한 파일을 고쳤어요 | `git checkout .` 로 되돌립니다 (모듈 0에서 한 번 연습합니다) |
| ❌ 가 안 없어져요 | 카드를 한 번 더 그대로 붙여넣으세요. 덮어씁니다 |

---

**위드드림컨설팅** 교육용 스타터킷 · v0.1
