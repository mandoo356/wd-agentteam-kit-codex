# 나의 AI 회사 만들기 — 스타터킷

개정일: 2026-09-18 — 저녁 8시 자동 블로그, AI 삽화 제거, 로그 배포 제외 반영

이 폴더가 여러분의 회사가 될 자리입니다. 지금은 뼈대만 있고, 오늘 하루 동안 내용을 채웁니다.

카드 P20-c까지 마치면 `저녁블로그/`가 오늘 끝난 강의를 확인해 블로그 원고를 만들고 네이버 임시저장까지만 처리합니다. 공개 발행은 사람이 합니다.

---

## 가장 쉬운 길 — 명령어 한 줄 설치

시작 메뉴에서 **PowerShell** 을 열고(관리자 권한 필요 없음) 아래 한 줄을 붙여넣고 Enter.
붙여넣기는 **마우스 오른쪽 클릭**입니다.

```powershell
irm https://raw.githubusercontent.com/mandoo356/wd-agentteam-kit-codex/main/install.ps1 | iex
```

**CMD 창밖에 안 열리면**(줄 맨 앞에 `PS` 가 없으면) 아래 한 줄을 대신 붙여넣습니다. 파워셸을 잠깐 빌려 똑같이 설치합니다.

```bat
powershell -NoProfile -ExecutionPolicy Bypass -Command "irm https://raw.githubusercontent.com/mandoo356/wd-agentteam-kit-codex/main/install.ps1 | iex"
```

이 한 줄이 스타터킷을 받아 `C:\Agent\01_KIT\starter-kit` 에 설치하고, 환경점검까지 이어서 돌립니다.
같이 만들어지는 **`C:\Agent\MyData\`** 에 제안서 3개·블로그 글 3개·회사 로고를 수업 전에 넣어 두세요 (아래).
프로그램(Node·Git·Codex CLI)은 없으면 **묻지 않고** 명령어로 깔리고, **Python 은 있어도 무조건 3.14** 가 깔립니다(낮은 버전은 그 자리에서 올라갑니다).
파이썬 꾸러미 설치·확인, 슬랙 열쇠 3개 입력·확인, 서버 첫 기동까지 이어서 합니다. 사람이 할 일은 **로그인과 열쇠 붙여넣기**뿐입니다.
그래서 **슬랙 열쇠 3개(xoxb-·xapp-·멤버 ID)를 먼저 받아 두고** 이 한 줄을 실행하세요 (교안 2~3쪽). 인터넷이 켜져 있어야 합니다.

---

## 예비: ZIP으로 받았다면 — 폴더부터 자동으로 만들기

1. ZIP 파일에서 **모두 압축 풀기**를 먼저 합니다.
2. 풀린 스타터킷의 **`00_폴더생성_및_설치.bat`**을 더블클릭합니다.
3. 프로그램이 아래 표준 위치를 만들고 가벼운 스타터킷을 복사합니다.

```text
C:\Agent\01_KIT\starter-kit
```

이동을 오래 걸리게 하는 `office/node_modules`와 캐시·로그·비밀키는 복사하지 않습니다.
기존 설치가 있으면 `workspace`·회사 설정·비밀값·로그인 상태는 보존하고 실행 파일만 최신 Codex판으로 갱신합니다. 구형 실행 파일은 `04_LEARNER_BACKUP`에 옮긴 뒤 환경점검을 이어서 실행합니다.

---

## 시작 전에 — PC 절전부터 끄세요

직원들이 일하는 서버는 내 PC 안에서 돕니다. **PC가 절전으로 넘어가면 직원도 같이 잠듭니다.**
검은 창을 켜 둬도 슬랙에 답하지 못합니다.

**설정 → 시스템 → 전원 및 배터리 → 절전 모드로 전환**을 **안 함**으로 바꿔 주세요.
검은 창에서 한 줄로 바꾸려면:

```powershell
powercfg /change standby-timeout-ac 0
```

- 화면 끄기는 그대로 두셔도 됩니다. 모니터만 꺼지고 서버는 계속 실행됩니다.
- 노트북 덮개를 닫아도 절전될 수 있습니다. **제어판 → 전원 옵션 → 덮개를 닫을 때 수행할 작업**을 **아무 것도 안 함**으로 바꿉니다.
- `py 점검.py 4`의 `PC가 절전으로 안 넘어간다` 항목에서 확인할 수 있습니다.

---

## 설치된 폴더에서 시작 전에 딱 하나만

이 폴더의 **`환경점검.bat` 을 더블클릭**하세요. 검은 창이 열리고 알아서 돕니다.

- 파워셸 스크립트 실행을 허용합니다 → 어느 창에서든 `codex.cmd` 한 단어로 부를 수 있습니다
- Node·Git·Codex CLI 가 없으면 **묻지 않고** 설치합니다. **Python 은 무조건 3.14** 를 설치하고 `py` 를 3.14 로 고정합니다
- 파이썬 꾸러미 6개를 설치하고 **실제로 불러와지는지** 확인합니다
- Codex CLI 로그인 창을 띄웁니다 — 로그인은 본인이 직접
- 슬랙 열쇠 3개를 붙여넣게 하고(별표로 가림) `.env` 에 저장한 뒤, **실제로 슬랙에 통하는지** 물어보고 **서버를 한 번 켜서 "준비 완료"** 까지 봅니다
- 끝나면 체크리스트가 검은 창과 브라우저(`환경점검_결과.html`)에 뜹니다

✅ 가 필수 항목에 전부 켜지면 준비 끝입니다. ❌ 가 있으면 그 줄에 뭘 해야 하는지 적혀 있어요.
슬랙 열쇠를 아직 못 받았으면 `s` 를 치고 건너뛴 뒤, 받아서 `환경점검.bat` 을 다시 실행하면 그 자리부터 다시 묻습니다.

> 파이썬이 없어도 돌아가게 PowerShell 로 만들어져 있습니다.
> "Windows 의 PC 보호" 창이 뜨면 「추가 정보 → 실행」. 인터넷이 켜져 있어야 합니다.

옛 방식(파이썬이 이미 있을 때)도 그대로 됩니다 — 폴더 안 빈 공간에서
**Shift + 오른쪽 클릭 → "여기에 PowerShell 창 열기"** 후:

```
py -3 점검.py 0
```

> 붙여넣기는 **마우스 오른쪽 클릭**입니다. Ctrl+V 는 안 될 때가 있어요.

---

## 내 자료 미리 넣기 — `C:\Agent\MyData\`

설치가 이 폴더를 만들어 둡니다. 모듈 3.5(내 자료)에서 직원이 **내 문체·구성**을 여기서 배웁니다.
**수업 전날까지** 넣어 두면 당일이 편합니다. 없는 칸은 비워 두어도 수업은 진행됩니다.

| 폴더 | 넣을 것 | 개수 |
|---|---|---|
| `MyData\Proposal\` (제안서) | 내가 실제로 보낸 제안서 (pptx·pdf·docx·hwp) | **3개** |
| `MyData\Blog\` (블로그) | 내가 쓴 블로그 글 (txt·docx·pdf, 또는 글 주소를 적은 txt) | **3개** |
| `MyData\Logo\` (로고) | 회사 로고 (png·jpg·svg). 명함·홈페이지 캡처도 됩니다 | **1개** |
| `MyData\Profile\` (프로필) | (선택) 강사 프로필·이력서 | 3개 |
| `MyData\Skill\` (내 스킬) | (선택) 이미 쓰시던 스킬 문서 `SKILL.md` | 있는 만큼 |

`Skill` 폴더는 이미 Codex를 써 오신 분만 채우면 됩니다. 모듈 3.5에서 이 스킬을 정본으로 설치하고 수업에서 만든 같은 용도의 스킬은 `_보관`으로 옮깁니다. 처음이시면 비워 두세요.

폴더 이름이 영문인 것은 프로그램이 한글 경로에서 막히는 일을 없애기 위해서입니다. 폴더 안의 `여기에_넣으세요.txt` 에 같은 내용이 적혀 있습니다. 환경점검 「선택」 항목에 몇 개 들어갔는지 표시됩니다.

---

## 폴더가 뭐가 뭔지

| 폴더 | 여기에 뭐가 들어가나 | 내가 할 일 |
|---|---|---|
| `.codex/agents/` | 직원들. 한 명당 파일 하나 | **모듈 1에서 만듭니다** (`직원견본/` 5명을 데려오는 게 기본) |
| `.agents/skills/` | 일하는 방법 설명서 | **모듈 2에서 만듭니다** |
| `workspace/inbox/` | 직원끼리 넘기는 쪽지함 | 모듈 3에서 채워집니다 |
| `workspace/memory/` | 팀 전체가 아는 규약 | **모듈 3에서 씁니다** |
| `workspace/결과물/` | 실제로 만들어진 것들 | 모듈 2부터 저절로 쌓입니다 |
| `workspace/받은파일/` | **슬랙으로 보낸 사진·PDF·PPT 가 날짜별로 저장됩니다** | 저절로 쌓입니다. 직원에게 "방금 보낸 파일 읽어줘" |
| `workspace/기록/` | 직원들이 한 일을 한 줄씩 적는 업무일지 | 저절로 쌓입니다 |
| `slack-server/` | 슬랙에서 부르면 깨워주는 프로그램 | 프로그램은 이미 들어 있습니다. **모듈 4에서 `.env` 에 열쇠만** |
| `office/` | 가상 오피스 화면 | 프로그램은 이미 들어 있습니다. **모듈 5에서 `company.config.ts` 만** |
| `naver-blog/` | 네이버 블로그 임시저장 자동화 (글만, 이미지는 안 넣음) | 프로그램은 이미 들어 있습니다. **모듈 3.5에서 `--blog-id` 만** |
| `mail/` | 네이버·다음·지메일 문의 메일 읽기·회신 초안 (IMAP + 앱 비밀번호) | 환경점검이 `mail/.env` 를 만듭니다. **카드 P19** 로 팀장에게 연결 |
| `프로필-스킬/` | 강사 프로필 덱(HTML 슬라이드) 완성 스킬 | `.agents/skills/프로필` 로 복사 (전문 스킬 S7) |
| `직원견본/` | **프리랜서 강사용 핵심 직원 5명 견본** — 팀장·교재대리·블로그대리·일정매니저·프로필대리 + `추가/` 3명(교안수석·정산과장·디자이너) | 모듈 1 카드 P3 에서 `.codex/agents/` 로 데려갑니다. 추가 3명은 모듈 6 P14. 여기 있는 채로는 직원이 아닙니다 |

> `slack-server` 와 `office` 는 **완성된 프로그램이 이미 들어 있습니다.**
> 여러분이 만드는 건 직원(`.codex/agents/`)과 스킬(`.agents/skills/`)이고,
> 저 둘은 그걸 **불러 쓰고 보여주는** 도구입니다. 설정 한 줄씩만 채우면 됩니다.

---

## 카드가 세 벌 있습니다

| 카드 | 언제 | 몇 장 |
|---|---|---|
| `프롬프트카드.md` | **오늘 강의장에서** — 모듈 1~6 순서대로 | 기본 P1~P14 + 연결·확장 P15~P23 (P0·관문 카드는 환경점검이 대신합니다) |
| `프롬프트카드_연결.md` | 모듈 3.5 이후. 시간 남으면 강의장, 아니면 집 | 4장 (C1~C4) |
| `프롬프트카드_전문스킬.md` | 집에 가서, 내가 자주 하는 일부터 | 5장 (블로그·교안·메일·프로필·카드뉴스) |

**`프롬프트카드_연결.md` 를 빠뜨리지 마세요.** 모듈 6까지 하면 직원이 슬랙에서 대답은 하는데
**내 일정도 내 자료도 모릅니다.** 그 상태로 집에 가면 "만들긴 했는데 안 쓰게 되는" 자리에서 멈춥니다.
캘린더를 붙이고(C1) 내 제안서 폴더를 열어주면(C2~C4) 그때부터 실제로 씁니다.

---

## 이미 설치했다면 — 새 판으로 올리기 (2026-09-08)

강의장 판은 슬랙에서 **직원이 나를 남의 이름으로 부르고 · 팀장만 대답하고 · 이모지가 안 뜨는** 문제가 있었습니다.
PowerShell 에 아래 한 줄. 엔진만 바꾸고 내가 만든 직원·스킬·`personas.py`·`.env` 는 그대로 둡니다.

```powershell
irm https://raw.githubusercontent.com/mandoo356/wd-agentteam-kit-codex/main/update.ps1 | iex
```

## 합격 판정 받는 법

모듈이 끝날 때마다 검은 창에서:

```
py 점검.py 1     ← 모듈 1이 끝났으면
py 점검.py 2     ← 모듈 2가 끝났으면
```

✅ 가 전부 켜지면 다음으로 넘어갑니다. ❌ 가 있으면 **그 줄만** 고치면 돼요.
`py` 가 안 먹으면 `py -3 점검.py 1` 로 해보세요.

**`점검.py` 는 고치지 마세요.** 여러분이 만든 게 규격에 맞는지 보는 채점표입니다.

---

## 끝나고 내 회사 가져가기

스타터킷 폴더의 **`02_내작업_백업.bat`**을 더블클릭하세요.

- 내가 만든 `workspace`, 직원, 스킬, 오피스 회사 설정만 ZIP으로 묶습니다.
- `.env`, 슬랙 열쇠, 로그인 상태, `node_modules`는 넣지 않습니다.
- 자동 설치한 경우 `C:\Agent\04_LEARNER_BACKUP`에 저장됩니다.
- 만들어진 ZIP을 개인 USB나 클라우드에 보관하세요.

새 PC에서 복원할 때는 깨끗한 스타터킷을 먼저 설치하고 슬랙 서버와 오피스를 끈 뒤,
백업 ZIP의 `workspace`, `.codex`, `.agents`, `office`를 설치된 스타터킷에 덮어씁니다.
슬랙 `.env`·메일 `.env`와 구글·네이버 로그인은 보안상 백업되지 않으므로 새 PC에서 다시 설정합니다.

---

## 이름 끝에 `.txt` 가 붙은 파일

`_예시_직원.toml.txt`처럼 `.txt`로 끝나는 건 **보라고 넣어둔 견본**입니다.
채점에는 안 들어가니 열어보고 참고만 하세요. 여러분이 만드는 진짜 파일은 `.txt` 없이 만듭니다.

---

## 직원이 한 일은 자동으로 남고, 지우는 일은 꼭 묻습니다

스타터킷에 **안전장치**가 들어 있습니다 (`.codex/hooks.json` + `.codex/hooks/runtime.mjs`). 첫 실행 뒤 `/hooks`에서 내용을 검토하고 신뢰해야 작동합니다.

| 무엇 | 어떻게 |
|---|---|
| 자동 기록 | 내가 친 명령·직원이 만들고 고친 파일이 `workspace/기록/작업기록_<날짜>.md` 에 한 줄씩 남습니다 |
| 자동 저장 | 직원이 답을 마칠 때마다 git 에 자동 저장 지점이 생깁니다 (모듈 0 의 `git init` 이후). `git log --oneline` 으로 봅니다 |
| 삭제·덮어쓰기 차단 | 파일 삭제 · 기존 파일 덮어쓰기 · 직원/스킬/팀 규약 수정 · `git checkout`류는 훅이 차단합니다. 필요하면 사용자가 별도 터미널에서 직접 실행합니다 |
| 못 고치는 것 | `점검.py` · `.codex/hooks.json` · `.codex/hooks/` — 직원이 수정하려 하면 막힙니다 |
| 슬랙 | 위험 작업은 **막고** "별도 터미널에서 직접 진행해 주세요"라고 답합니다. 고장이 아닙니다 |

## 막혔을 때

| 증상 | 이렇게 |
|---|---|
| `py` 를 모르는 명령어래요 | Python 이 안 깔린 겁니다. 검은 창을 새로 열어보고, 그래도 안 되면 손 드세요 |
| 한글이 깨져 보여요 | 검은 창에 `chcp 65001` 치고 다시 실행 |
| AI가 엉뚱한 파일을 고쳤어요 | `git checkout .` 로 되돌립니다 (모듈 0에서 한 번 연습합니다) |
| ❌ 가 안 없어져요 | 카드를 한 번 더 그대로 붙여넣으세요. 덮어씁니다 |

---

**위드드림컨설팅** 교육용 스타터킷 · v0.1
